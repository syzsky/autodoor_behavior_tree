# 优化方案：放大镜功能设计

## 1 功能概述

### 1.1 需求背景

在区域选择、位置选择、颜色选择等操作中，用户需要精确定位屏幕上的特定位置或区域。当前的实现方式只显示鼠标光标，缺乏辅助定位工具，导致用户难以精确选择小区域或特定像素点。

### 1.2 功能目标

为所有节点的选择区域功能增加放大镜功能，类似微信截图时的放大镜效果，显示鼠标焦点区域的放大图像，帮助用户精确定位。

### 1.3 适用场景

| 场景     | 节点类型                | 字段类型            | 功能说明       |
| ------ | ------------------- | --------------- | ---------- |
| 检测区域选择 | OCRConditionNode    | RegionField     | 精确选择文字检测区域 |
| 检测区域选择 | ImageConditionNode  | RegionField     | 精确选择图像匹配区域 |
| 检测区域选择 | ColorConditionNode  | RegionField     | 精确选择颜色检测区域 |
| 检测区域选择 | NumberConditionNode | RegionField     | 精确选择数字识别区域 |
| 截图区域选择 | ImageConditionNode  | ScreenshotField | 精确选择截图区域   |
| 位置选择   | MouseClickNode      | PositionField   | 精确选择点击位置   |
| 位置选择   | MouseMoveNode       | PositionField   | 精确选择移动目标   |
| 颜色选择   | ColorConditionNode  | ColorField      | 精确选择目标颜色   |

***

## 2 核心组件设计

### 2.1 MagnifierWindow 类

```python
# bt_utils/magnifier.py

import tkinter as tk
from PIL import Image, ImageGrab, ImageTk
from typing import Optional, Tuple


class MagnifierWindow:
    """放大镜窗口组件"""
    
    def __init__(
        self,
        zoom_factor: int = 4,
        size: int = 150,
        show_crosshair: bool = True,
        show_color_info: bool = True
    ):
        """
        初始化放大镜
        
        Args:
            zoom_factor: 放大倍数（默认4倍）
            size: 放大镜窗口大小（默认150x150像素）
            show_crosshair: 是否显示十字准线
            show_color_info: 是否显示颜色信息
        """
        self.zoom_factor = zoom_factor
        self.size = size
        self.show_crosshair = show_crosshair
        self.show_color_info = show_color_info
        
        self.window: Optional[tk.Toplevel] = None
        self.canvas: Optional[tk.Canvas] = None
        self.info_label: Optional[tk.Label] = None
        self.last_screenshot: Optional[Image.Image] = None
        
    def show(self, x: int, y: int):
        """
        在指定位置显示放大镜
        
        Args:
            x: 鼠标X坐标
            y: 鼠标Y坐标
        """
        if self.window is not None:
            return
        
        self.window = tk.Toplevel()
        self.window.overrideredirect(True)
        self.window.attributes("-topmost", True)
        
        canvas_size = self.size
        info_height = 60 if self.show_color_info else 0
        
        self.window.geometry(f"{canvas_size}x{canvas_size + info_height}")
        
        self.canvas = tk.Canvas(
            self.window,
            width=canvas_size,
            height=canvas_size,
            bg="#000000",
            highlightthickness=0
        )
        self.canvas.pack()
        
        if self.show_color_info:
            self.info_label = tk.Label(
                self.window,
                text="",
                font=("Consolas", 9),
                bg="#2b2b2b",
                fg="#ffffff",
                anchor="w",
                padx=5
            )
            self.info_label.pack(fill="x")
        
        self.update(x, y)
    
    def update(self, x: int, y: int):
        """
        更新放大镜显示内容
        
        Args:
            x: 鼠标X坐标
            y: 鼠标Y坐标
        """
        if self.window is None or self.canvas is None:
            return
        
        window_x = x + 20
        window_y = y + 20
        
        screen_width = self.window.winfo_screenwidth()
        screen_height = self.window.winfo_screenheight()
        
        if window_x + self.size > screen_width:
            window_x = x - self.size - 20
        if window_y + self.size > screen_height:
            window_y = y - self.size - 20
        
        self.window.geometry(f"+{window_x}+{window_y}")
        
        capture_size = self.size // self.zoom_factor
        half_capture = capture_size // 2
        
        left = x - half_capture
        top = y - half_capture
        right = x + half_capture
        bottom = y + half_capture
        
        screenshot = ImageGrab.grab(bbox=(left, top, right, bottom))
        self.last_screenshot = screenshot
        
        enlarged = screenshot.resize(
            (self.size, self.size),
            Image.Resampling.NEAREST
        )
        
        self.photo = ImageTk.PhotoImage(enlarged)
        self.canvas.delete("all")
        self.canvas.create_image(0, 0, anchor="nw", image=self.photo)
        
        if self.show_crosshair:
            center = self.size // 2
            self.canvas.create_line(
                center, 0, center, self.size,
                fill="#ff0000", width=1, dash=(2, 2)
            )
            self.canvas.create_line(
                0, center, self.size, center,
                fill="#ff0000", width=1, dash=(2, 2)
            )
        
        if self.show_color_info and self.info_label:
            pixel = screenshot.getpixel((half_capture, half_capture))
            if isinstance(pixel, int):
                r, g, b = pixel, pixel, pixel
            elif len(pixel) == 4:
                r, g, b, a = pixel
            else:
                r, g, b = pixel[:3]
            
            hex_color = f"#{r:02x}{g:02x}{b:02x}"
            
            info_text = f"坐标: ({x}, {y})\n颜色: {hex_color}  RGB({r}, {g}, {b})"
            self.info_label.config(text=info_text)
    
    def hide(self):
        """隐藏放大镜窗口"""
        if self.window is not None:
            self.window.destroy()
            self.window = None
            self.canvas = None
            self.info_label = None
            self.last_screenshot = None
```

### 2.2 类图

```
┌─────────────────────────────────────────────────────────┐
│                    MagnifierWindow                       │
├─────────────────────────────────────────────────────────┤
│ - zoom_factor: int = 4                                   │
│ - size: int = 150                                        │
│ - show_crosshair: bool = True                            │
│ - show_color_info: bool = True                           │
│ - window: Optional[tk.Toplevel]                          │
│ - canvas: Optional[tk.Canvas]                            │
│ - info_label: Optional[tk.Label]                         │
│ - last_screenshot: Optional[Image.Image]                 │
├─────────────────────────────────────────────────────────┤
│ + __init__(zoom_factor, size, show_crosshair, ...)       │
│ + show(x: int, y: int)                                   │
│ + update(x: int, y: int)                                 │
│ + hide()                                                 │
│ - _get_pixel_color(x, y) -> Tuple[int, int, int]        │
└─────────────────────────────────────────────────────────┘
```

***

## 3 集成方案

### 3.1 修改 RegionField 类

```python
# bt_gui/bt_editor/property.py

class RegionField(FieldWidget):
    def _start_selection(self):
        import time
        from bt_utils.magnifier import MagnifierWindow
        
        try:
            import screeninfo
            
            self.app.iconify()
            time.sleep(0.2)
            
            monitors = screeninfo.get_monitors()
            min_x = min(monitor.x for monitor in monitors)
            min_y = min(monitor.y for monitor in monitors)
            max_x = max(monitor.x + monitor.width for monitor in monitors)
            max_y = max(monitor.y + monitor.height for monitor in monitors)
            
            select_window = tk.Toplevel(self.app)
            select_window.geometry(f"{max_x - min_x}x{max_y - min_y}+{min_x}+{min_y}")
            select_window.overrideredirect(True)
            select_window.attributes("-alpha", 0.3)
            select_window.attributes("-topmost", True)
            select_window.configure(cursor="cross", bg=self._dark_colors['primary'])
            
            canvas = tk.Canvas(select_window, bg=self._dark_colors['primary'], highlightthickness=0)
            canvas.pack(fill=tk.BOTH, expand=True)
            
            start_x_abs = [0]
            start_y_abs = [0]
            start_x_rel = [0]
            start_y_rel = [0]
            rect = [None]
            
            magnifier = MagnifierWindow(zoom_factor=4, size=150)
            
            def on_mouse_move(event):
                magnifier.update(event.x_root, event.y_root)
            
            def on_mouse_down(event):
                magnifier.hide()
                start_x_abs[0] = event.x_root
                start_y_abs[0] = event.y_root
                start_x_rel[0] = event.x_root - min_x
                start_y_rel[0] = event.y_root - min_y
                rect[0] = None
            
            def on_mouse_drag(event):
                magnifier.update(event.x_root, event.y_root)
                current_x_rel = event.x_root - min_x
                current_y_rel = event.y_root - min_y
                if rect[0]:
                    canvas.delete(rect[0])
                rect[0] = canvas.create_rectangle(
                    start_x_rel[0], start_y_rel[0], current_x_rel, current_y_rel,
                    outline="#FFFFFF", width=2, fill=""
                )
            
            def on_mouse_up(event):
                magnifier.hide()
                end_x_abs = event.x_root
                end_y_abs = event.y_root
                
                if abs(end_x_abs - start_x_abs[0]) < 3 or abs(end_y_abs - start_y_abs[0]) < 3:
                    messagebox.showwarning("警告", "选择的区域太小，请重新选择")
                    select_window.destroy()
                    self.app.deiconify()
                    return
                
                region = (
                    min(start_x_abs[0], end_x_abs),
                    min(start_y_abs[0], end_y_abs),
                    max(start_x_abs[0], end_x_abs),
                    max(start_y_abs[0], end_y_abs)
                )
                
                self.var.set(f"{region[0]},{region[1]},{region[2]},{region[3]}")
                self.on_change(self.key, list(region))
                
                select_window.destroy()
                self.app.deiconify()
            
            def on_escape(e):
                magnifier.hide()
                select_window.destroy()
                self.app.deiconify()
            
            canvas.bind("<Motion>", on_mouse_move)
            canvas.bind("<Button-1>", on_mouse_down)
            canvas.bind("<B1-Motion>", on_mouse_drag)
            canvas.bind("<ButtonRelease-1>", on_mouse_up)
            select_window.bind("<Escape>", on_escape)
            select_window.focus_set()
            
        except ImportError:
            self.app.deiconify()
            messagebox.showerror("错误", "screeninfo库未安装")
        except Exception as e:
            self.app.deiconify()
            messagebox.showerror("错误", f"区域选择失败: {str(e)}")
```

### 3.2 修改 ScreenshotField 类

与 RegionField 类似，在 `_start_screenshot_selection()` 方法中集成放大镜。

### 3.3 修改 PositionField 类

```python
class PositionField(FieldWidget):
    def _pick_position(self):
        import time
        from bt_utils.magnifier import MagnifierWindow
        
        try:
            import screeninfo
            
            self.app.iconify()
            time.sleep(0.2)
            
            monitors = screeninfo.get_monitors()
            min_x = min(monitor.x for monitor in monitors)
            min_y = min(monitor.y for monitor in monitors)
            max_x = max(monitor.x + monitor.width for monitor in monitors)
            max_y = max(monitor.y + monitor.height for monitor in monitors)
            
            select_window = tk.Toplevel(self.app)
            select_window.geometry(f"{max_x - min_x}x{max_y - min_y}+{min_x}+{min_y}")
            select_window.overrideredirect(True)
            select_window.attributes("-alpha", 0.2)
            select_window.attributes("-topmost", True)
            select_window.configure(cursor="crosshair", bg=self._dark_colors['primary'])
            
            label = tk.Label(
                select_window,
                text="点击选择位置",
                font=("Microsoft YaHei", 24),
                bg=self._dark_colors['primary'],
                fg="#FFFFFF"
            )
            label.place(relx=0.5, rely=0.5, anchor="center")
            
            magnifier = MagnifierWindow(zoom_factor=4, size=150)
            
            def on_mouse_move(event):
                magnifier.update(event.x_root, event.y_root)
            
            def on_click(event):
                magnifier.hide()
                x, y = event.x_root, event.y_root
                self.var.set(f"{x}, {y}")
                self.on_change(self.key, [x, y])
                select_window.destroy()
                self.app.deiconify()
            
            def on_escape(e):
                magnifier.hide()
                select_window.destroy()
                self.app.deiconify()
            
            select_window.bind("<Motion>", on_mouse_move)
            select_window.bind("<Button-1>", on_click)
            select_window.bind("<Escape>", on_escape)
            select_window.focus_set()
            
        except ImportError:
            self.app.deiconify()
            messagebox.showerror("错误", "screeninfo库未安装")
        except Exception as e:
            self.app.deiconify()
            messagebox.showerror("错误", f"位置选择失败: {str(e)}")
```

### 3.4 修改 ColorField 类

颜色选择器需要特殊处理，因为已经有颜色选择窗口，放大镜应该在颜色选择窗口中显示。

***

## 4 放大镜显示内容

### 4.1 界面布局

```
┌─────────────────────────┐
│                         │
│    [放大图像区域]        │
│    (4倍放大)            │
│         ┼               │  ← 十字准线
│                         │
│                         │
├─────────────────────────┤
│ 坐标: (1234, 567)       │
│ 颜色: #FF5733           │
│ RGB: (255, 87, 51)      │
└─────────────────────────┘
```

### 4.2 显示信息

| 信息类型 | 说明          | 示例            |
| ---- | ----------- | ------------- |
| 放大图像 | 鼠标周围区域的放大显示 | 4倍放大          |
| 十字准线 | 精确定位中心点     | 红色虚线          |
| 坐标信息 | 当前鼠标的屏幕坐标   | (1234, 567)   |
| 颜色信息 | 当前像素的颜色值    | #FF5733       |
| RGB值 | 颜色的RGB分量    | (255, 87, 51) |

***

## 5 技术实现要点

### 5.1 屏幕截图

```python
from PIL import ImageGrab

screenshot = ImageGrab.grab(bbox=(left, top, right, bottom))
```

### 5.2 图像放大

```python
from PIL import Image

enlarged = screenshot.resize(
    (new_width, new_height),
    Image.Resampling.NEAREST
)
```

### 5.3 窗口定位

```python
window_x = mouse_x + 20
window_y = mouse_y + 20

if window_x + size > screen_width:
    window_x = mouse_x - size - 20
if window_y + size > screen_height:
    window_y = mouse_y - size - 20
```

### 5.4 性能优化

| 优化方式 | 说明                       |
| ---- | ------------------------ |
| 局部截图 | 只截取鼠标周围小区域，而非整个屏幕        |
| 缓存机制 | 缓存最近截图，避免频繁截取            |
| 延迟更新 | 设置最小更新间隔（如30ms），避免过于频繁更新 |
| 异步处理 | 在后台线程中进行截图和图像处理          |

### 5.5 十字准线绘制

```python
center = size // 2

canvas.create_line(
    center, 0, center, size,
    fill="#ff0000", width=1, dash=(2, 2)
)

canvas.create_line(
    0, center, size, center,
    fill="#ff0000", width=1, dash=(2, 2)
)
```

***

## 6 配置参数

### 6.1 设置文件

```json
// config/settings.json
{
  "magnifier": {
    "enabled": true,
    "zoom_factor": 4,
    "size": 150,
    "show_color_info": true,
    "show_crosshair": true,
    "crosshair_color": "#ff0000",
    "crosshair_style": "dash",
    "update_interval_ms": 30
  }
}
```

### 6.2 参数说明

| 参数                   | 类型   | 默认值       | 说明                 |
| -------------------- | ---- | --------- | ------------------ |
| enabled              | bool | true      | 是否启用放大镜            |
| zoom\_factor         | int  | 4         | 放大倍数               |
| size                 | int  | 150       | 放大镜窗口大小（像素）        |
| show\_color\_info    | bool | true      | 是否显示颜色信息           |
| show\_crosshair      | bool | true      | 是否显示十字准线           |
| crosshair\_color     | str  | "#ff0000" | 十字准线颜色             |
| crosshair\_style     | str  | "dash"    | 十字准线样式（solid/dash） |
| update\_interval\_ms | int  | 30        | 最小更新间隔（毫秒）         |

***

## 7 实现优先级

### 7.1 第一阶段（核心功能）

- [ ] 创建 MagnifierWindow 类
- [ ] 集成到 RegionField
- [ ] 集成到 ScreenshotField
- [ ] 集成到 PositionField

### 7.2 第二阶段（优化完善）

- [ ] 添加配置参数支持
- [ ] 性能优化（缓存、延迟更新）
- [ ] 集成到 ColorField

### 7.3 第三阶段（扩展功能）

- [ ] 支持自定义放大倍数
- [ ] 支持自定义窗口大小
- [ ] 支持更多显示信息（如窗口标题）

***

## 8 测试要点

### 8.1 功能测试

| 测试项  | 测试内容          | 预期结果     |
| ---- | ------------- | -------- |
| 显示测试 | 鼠标移动时放大镜是否跟随  | 正确跟随显示   |
| 定位测试 | 放大镜窗口是否避免超出屏幕 | 自动调整位置   |
| 放大测试 | 放大图像是否清晰      | 正确放大显示   |
| 颜色测试 | 颜色信息是否准确      | 正确显示RGB值 |
| 坐标测试 | 坐标信息是否准确      | 正确显示坐标   |

### 8.2 性能测试

| 测试项   | 测试内容         | 预期结果     |
| ----- | ------------ | -------- |
| 更新频率  | 快速移动鼠标时的响应   | 无明显延迟    |
| 内存占用  | 长时间使用的内存情况   | 内存稳定，无泄漏 |
| CPU占用 | 放大镜显示时的CPU使用 | CPU占用合理  |

### 8.3 兼容性测试

| 测试项   | 测试内容        | 预期结果   |
| ----- | ----------- | ------ |
| 多显示器  | 多显示器环境下的表现  | 正确处理坐标 |
| 不同分辨率 | 不同屏幕分辨率下的表现 | 正确显示   |
| 不同DPI | 高DPI屏幕下的表现  | 正确缩放   |

***

## 9 注意事项

1. **性能考虑**：放大镜需要频繁截图和更新，应注意性能优化
2. **多显示器支持**：需要正确处理多显示器环境下的坐标
3. **高DPI适配**：在高DPI屏幕上需要正确处理缩放
4. **用户体验**：放大镜窗口位置应避免遮挡用户视线
5. **配置灵活性**：应允许用户自定义放大镜的各项参数

