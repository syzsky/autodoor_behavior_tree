# AutoDoor 行为树系统 — 详细实现方法

## 1 行为树节点类型实现

### 1.1 节点基类 (Node)

所有节点的抽象基类，定义通用接口和默认行为：

```python
class Node(ABC):
    NODE_TYPE = "Node"                    # 类级别节点类型标识
    
    def __init__(self, node_id=None, config=None):
        self.node_id = node_id or uuid4()[:8]   # 唯一标识
        self.config = config or NodeConfig()     # 节点配置
        self.status = NodeStatus.SUCCESS         # 当前状态
        self.children: List[Node] = []           # 子节点列表
        self.parent: Optional[Node] = None       # 父节点引用
        self._tick_count = 0                     # tick计数
    
    @abstractmethod
    def tick(self, context: ExecutionContext) -> NodeStatus:
        """核心方法：执行节点逻辑，返回执行状态"""
        pass
```

**设计要点：**
- 使用 `uuid4()[:8]` 生成短 ID，兼顾唯一性与可读性
- `config` 使用 `NodeConfig` 数据类，采用扁平化结构存储节点参数（与原项目兼容）
- `children` 列表维护子节点有序集合
- `tick()` 是纯抽象方法，子类必须实现
- `reset(reset_counters=True)` 方法支持控制是否重置计数器，用于 retry/repeat 场景

**节点重置机制：**

节点重置是行为树状态管理的核心，需要区分不同场景：

| 场景 | 方法 | 行为 |
|------|------|------|
| 完全重置 | `reset(reset_counters=True)` | 重置所有状态和计数器 |
| 重试重置 | `_reset_for_retry()` | 设置 `status=RUNNING`，保留重试计数器 |
| 重复重置 | `_reset_for_repeat()` | 调用 `reset(False)`，保留重复计数器 |

**重要：** `_reset_for_retry()` 和 `_reset_for_repeat()` 必须将 `status` 设置为 `RUNNING`，确保节点在重试/重复期间返回正确状态。

**`_reset_for_retry()` 完整实现（V1.3.2b 修复）：**

```python
def _reset_for_retry(self) -> None:
    """重试时重置状态（保留重试计数器）
    
    V1.3.2b 修复：必须重置所有执行状态字段，否则会导致：
    - ConditionNode 跳过条件检查（_children_running 未重置）
    - SequenceNode 从错误索引继续（current_index 未重置）
    - ConditionNode 返回缓存状态（_last_check_time 未正确重置）
    """
    self.status = NodeStatus.RUNNING
    self._tick_count = 0
    self._start_time = None
    self._child_index = 0
    self._children_running = False
    
    # CompositeNode 子类字段
    if hasattr(self, 'current_index'):
        self.current_index = 0
    if hasattr(self, '_last_child_finish_time'):
        self._last_child_finish_time = None
    
    # 递归重置所有子节点
    for child in self.children:
        child.reset()
```

**各组合节点的 `_reset_for_retry()` 覆盖：**

| 节点类型 | 额外重置字段 |
|----------|--------------|
| SequenceNode | `_last_child_finish_time = None` |
| SelectorNode | `_last_child_time = 0` |
| ParallelNode | `cached_statuses.clear()` |
| RandomNode | `_executed_indices.clear()`, `_success_indices.clear()`, `_failure_indices.clear()`, `_current_running_index = None` |

**ConditionNode.reset() 修复（V1.3.2b）：**

```python
def reset(self, reset_counters: bool = True) -> None:
    super().reset(reset_counters)
    # 关键：必须与构造函数一致，确保首次 tick 执行条件检查
    self._last_check_time = -self.check_interval_ms - 1  # 而非 0
```

### 1.2 复合节点 (CompositeNode)

复合节点是行为树的核心控制结构，包含多个子节点并按特定策略执行。

#### 1.2.1 顺序节点 (SequenceNode)

**语义：** 按顺序依次执行子节点，所有子节点成功才返回成功，任一失败立即返回失败。

**支持参数：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| continue_on_failure | 失败后是否继续执行后续子节点 | False |
| childinterval | 子节点执行间隔（毫秒） | 0 |
| childinterval_random | 子节点间隔随机范围（±毫秒） | 0 |

```
执行流程:
  child[0] ──SUCCESS──► child[1] ──SUCCESS──► child[2] ──SUCCESS──► return SUCCESS
     │                      │                      │
   FAILURE                FAILURE                FAILURE
     │                      │                      │
     ▼                      ▼                      ▼
  return FAILURE        return FAILURE         return FAILURE
  
  (若 continue_on_failure=True，失败后继续执行下一个子节点)
```

**实现伪代码：**

```
function SequenceNode.tick(context):
    if children is empty:
        return SUCCESS
    
    while current_index < len(children):
        child = children[current_index]
        
        if not child.config.enabled:
            current_index += 1
            continue
        
        status = child.tick(context)
        
        if status == RUNNING:
            return RUNNING          // 等待当前子节点完成
        
        if status == FAILURE:
            if continue_on_failure:
                current_index += 1  // 继续执行下一个子节点
                continue
            current_index = 0       // 重置，下次从头开始
            return FAILURE          // 短路：任一失败即失败
        
        // 子节点执行成功，添加间隔
        if childinterval > 0:
            actual_interval = get_random_duration(childinterval, childinterval_random)
            sleep(actual_interval / 1000.0)
        
        current_index += 1          // 成功则继续下一个
    
    current_index = 0               // 全部成功，重置
    return SUCCESS
```

**关键实现细节：**
- `current_index` 记录当前执行位置，支持 RUNNING 状态的记忆
- 失败时重置 `current_index = 0`，确保下次 tick 从头开始
- 跳过 `enabled = False` 的子节点
- `continue_on_failure=True` 时，失败后继续执行后续子节点
- `childinterval` 支持在子节点之间添加延迟，模拟更自然的操作

#### 1.2.2 选择节点 (SelectorNode)

**语义：** 按顺序执行子节点，任一成功即返回成功，全部失败才返回失败。

**支持参数：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| childinterval | 子节点执行间隔（毫秒） | 0 |
| childinterval_random | 子节点间隔随机范围（±毫秒） | 0 |

```
执行流程:
  child[0] ──FAILURE──► child[1] ──FAILURE──► child[2] ──FAILURE──► return FAILURE
     │                      │                      │
   SUCCESS                SUCCESS                SUCCESS
     │                      │                      │
     ▼                      ▼                      ▼
  return SUCCESS         return SUCCESS          return SUCCESS
```

**实现伪代码：**

```
function SelectorNode.tick(context):
    if children is empty:
        return FAILURE
    
    while current_index < len(children):
        child = children[current_index]
        
        if not child.config.enabled:
            current_index += 1
            continue
        
        status = child.tick(context)
        
        if status == RUNNING:
            return RUNNING
        
        if status == SUCCESS:
            current_index = 0       // 重置
            return SUCCESS          // 短路：任一成功即成功
        
        // 子节点执行失败，添加间隔后尝试下一个
        if childinterval > 0:
            actual_interval = get_random_duration(childinterval, childinterval_random)
            sleep(actual_interval / 1000.0)
        
        current_index += 1          // 失败则尝试下一个
    
    current_index = 0
    return FAILURE                  // 全部失败
```

#### 1.2.3 并行节点 (ParallelNode)

**语义：** 同时执行所有子节点，根据成功策略决定返回条件。

**成功策略：**
- `require_all`：所有启用的子节点成功才返回成功
- `require_one`：任一子节点成功即返回成功

**实现伪代码：**

```
function ParallelNode.tick(context):
    if children is empty:
        return SUCCESS
    
    success_count = 0
    failure_count = 0
    running_children = []
    
    for i, child in enumerate(children):
        if not child.config.enabled:
            continue
        
        // 检查缓存状态（已完成的子节点不再重复执行）
        if i in cached_statuses:
            status = cached_statuses[i]
            if status == SUCCESS: success_count += 1; continue
            if status == FAILURE: failure_count += 1; continue
        
        status = child.tick(context)
        
        if status == SUCCESS:
            cached_statuses[i] = SUCCESS
            success_count += 1
        elif status == FAILURE:
            cached_statuses[i] = FAILURE
            failure_count += 1
        elif status == RUNNING:
            running_children.append(child)
    
    // require_one 策略：任一成功即返回
    if success_policy == REQUIRE_ONE and success_count > 0:
        abort_running(context, running_children)
        return SUCCESS
    
    // 仍有运行中的子节点
    if running_children:
        return RUNNING
    
    // 所有子节点执行完毕
    if success_policy == REQUIRE_ALL:
        return SUCCESS if success_count == enabled_count else FAILURE
    else:
        return SUCCESS if success_count > 0 else FAILURE
```

**关键实现细节：**
- `cached_statuses` 缓存已完成子节点的状态，避免重复 tick
- `require_one` 模式下，一旦有子节点成功，立即中止其他运行中的子节点
- `reset()` 时清空缓存
- `_reset_for_retry()` 和 `_reset_for_repeat()` 时也清空缓存，确保重试/重复执行时重新评估所有子节点

**日志输出实现：**

```python
def _tick_internal(self, context: ExecutionContext) -> NodeStatus:
    from bt_utils.log_manager import LogManager
    
    if not self.children:
        LogManager.instance().log_success(
            node_type="并行节点",
            node_name=self.name
        )
        return NodeStatus.SUCCESS
    
    # ... 执行逻辑 ...
    
    if success_count == enabled_count:
        LogManager.instance().log_success(
            node_type="并行节点",
            node_name=self.name
        )
        return NodeStatus.SUCCESS
    else:
        LogManager.instance().log_failure(
            node_type="并行节点",
            node_name=self.name,
            reason=f"成功 {success_count}/{enabled_count} 个子节点"
        )
        return NodeStatus.FAILURE
```

#### 1.2.4 随机节点 (RandomNode)

**语义：** 随机执行子节点，根据成功策略决定返回条件。

**成功策略：**
- `require_all`：所有启用的子节点成功才返回成功
- `require_one`：任一子节点成功即返回成功

**参数配置：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| success_policy | 成功策略（require_all/require_one） | "require_all" |
| continue_on_failure | 失败后是否继续执行（仅 require_all 策略生效） | False |
| fully_random | 完全随机模式（True 时已执行的子节点仍可被选中） | False |

**执行流程：**

```
执行流程（非完全随机模式）:
  随机池: [child[0], child[1], child[2], ...]
      │
      ▼
  随机选择 child[i] 执行
      │
      ├── SUCCESS: 记录成功，从随机池移除
      │   └── require_one 策略: 立即返回 SUCCESS
      │
      └── FAILURE: 记录失败，从随机池移除
          └── continue_on_failure=False 且 require_all 策略: 立即返回 FAILURE
      │
      ▼
  随机池为空？
      │
      ├── 是: 根据成功策略返回最终状态
      └── 否: 继续随机选择执行
```

**实现伪代码：**

```
function RandomNode.tick(context):
    if children is empty:
        log_success(node_type="随机节点", node_name=self.name)
        return SUCCESS
    
    // 初始化启用的子节点索引
    if enabled_indices is empty:
        enabled_indices = [i for i, child in enumerate(children) if child.config.enabled]
    
    if enabled_indices is empty:
        log_success(node_type="随机节点", node_name=self.name)
        return SUCCESS
    
    // 恢复运行中的子节点
    if current_running_index is not None:
        child = children[current_running_index]
        status = child.tick(context)
        
        if status == RUNNING:
            return RUNNING
        
        if status == SUCCESS:
            success_indices.add(current_running_index)
            if success_policy == REQUIRE_ONE:
                log_success(node_type="随机节点", node_name=self.name)
                return SUCCESS
        elif status == FAILURE:
            failure_indices.add(current_running_index)
            if not continue_on_failure and success_policy == REQUIRE_ALL:
                log_failure(node_type="随机节点", node_name=self.name, 
                           reason=f"子节点 '{child.name}' 执行失败")
                return FAILURE
        
        current_running_index = None
    
    // 获取可执行的子节点索引
    available_indices = get_available_indices()
    
    if available_indices is empty:
        return determine_final_status()
    
    // 完全随机模式：所有子节点都执行过一次后结束
    if fully_random and executed_indices == set(enabled_indices):
        return determine_final_status()
    
    // 随机选择下一个子节点
    next_index = random.choice(available_indices)
    executed_indices.add(next_index)
    current_running_index = next_index
    
    child = children[next_index]
    status = child.tick(context)
    
    if status == RUNNING:
        return RUNNING
    
    if status == SUCCESS:
        success_indices.add(next_index)
        if success_policy == REQUIRE_ONE:
            log_success(node_type="随机节点", node_name=self.name)
            return SUCCESS
    elif status == FAILURE:
        failure_indices.add(next_index)
        if not continue_on_failure and success_policy == REQUIRE_ALL:
            log_failure(node_type="随机节点", node_name=self.name,
                       reason=f"子节点 '{child.name}' 执行失败")
            return FAILURE
    
    current_running_index = None
    
    // 检查是否还有可执行的子节点
    available_indices = get_available_indices()
    if available_indices is empty:
        return determine_final_status()
    
    return RUNNING

function get_available_indices():
    if fully_random:
        return enabled_indices  // 完全随机：所有启用的子节点都可被选中
    else:
        return [i for i in enabled_indices if i not in executed_indices]  // 非完全随机：已执行的不再被选中

function determine_final_status():
    enabled_count = len(enabled_indices)
    success_count = len(success_indices)
    
    if success_policy == REQUIRE_ALL:
        if success_count == enabled_count:
            log_success(node_type="随机节点", node_name=self.name)
            return SUCCESS
        else:
            log_failure(node_type="随机节点", node_name=self.name,
                       reason=f"成功 {success_count}/{enabled_count} 个子节点")
            return FAILURE
    else:
        if success_count > 0:
            log_success(node_type="随机节点", node_name=self.name)
            return SUCCESS
        else:
            log_failure(node_type="随机节点", node_name=self.name,
                       reason="所有子节点都执行失败")
            return FAILURE
```

**关键实现细节：**
- `_executed_indices`：记录已执行的子节点索引（用于非完全随机模式）
- `_success_indices`：记录成功的子节点索引
- `_failure_indices`：记录失败的子节点索引
- `_current_running_index`：当前正在执行的子节点索引（用于恢复 RUNNING 状态）
- `fully_random=True` 时，子节点可被重复选中，直到所有子节点都执行过一次
- `continue_on_failure` 仅在 `require_all` 策略下生效

**日志输出实现：**

```python
def _tick_internal(self, context: ExecutionContext) -> NodeStatus:
    from bt_utils.log_manager import LogManager
    import random
    
    if not self.children:
        LogManager.instance().log_success(
            node_type="随机节点",
            node_name=self.name
        )
        return NodeStatus.SUCCESS
    
    # ... 执行逻辑 ...
    
    enabled_count = len(self._enabled_indices)
    success_count = len(self._success_indices)
    
    if self.success_policy == self.SUCCESS_POLICY_ALL:
        if success_count == enabled_count:
            LogManager.instance().log_success(
                node_type="随机节点",
                node_name=self.name
            )
            return NodeStatus.SUCCESS
        else:
            LogManager.instance().log_failure(
                node_type="随机节点",
                node_name=self.name,
                reason=f"成功 {success_count}/{enabled_count} 个子节点"
            )
            return NodeStatus.FAILURE
    else:
        if success_count > 0:
            LogManager.instance().log_success(
                node_type="随机节点",
                node_name=self.name
            )
            return NodeStatus.SUCCESS
        else:
            LogManager.instance().log_failure(
                node_type="随机节点",
                node_name=self.name,
                reason="所有子节点都执行失败"
            )
            return NodeStatus.FAILURE
```

#### 1.2.5 开始节点 (StartNode)

**语义：** 作为行为树的根节点，顺序执行所有子节点，但与 SequenceNode 不同的是，子节点失败后会继续执行后续子节点。

**继承关系：** StartNode 继承自 CompositeNode，使用 `current_index` 实现顺序执行逻辑。

```
执行流程:
  child[0] ──SUCCESS/FAILURE──► child[1] ──SUCCESS/FAILURE──► child[2] ──► ...
     │                              │                              │
     └──────────────────────────────┴──────────────────────────────┘
                    所有子节点执行完毕后返回
     │
     ▼
  if has_failure: return FAILURE
  else: return SUCCESS
```

**与 SequenceNode 的区别：**

| 特性 | SequenceNode | StartNode |
|------|--------------|-----------|
| 失败处理 | 任一失败立即返回 | 失败后继续执行 |
| 执行模式 | 短路执行 | 全部执行 |
| 用途 | 条件序列 | 行为树根节点 |
| 状态记忆 | 使用 current_index | 使用 current_index |

**实现伪代码：**

```
function StartNode.tick(context):
    if children is empty:
        log_success(node_type="开始节点", node_name=self.name)
        return SUCCESS
    
    has_failure = false
    
    while current_index < len(children):
        child = children[current_index]
        
        if not child.config.enabled:
            current_index += 1
            continue
        
        status = child.tick(context)
        
        if status == RUNNING:
            return RUNNING    // 等待当前子节点完成
        
        if status == FAILURE:
            has_failure = true
        // SUCCESS: 继续执行下一个子节点
        
        current_index += 1
    
    // 所有子节点执行完毕，重置索引
    current_index = 0
    
    // 注意：StartNode 始终返回 SUCCESS，即使有子节点失败
    // 这确保行为树可以继续执行，不会因单个节点失败而中断
    log_success(node_type="开始节点", node_name=self.name)
    return SUCCESS
```

**关键实现细节：**
- `current_index` 记录当前执行位置，支持 RUNNING 状态的记忆
- 子节点失败时设置 `has_failure = true`，但继续执行后续子节点
- 所有子节点执行完毕后重置 `current_index = 0`
- 跳过 `enabled = False` 的子节点
- **始终返回 SUCCESS**：即使有子节点失败，StartNode 也返回 SUCCESS，确保行为树可以继续执行

### 1.3 条件节点 (ConditionNode)

条件节点检测环境状态，返回 SUCCESS 或 FAILURE，不产生副作用。

**基类实现：**

```python
class ConditionNode(Node):
    def __init__(self, node_id=None, config=None):
        super().__init__(node_id, config)
        self.invert = self.config.get("invert", False)           # 结果取反
        self.check_interval_ms = self.config.get("check_interval_ms", 300)  # 检测间隔
        self._last_check_time = 0
        self._children_running = False  # 子节点执行状态追踪标志
        
        # 坐标偏移参数 (向后兼容：支持旧的 offset_x/offset_y 格式)
        offset = self.config.get("offset", None)
        if offset is not None:
            if isinstance(offset, (list, tuple)) and len(offset) >= 2:
                self.offset = (int(offset[0]), int(offset[1]))
            else:
                self.offset = (0, 0)
        else:
            # 向后兼容：从旧的 offset_x/offset_y 读取
            offset_x = self.config.get_int("offset_x", 0)
            offset_y = self.config.get_int("offset_y", 0)
            self.offset = (offset_x, offset_y)
    
    def _apply_offset(self, position: tuple) -> tuple:
        """应用坐标偏移"""
        if position is None:
            return None
        return (position[0] + self.offset[0], position[1] + self.offset[1])
    
    def tick(self, context):
        // 检查子节点是否正在执行
        if self._children_running and self.children:
            return self._execute_children(context)
        
        // 检测间隔控制
        current_time = context.elapsed_time * 1000
        if current_time - self._last_check_time < self.check_interval_ms:
            return self.status    // 未到检测时间，返回上次状态
        
        self._last_check_time = current_time
        result = self._check_condition(context)   // 子类实现
        
        if self.invert:
            result = not result
        
        status = SUCCESS if result else FAILURE
        self.status = status
        
        // 条件成功时执行子节点
        if status == SUCCESS and self.children:
            // 通知 GUI 节点状态为 SUCCESS
            context.notify_node_status(self.node_id, "success")
            self._children_running = True
            return self._execute_children(context)
        
        return status
    
    @abstractmethod
    def _check_condition(self, context) -> bool:
        pass
    
    # 通用辅助方法（已提取到基类）
    def _validate_region(self, region) -> bool:
        """验证区域配置是否有效"""
        if region is None:
            return True
        if isinstance(region, (list, tuple)) and len(region) == 4:
            return all(isinstance(x, int) for x in region)
        return False
    
    def _get_region_image(self, context):
        """获取区域图像（截图+裁剪）"""
        try:
            return context.get_screenshot(self.region)
        except Exception as e:
            self._log_condition_result(False, f"截图失败: {e}")
            return None
    
    def _log_condition_result(self, success, reason=None, extra_info=None):
        """记录条件检测结果日志"""
        from bt_utils.log_manager import LogManager
        if success:
            LogManager().log_success(node_type=self.NODE_TYPE, node_name=self.name)
        else:
            log_reason = reason or "条件不满足"
            if extra_info:
                log_reason += f"，{extra_info}"
            LogManager().log_failure(node_type=self.NODE_TYPE, node_name=self.name, reason=log_reason)
    
    def _save_position(self, context, position):
        """保存检测位置到黑板"""
        if position and hasattr(context, 'blackboard'):
            context.blackboard.set(self.position_key, position)
```

**子节点执行状态追踪：**

ConditionNode 使用 `_children_running` 标志追踪子节点的执行状态：

| 属性 | 说明 |
|------|------|
| `_children_running` | 布尔标志，标记子节点是否正在执行 |

**状态追踪流程：**

```
ConditionNode 执行流程:
┌─────────────────────────────────────────────────────────────────────┐
│  1. 首次 tick:                                                       │
│     - 检查 _children_running 标志                                    │
│     - 若为 False，执行条件检测                                        │
│     - 若条件成功且有子节点:                                           │
│       - 通知 GUI 节点状态为 SUCCESS                                   │
│       - 设置 _children_running = True                                │
│       - 开始执行子节点                                                │
│                                                                      │
│  2. 后续 tick:                                                       │
│     - 检查 _children_running 标志                                    │
│     - 若为 True，跳过条件检测，继续执行子节点                          │
│     - 子节点执行完成后，重置 _children_running = False                │
└─────────────────────────────────────────────────────────────────────┘
```

**装饰参数（所有条件节点通用）：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| invert | 结果取反 | False |
| retry_count | 失败重试次数 | 0 |
| timeout_ms | 超时时间 | 0 |
| check_interval_ms | 检测间隔 | 300 |
| offset | 坐标偏移 (x, y) | (0, 0) |

**坐标偏移实现：**

条件节点支持坐标偏移功能，可以将检测到的位置偏移后再保存到黑板：

```python
def _check_condition(self, context: ExecutionContext) -> bool:
    # ... 执行检测逻辑 ...
    
    if found and position:
        # 应用偏移后再保存到黑板
        adjusted_position = self._apply_offset(position)
        context.blackboard.set(self.position_key, adjusted_position)
    
    return found
```

**向后兼容性：**

为兼容旧版本 JSON 文件，offset 参数支持两种格式：

| 格式 | 示例 | 说明 |
|------|------|------|
| 新格式 | `"offset": [10, 20]` | 推荐，单一参数 |
| 旧格式 | `"offset_x": 10, "offset_y": 20` | 向后兼容 |

序列化时统一输出新格式：

```python
def to_dict(self) -> Dict[str, Any]:
    data = super().to_dict()
    data["config"]["offset"] = list(self.offset)
    return data
```

#### 1.3.1 文字检测节点 (OCRConditionNode)

```
执行流程:
  1. context.get_screenshot(region)     → 获取截图
  2. context.perform_ocr(image, keywords, language)  → OCR识别
  3. 若找到关键词:
     - blackboard.set(position_key, position)  → 记录位置（position_key 可配置）
     - return True
  4. 若未找到:
     - 输出识别到的所有文本内容
     - return False
```

**参数说明：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| region | 检测区域 | 全屏 |
| keywords | 检测关键词 | — |
| language | OCR语言（简体中文/English/繁体中文） | "简体中文" |
| preprocess_mode | 图像预处理模式（默认/复杂色彩） | "默认" |
| position_key | 位置保存的黑板变量名 | "last_detection_position" |
| offset | 坐标偏移量 | (0, 0) |

**语言映射：**

| GUI显示 | OCR引擎值 |
|---------|----------|
| 简体中文 | chi_sim |
| English | eng |
| 繁体中文 | chi_tra |

**日志输出实现（含识别文本）：**

```python
LANGUAGE_MAP = {
    "English": "eng",
    "简体中文": "chi_sim",
    "繁体中文": "chi_tra",
}

def _check_condition(self, context: ExecutionContext) -> bool:
    from bt_utils.log_manager import LogManager
    
    # ... 获取截图 ...
    language_code = LANGUAGE_MAP.get(self.language, "chi_sim")
    
    found, position, all_text = context.perform_ocr(
        screenshot, self.keywords, language_code, self.preprocess_mode
    )
    
    if found:
        if position:
            context.blackboard.set(self.position_key, position)
        LogManager.instance().log_success(
            node_type="文字检测节点",
            node_name=self.name
        )
        return True
    else:
        reason = f"未找到关键词: '{self.keywords}'"
        if all_text:
            reason += f"，识别到的文本: {all_text}"
        LogManager.instance().log_failure(
            node_type="文字检测节点",
            node_name=self.name,
            reason=reason
        )
        return False
```

#### 1.3.2 图像匹配节点 (ImageConditionNode)

```
执行流程:
  1. Image.open(template_path)          → 加载模板图像（首次缓存）
  2. context.get_screenshot(region)     → 获取截图
  3. ImageProcessor.find_template(screenshot, template, threshold)  → 模板匹配
     返回: (found: bool, position: Optional[Tuple], confidence: float)
  4. 若匹配成功:
     - blackboard.set(position_key, position)  → 记录位置（position_key 可配置）
     - return True
  5. 若匹配失败:
     - 输出置信度和阈值信息
     - return False
```

**参数说明：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| region | 检测区域 | 全屏 |
| template_path | 模板图像路径 | — |
| threshold | 匹配阈值（支持百分比和小数两种格式） | 0.8 |
| position_key | 位置保存的黑板变量名 | "last_detection_position" |

**threshold 参数兼容性：**

为兼容旧项目（autodoor）的配置格式，threshold 参数支持两种输入格式：
- **百分比格式**：`65` 表示 65%，自动转换为 `0.65`
- **小数格式**：`0.65` 直接使用

转换逻辑：若 `threshold > 1.0`，则 `threshold = threshold / 100.0`

**ImageProcessor.find_template 返回值变更：**

```python
@staticmethod
def find_template(source: Image.Image, template: Image.Image,
                  threshold: float = 0.8) -> Tuple[bool, Optional[Tuple[int, int]], float]:
    """
    模板匹配
    
    Returns:
        Tuple[bool, Optional[Tuple[int, int]], float]: 
            - found: 是否找到匹配
            - position: 匹配位置（中心点坐标）
            - confidence: 最高置信度值
    """
    # ... OpenCV matchTemplate 实现 ...
    
    if max_val >= threshold:
        return True, (center_x, center_y), max_val
    return False, None, max_val
```

**日志输出实现（含置信度）：**

```python
def _check_condition(self, context: ExecutionContext) -> bool:
    from bt_utils.log_manager import LogManager
    
    # ... 获取截图和模板 ...
    
    found, position, confidence = ImageProcessor.find_template(
        screenshot, self._template_image, self.threshold
    )
    
    if found:
        if position:
            context.blackboard.set(self.position_key, position)
        LogManager.instance().log_success(
            node_type="图像检测节点",
            node_name=self.name
        )
        return True
    else:
        LogManager.instance().log_failure(
            node_type="图像检测节点",
            node_name=self.name,
            reason=f"未找到匹配的图像，最高置信度: {confidence:.2%}，阈值: {self.threshold:.2%}"
        )
        return False
```

#### 1.3.3 颜色检测节点 (ColorConditionNode)

```
执行流程:
  1. context.get_screenshot(region)     → 获取截图
  2. ImageProcessor.find_color(screenshot, target_color, tolerance)  → 颜色搜索
  3. 若找到匹配颜色且匹配像素数 >= min_pixels:
     - blackboard.set(position_key, position)  → 记录位置（position_key 可配置）
     - return True
  4. 若未找到:
     - return False
```

**参数说明：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| region | 检测区域 | 全屏 |
| target_color | 目标颜色 | — |
| tolerance | 颜色容差 | 30 |
| min_pixels | 最小匹配像素数 | 1 |
| position_key | 位置保存的黑板变量名 | "last_detection_position" |

#### 1.3.4 数字比较节点 (NumberConditionNode)

```
执行流程:
  1. context.get_screenshot(region)     → 获取截图
  2. OCRManager.recognize_number_with_position(screenshot, language, preprocess_mode, extract_mode, extract_pattern, min_confidence)  → 数字OCR
  3. 若识别成功:
     - blackboard.set("last_number_value", value)
     - blackboard.set(position_key, position)  → 记录检测位置（position_key 可配置）
     - blackboard.set(value_key, value)        → 记录数字值（value_key 可配置）
     - OPERATORS[compare_mode](value, threshold)  → 比较运算
  4. 若识别失败:
     - 输出识别到的所有文本内容
     - return False
```

**参数说明：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| region | 检测区域 | 全屏 |
| preprocess_mode | 预处理模式（默认/复杂色彩） | "默认" |
| extract_mode | 提取模式（无规则/x/y/自定义） | "无规则" |
| extract_pattern | 自定义提取模式（使用*作为通配符） | "" |
| compare_mode | 比较运算符（>、<、>=、<=、==、!=） | ">" |
| threshold | 比较目标值 | 0 |
| min_confidence | 最小置信度 | 0.5 |
| value_key | 数字值保存的黑板变量名 | "last_number_value" |
| position_key | 检测位置保存的黑板变量名 | "last_detection_position" |
| offset | 坐标偏移量（相对偏移） | (0, 0) |

**提取模式说明：**

| 模式 | 说明 | 示例 |
|------|------|------|
| 无规则 | 提取第一个数字 | "HP:100/200" → 100 |
| x/y | 提取分数形式的分子 | "3/5" → 3 |
| 自定义 | 使用 `*` 通配符定义提取模式 | "HP:*" 匹配 "HP:100" → 100 |

**日志输出实现（含识别文本）：**

```python
def _check_condition(self, context: ExecutionContext) -> bool:
    from bt_utils.log_manager import LogManager
    
    # ... 获取截图 ...
    
    found, value, all_text, position = OCRManager.instance().recognize_number_with_position(
        screenshot, self.language, self.preprocess_mode, 
        self.extract_mode, self.extract_pattern, self.min_confidence
    )
    
    if found:
        context.blackboard.set("last_number_value", value)
        if position:
            context.blackboard.set(self.position_key, position)
        if self.value_key:
            context.blackboard.set(self.value_key, value)
        
        result = OPERATORS[self.compare_mode](value, self.threshold)
        if result:
            LogManager.instance().log_success(
                node_type="数字节点",
                node_name=self.name
            )
            return True
        else:
            LogManager.instance().log_failure(
                node_type="数字节点",
                node_name=self.name,
                reason=f"值 {value} 不满足 {self.compare_mode} {self.threshold}"
            )
            return False
    else:
        reason = "未识别到数字"
        if all_text:
            reason += f"，识别到的文本: {all_text}"
        LogManager.instance().log_failure(
            node_type="数字节点",
            node_name=self.name,
            reason=reason
        )
        return False
```

#### 1.3.5 变量判断节点 (VariableConditionNode)

```
执行流程:
  1. blackboard.get(variable_name)      → 获取变量值
  2. OPERATORS[operator](value, target_value)  → 比较运算
  3. 支持运算符: ==, !=, >, >=, <, <=, contains, exists, not_exists, not_contains
```

#### 1.3.6 文本提取节点 (TextExtractNode)

**语义：** 通过 OCR 识别屏幕指定区域的文字内容，提取文本并保存到黑板变量，供后续节点使用。

**支持参数：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| region | 检测区域 | None |
| extract_mode | 提取模式（all/keywords） | all |
| keywords | 关键词（keywords模式时使用） | "" |
| language | OCR识别语言 | chi_sim |
| preprocess_mode | 图像预处理模式（normal/game） | normal |
| output_key | 提取文本输出变量名 | last_extracted_text |
| save_all_text | 是否保存全部OCR文本 | False |
| all_text_key | 全部文本变量名 | all_ocr_text |
| save_position | 是否保存检测位置 | True |
| position_key | 位置变量名 | last_detection_position |
| offset | 坐标偏移 | (0, 0) |

**中文显示映射：**

| 中文显示 | 内部值 | 说明 |
|---------|--------|------|
| 简体中文 | chi_sim | OCR语言 |
| English | eng | OCR语言 |
| 繁体中文 | chi_tra | OCR语言 |
| 默认 | normal | 预处理模式 |
| 复杂色彩 | game | 预处理模式 |

**实现伪代码：**

```
function TextExtractNode._check_condition(context):
    screenshot = _get_region_image(context)
    if screenshot is None:
        return False
    
    all_text = OCRManager.get_all_text(screenshot, language, preprocess_mode)
    if not all_text:
        log_condition_result(False, "未识别到文本")
        return False
    
    if extract_mode == "all":
        extracted_text = all_text
    else:
        extracted_text = _extract_keywords_text(all_text, keywords)
    
    blackboard.set(output_key, extracted_text)
    
    if save_all_text:
        blackboard.set(all_text_key, all_text)
    
    if save_position and region:
        center = ((region[0]+region[2])//2, (region[1]+region[3])//2)
        blackboard.set(position_key, center)
    
    if extracted_text:
        log_condition_result(True)
        return True
    else:
        log_condition_result(False, "未提取到匹配的文本")
        return False
```

**关键词提取逻辑：**

```
function _extract_keywords_text(all_text, keywords):
    lines = all_text.split('\n')
    matched_lines = []
    for line in lines:
        line = line.strip()
        if line and keywords in line:
            matched_lines.append(line)
    return '\n'.join(matched_lines)
```

**关键实现细节：**
- 继承 `ConditionNode`，使用 `_check_condition()` 实现检测逻辑
- 使用 `_get_region_image()` 获取区域截图（继承自 ConditionNode）
- 使用 `_log_condition_result()` 输出检测日志（继承自 ConditionNode）
- `output_key` 支持自定义变量名，默认为 `last_extracted_text`，与 TextInputNode 的默认 `blackboard_key` 一致
- `save_all_text` 可选保存完整 OCR 识别结果，用于需要原始文本的场景
- `save_position` 保存检测区域中心坐标，与 OCRConditionNode 等条件节点保持一致

### 1.4 动作节点 (ActionNode)

动作节点执行具体操作，产生副作用。

**基类实现：**

```python
class ActionNode(Node):
    def __init__(self, node_id=None, config=None):
        super().__init__(node_id, config)
        self._children_running = False  # 子节点执行状态追踪标志
    
    def tick(self, context):
        // 检查子节点是否正在执行
        if self._children_running and self.children:
            return self._execute_children(context)
        
        status = self._execute_action(context)   // 子类实现
        self.status = status
        
        // 动作成功后执行子节点
        if status == SUCCESS and self.children:
            // 通知 GUI 节点状态为 SUCCESS
            context.notify_node_status(self.node_id, "success")
            self._children_running = True
            return self._execute_children(context)
        
        return status
    
    @abstractmethod
    def _execute_action(self, context) -> NodeStatus:
        pass
```

**子节点执行状态追踪：**

ActionNode 使用 `_children_running` 标志追踪子节点的执行状态：

| 属性 | 说明 |
|------|------|
| `_children_running` | 布尔标志，标记子节点是否正在执行 |

**状态追踪流程：**

```
ActionNode 执行流程:
┌─────────────────────────────────────────────────────────────────────┐
│  1. 首次 tick:                                                       │
│     - 检查 _children_running 标志                                    │
│     - 若为 False，执行动作                                            │
│     - 若动作成功且有子节点:                                           │
│       - 通知 GUI 节点状态为 SUCCESS                                   │
│       - 设置 _children_running = True                                │
│       - 开始执行子节点                                                │
│                                                                      │
│  2. 后续 tick:                                                       │
│     - 检查 _children_running 标志                                    │
│     - 若为 True，跳过动作执行，继续执行子节点                          │
│     - 子节点执行完成后，重置 _children_running = False                │
└─────────────────────────────────────────────────────────────────────┘
```

**GUI 状态通知机制：**

当 ActionNode 或 ConditionNode 成功执行后需要执行子节点时，必须先通知 GUI 节点状态为 SUCCESS，确保界面正确显示节点状态：

```python
# 在执行子节点前通知 SUCCESS 状态
if status == NodeStatus.SUCCESS and self.children:
    context.notify_node_status(self.node_id, "success")
    self._children_running = True
    return self._execute_children(context)
```

**装饰参数（所有动作节点通用）：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| repeat_count | 重复次数 | 0 |
| timeout_ms | 超时时间 | 0 |

**随机范围参数（部分动作节点支持）：**

为模拟更自然的人类操作，部分动作节点支持为时长参数设置随机范围：

| 节点 | 参数 | 随机范围参数 |
|------|------|-------------|
| DelayNode | duration_ms | duration_ms_random |
| MouseClickNode | click_interval | click_interval_random |
| MouseMoveNode | drag_duration | drag_duration_random |
| KeyPressNode | duration | duration_random |

**随机范围实现：**

```python
from bt_utils.helpers import get_random_duration

class DelayNode(ActionNode):
    def __init__(self, node_id=None, config=None):
        super().__init__(node_id, config)
        self.duration_ms = self.config.get_int("duration_ms", 1000)
        self.duration_ms_random = self.config.get_int("duration_ms_random", 0)  # 随机范围
    
    def _execute_action(self, context) -> NodeStatus:
        # 计算实际延时时长（带随机）
        actual_duration = get_random_duration(self.duration_ms, self.duration_ms_random)
        
        # 执行延时
        time.sleep(actual_duration / 1000)
        return NodeStatus.SUCCESS
```

**随机值计算函数：**

```python
def get_random_value(base_value: int, random_range: int = 0, min_value: int = None) -> int:
    """
    计算带随机范围的值
    
    Args:
        base_value: 基础值
        random_range: 随机范围（±）
        min_value: 最小值限制（可选）
    
    Returns:
        计算后的随机值
    """
    if random_range <= 0:
        return base_value
    
    min_val = base_value - random_range
    max_val = base_value + random_range
    
    if min_value is not None:
        min_val = max(min_val, min_value)
    
    return random.randint(int(min_val), int(max_val))
```

**成功日志简化原则：**

动作节点的成功日志仅输出节点类型和名称，不包含额外信息（如位置、操作参数等），保持简洁：

```python
# 成功日志示例（简化后）
LogManager.instance().log_success(
    node_type="鼠标移动节点",
    node_name="移动到按钮位置"
)

# 失败日志示例（包含详细原因）
LogManager.instance().log_failure(
    node_type="鼠标点击节点",
    node_name="点击确认按钮",
    reason="点击操作失败"
)
```

#### 各动作节点实现要点

| 节点 | 核心调用 | 关键参数 |
|------|----------|----------|
| KeyPressNode | `context.execute_key_press(key, action, duration)` | key, action(press/down/up), duration, duration_random |
| MouseClickNode | `context.execute_mouse_click(button, position, action, duration)` | button, position, click_count(-1=无限点击), click_interval, click_interval_random |
| MouseMoveNode | `context.execute_mouse_move(position, relative, smooth)` | position, relative, smooth, move_type(移动/拖拽), drag_button, end_position, drag_duration |
| MouseScrollNode | `context.execute_mouse_scroll(...)` | distance, clicks, direction |
| DelayNode | `time.sleep()` + 状态记忆 | duration_ms, duration_ms_random, _start_time |
| SetVariableNode | `blackboard.set/increment/delete()` | variable_name, operation, value |
| ScriptNode | `ScriptExecutor.run_script()` | script_path, loop |
| CodeNode | `subprocess.run/Popen()` | code_path, args, wait_complete |
| AlarmNode | `AlarmPlayer.play()` | sound_path, volume, wait_complete |

**MouseClickNode 无限点击功能：**

当 `click_count = -1` 时，MouseClickNode 进入无限点击模式，持续点击直到行为树停止：

```python
class MouseClickNode(ActionNode):
    def _execute_action(self, context) -> NodeStatus:
        # click_count = -1 表示无限点击
        if self.click_count == -1:
            while not self._abort_flag:
                context.input_controller.click(...)
                if self.click_interval > 0:
                    actual_interval = get_random_interval(self.click_interval, self.click_interval_random)
                    time.sleep(actual_interval)
            return NodeStatus.SUCCESS
        
        # 正常点击模式
        for _ in range(self.click_count):
            if self._abort_flag:
                return NodeStatus.SUCCESS
            context.input_controller.click(...)
            if self.click_interval > 0:
                actual_interval = get_random_interval(self.click_interval, self.click_interval_random)
                time.sleep(actual_interval)
        
        return NodeStatus.SUCCESS
```

**MouseMoveNode 拖拽功能：**

MouseMoveNode 支持两种移动类型：普通移动和拖拽操作：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| position | 起点位置（相对移动时为偏移值） | — |
| use_blackboard | 是否使用黑板变量作为起点位置 | False |
| position_key | 起点位置的黑板变量名 | "last_detection_position" |
| relative | 相对移动模式（开启时隐藏拖拽参数） | False |
| smooth | 平滑移动 | True |
| move_type | 移动类型（移动/拖拽） | "移动" |
| drag_button | 拖拽按键（相对移动时隐藏） | "left" |
| end_position | 终点（相对移动时隐藏） | None |
| use_blackboard_end | 终点使用黑板位置（相对移动时隐藏） | False |
| position_key_end | 终点位置变量名（相对移动时隐藏） | "" |
| drag_duration | 拖拽时长（毫秒，相对移动时隐藏） | 0 |
| drag_duration_random | 拖拽时长随机范围（±毫秒，相对移动时隐藏） | 0 |

**相对移动说明：**

| relative | 行为 |
|----------|------|
| False（默认） | 移动到屏幕绝对坐标位置 |
| True | 从当前位置偏移指定距离 |

**GUI字段条件显示：**

当开启"相对移动"时，以下字段自动隐藏：
- 拖拽按键
- 终点
- 终点使用黑板位置
- 终点位置变量名
- 拖拽时长
- 拖拽时长随机范围

```python
class MouseMoveNode(ActionNode):
    def _execute_action(self, context) -> NodeStatus:
        # 获取起始位置
        start_pos = self._get_position(context)
        
        if self.move_type == "拖拽" and not self.relative:
            # 获取结束位置
            if self.use_blackboard_end:
                end_pos = context.blackboard.get(self.position_key_end)
            else:
                end_pos = self.end_position
            
            # 执行拖拽
            actual_duration = get_random_duration(self.drag_duration, self.drag_duration_random)
            context.input_controller.drag(start_pos, end_pos, self.drag_button, actual_duration)
        else:
            # 普通移动（支持相对移动）
            context.input_controller.move(start_pos, self.relative, self.smooth)
        
        return NodeStatus.SUCCESS
```

**AlarmNode 实现细节：**

AlarmNode 是一个特殊的动作节点，用于播放报警音效：

```python
class AlarmNode(ActionNode):
    NODE_TYPE = "AlarmNode"

    def __init__(self, node_id: str = None, config: NodeConfig = None):
        super().__init__(node_id, config)
        
        from bt_utils.resource_manager import ResourceManager
        from config.settings_manager import SettingsManager
        
        # 从设置中获取默认值
        default_sound = ResourceManager().get_alarm_sound_path()
        default_volume = SettingsManager().get("alarm_volume", 70)
        
        self.sound_path = self.config.get("sound_path", default_sound)
        self.volume = self.config.get_int("volume", default_volume)
        self.wait_complete = self.config.get_bool("wait_complete", True)
        self._abort_flag = False

    def _execute_action(self, context) -> NodeStatus:
        from bt_utils.alarm import AlarmPlayer
        player = AlarmPlayer()

        # 解析相对路径为绝对路径
        resolved_sound_path = context.resolve_path(self.sound_path)
        player.play(resolved_sound_path, self.volume, self.wait_complete)
        
        return NodeStatus.SUCCESS

    def abort(self, context) -> None:
        """中止时立即停止播放"""
        self._abort_flag = True
        from bt_utils.alarm import AlarmPlayer
        AlarmPlayer().stop()
        super().abort(context)
```

**参数设计说明：**

| 参数 | 说明 | 来源 |
|------|------|------|
| sound_path | 音效文件路径 | 节点配置（支持相对路径 `./audio/alarms/...`） |
| volume | 音量 (0-100) | 节点配置，默认从设置读取 |
| wait_complete | 是否等待播放完成 | 节点配置 |
| repeat_count | 重复次数 | 装饰器参数（不在节点配置中） |
| repeat_interval_ms | 重复间隔 | 装饰器参数（不在节点配置中） |

**重要：** `repeat_count` 和 `repeat_interval_ms` 是装饰器参数，通过 `ACTION_DECORATOR_FIELDS` 定义，由 `_execute_with_decorators()` 处理重复逻辑。节点配置中不应包含这些参数，避免 GUI 显示重复字段。

**相对路径解析：**

AlarmNode 的 `sound_path` 支持相对路径格式（如 `./audio/alarms/alarm.mp3`），在执行时通过 `context.resolve_path()` 解析为绝对路径：

```python
# 相对路径
sound_path = "./audio/alarms/custom_alarm.mp3"

# 执行时解析
resolved_path = context.resolve_path(sound_path)
# → "D:/Projects/MyProject/audio/alarms/custom_alarm.mp3"
```

这确保了项目可以在不同环境中正确加载音频资源。

**DelayNode 特殊实现：** DelayNode 是唯一可能返回 RUNNING 状态的动作节点。它使用 `_start_time` 记录开始时间，在 `duration_ms` 内持续返回 RUNNING，超时后返回 SUCCESS 并重置 `_start_time`。

**MouseMoveNode 平滑移动实现：** MouseMoveNode 支持 `smooth` 参数，启用时使用 smoothstep 缓动函数实现平滑移动效果。平滑移动通过 InputController 的 `mouse_move` 方法实现，将移动过程分解为多个小步，每步使用 smoothstep 插值计算位置，使鼠标移动更加自然流畅。

**MouseScrollNode 滚动实现：** MouseScrollNode 支持 `distance`（滚动距离）、`clicks`（点击次数）、`direction`（滚动方向）三个参数，与旧项目保持兼容。

**ScriptNode 执行器池管理：**

ScriptNode 使用类级别的执行器池管理脚本执行状态，确保节点重置时正确清理：

```python
class ScriptNode(ActionNode):
    _executor_pool: Dict[str, ScriptExecutor] = {}  # 类级别执行器池
    _pool_lock = threading.Lock()  # 线程安全锁
    
    def reset(self, reset_counters: bool = True) -> None:
        with self._lock:
            # 停止当前执行器
            if self._executor and self._executor.is_running:
                self._executor.stop_script()
            
            # 从执行器池中移除
            with ScriptNode._pool_lock:
                if self.node_id in ScriptNode._executor_pool:
                    del ScriptNode._executor_pool[self.node_id]
            
            self._executor = None
            self._script_started = False
            self._aborted = False
        super().reset(reset_counters=reset_counters)
```

**ScriptNode 循环执行逻辑：**

ScriptNode 的 `loop` 参数与 `repeat_count` 参数需要正确配合：

```python
# loop=true 且 repeat_count=0 时使用循环执行
use_loop = self.loop and self.config.repeat_count == 0
self._executor.run_script(self._script_content, loop=use_loop)
```

当 `repeat_count != 0` 时，由装饰器 `_execute_with_decorators()` 处理重复逻辑，不应同时启用 `loop`。

**ScriptExecutor delay 命令非阻塞实现：**

脚本中的 `delay` 命令需要支持中断，不能使用阻塞式 `time.sleep()`：

```python
elif command["type"] == "delay":
    delay_time = command["time"] / 1000
    elapsed = 0
    # 分段检查 is_running，支持中断
    while elapsed < delay_time and self.is_running:
        sleep_time = min(0.1, delay_time - elapsed)
        time.sleep(sleep_time)
        elapsed += sleep_time
```

这确保了用户点击停止时，脚本能够及时响应并停止执行。

#### 1.4.10 文本输入节点 (TextInputNode)

**语义：** 从黑板变量、预设文本或文件获取文本内容，逐字符输入到当前焦点窗口。

**支持参数：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| input_mode | 输入模式（extracted/preset/file） | extracted |
| preset_texts | 预设文本列表 | [] |
| execution_mode | 执行模式（sequential/random） | sequential |
| blackboard_key | 黑板变量名（extracted模式） | last_extracted_text |
| file_path | 文件路径（file模式） | "" |
| input_delay | 输入间隔（毫秒） | 50 |
| clear_before_input | 输入前清空 | False |
| save_input_text | 保存输入文本到黑板 | False |
| output_key | 输出变量名 | last_input_text |

**中文显示映射：**

| 中文显示 | 内部值 | 说明 |
|---------|--------|------|
| 文本提取值 | extracted | 输入模式 |
| 预设文本 | preset | 输入模式 |
| 文件 | file | 输入模式 |
| 顺序 | sequential | 执行模式 |
| 随机 | random | 执行模式 |

**实现伪代码：**

```
function TextInputNode._execute_action(context):
    text = _get_text(context)
    if not text:
        log_failure("未获取到文本内容")
        return FAILURE
    
    if clear_before_input:
        // Ctrl+A 全选后删除
        context.execute_key_press("ctrl", "down", 0)
        context.execute_key_press("a", "press", 0)
        context.execute_key_press("ctrl", "up", 0)
        sleep(0.1)
    
    _input_text(context, text)
    
    if save_input_text:
        blackboard.set(output_key, text)
    
    log_success()
    return SUCCESS
```

**文本获取逻辑：**

```
function _get_text(context):
    if input_mode == "preset":
        if preset_texts is empty:
            return ""
        if execution_mode == "sequential":
            return _get_next_preset_text(context)
        else:
            return _get_random_preset_text()
    
    elif input_mode == "extracted":
        return blackboard.get(blackboard_key, "")
    
    elif input_mode == "file":
        return _read_file(context)
    
    return ""
```

**逐字符输入机制：**

```
function _input_text(context, text):
    for char in text:
        if not context.check_running():
            break
        
        if char == '\n':
            context.execute_key_press("enter", "press", 0)
        elif char == '\t':
            context.execute_key_press("tab", "press", 0)
        else:
            pyperclip.copy(char)           // 复制字符到剪贴板
            context.execute_key_press("ctrl", "down", 0)
            context.execute_key_press("v", "press", 0)   // Ctrl+V 粘贴
            context.execute_key_press("ctrl", "up", 0)
        
        if input_delay > 0:
            sleep(input_delay / 1000.0)
```

**预设文本顺序执行逻辑：**

```
function _get_next_preset_text(context):
    index_key = node_id + "_text_index"
    current_index = blackboard.get(index_key, 0)
    
    text = preset_texts[current_index % len(preset_texts)]
    
    next_index = (current_index + 1) % len(preset_texts)
    blackboard.set(index_key, next_index)
    
    return text
```

**关键实现细节：**
- 继承 `ActionNode`，使用 `_execute_action()` 实现动作逻辑
- 使用 pyperclip + Ctrl+V 逐字符输入，兼容各种输入框（包括游戏内输入框）
- 换行符 `\n` 转换为 Enter 键，制表符 `\t` 转换为 Tab 键
- `input_delay` 控制字符输入间隔，避免输入过快导致丢失
- `clear_before_input` 使用 Ctrl+A 全选后输入新内容，实现清空效果
- 预设文本顺序执行使用黑板变量记录当前索引，支持循环使用
- `save_input_text` 将输入的文本保存到黑板，供后续节点使用
- `file_path` 已注册到 ResourceService，文件会自动导入到项目文件夹
- 序列化时使用中文显示名（`to_dict`），反序列化时通过映射表转换回内部值

---

## 2 节点间通信机制

### 2.1 日志管理器 (LogManager)

LogManager 是节点日志输出的核心组件，采用单例模式，提供统一的日志接口：

```python
class LogManager:
    _instance = None
    
    @classmethod
    def instance(cls) -> "LogManager":
        if cls._instance is None:
            cls._instance = cls()
        return cls._instance
    
    def log_success(self, node_type: str, node_name: str) -> None:
        """记录节点执行成功"""
        self._add_log("SUCCESS", node_type, node_name)
    
    def log_failure(self, node_type: str, node_name: str, reason: str = "") -> None:
        """记录节点执行失败"""
        message = f"{node_name}: 失败"
        if reason:
            message += f" - {reason}"
        self._add_log("FAILURE", node_type, message)
    
    def log_aborted(self, node_type: str, node_name: str) -> None:
        """记录节点被中止"""
        self._add_log("ABORTED", node_type, f"{node_name}: 已中止")
```

**日志级别说明：**

| 级别 | 颜色 | 用途 |
|------|------|------|
| SUCCESS | 绿色 | 节点执行成功 |
| FAILURE | 红色 | 节点执行失败，需提供原因 |
| ABORTED | 橙色 | 节点被用户中止 |
| INFO | 蓝色 | 一般信息 |

### 2.2 黑板通信模式

节点间通过黑板系统进行解耦通信，遵循"写入-读取"模式：

```
┌──────────────────┐     set()      ┌──────────────┐     get()      ┌──────────────────┐
│  OCRConditionNode │ ─────────────► │              │ ◄───────────── │  MouseClickNode   │
│  (写入检测位置)    │                │   Blackboard │                │  (读取检测位置)    │
└──────────────────┘                │              │                └──────────────────┘
                                    │  ┌────────┐  │
┌──────────────────┐     set()      │  │变量字典 │  │     get()      ┌──────────────────┐
│  NumberCondition  │ ─────────────► │  │        │  │ ◄───────────── │  VariableCondition│
│  Node (写入数字)  │                │  │last_   │  │                │  Node (读取数字)  │
└──────────────────┘                │  │detection│ │                └──────────────────┘
                                    │  │_position││
                                    │  │last_   ││
                                    │  │number_ ││
                                    │  │value   ││
                                    │  │exec_   ││
                                    │  │count   ││
                                    │  └────────┘  │
                                    └──────────────┘
```

### 2.3 典型通信场景

**场景1：OCR检测 → 点击检测位置**

```
OCRConditionNode                    MouseClickNode
     │                                    │
     │ 1. _check_condition()              │
     │    screenshot = get_screenshot()   │
     │    found, pos = perform_ocr()      │
     │                                    │
     │ 2. blackboard.set(                 │
     │    "last_detection_position",      │
     │    pos)                            │
     │ ──────────────────────────────────►│
     │                                    │ 3. _execute_action()
     │                                    │    position = blackboard.get(
     │                                    │      "last_detection_position")
     │                                    │    execute_mouse_click(
     │                                    │      position=position)
```

**场景2：数字识别 → 条件判断**

```
NumberConditionNode                 VariableConditionNode
     │                                    │
     │ 1. _check_condition()              │
     │    value = ocr.recognize_number()  │
     │    blackboard.set(                 │
     │      "last_number_value", value)   │
     │ ──────────────────────────────────►│
     │                                    │ 2. _check_condition()
     │                                    │    value = blackboard.get(
     │                                    │      "last_number_value")
     │                                    │    compare(value, target)
```

### 2.4 观察者通知机制

黑板系统支持订阅变量变化，实现被动通知：

```python
# 订阅位置变化
def on_position_change(old_value, new_value):
    print(f"检测位置变化: {old_value} → {new_value}")

blackboard.subscribe("last_detection_position", on_position_change)

# 当 OCRConditionNode 设置位置时，自动触发回调
blackboard.set("last_detection_position", (100, 200))
# → on_position_change(None, (100, 200)) 被调用
```

### 2.5 执行上下文作为中介

ExecutionContext 是节点访问基础设施的唯一入口，实现了延迟初始化和统一接口：

```
Node.tick(context)
  │
  ├── context.get_screenshot(region)
  │     └── ScreenshotManager (延迟初始化)
  │           └── PIL.ImageGrab.grab()
  │
  ├── context.execute_key_press(key, action, duration)
  │     └── InputController (延迟初始化)
  │           └── PyAutoGUI / DD64.dll
  │
  ├── context.perform_ocr(image, keywords, language)
  │     └── OCRManager (延迟初始化)
  │           └── RapidOCR → ONNX Runtime
  │
  └── context.blackboard
        └── Blackboard (直接访问)
```

---

## 3 行为树序列化与反序列化

### 3.1 序列化方案

序列化采用"扁平化节点 + 连线列表"的结构，而非嵌套树结构，便于编辑器操作：

**序列化流程：**

```
Root Node (树结构)
    │
    │  collect_nodes() 递归遍历
    ▼
┌─────────────────────────────────────────┐
│ nodes: {                                │
│   "n1": { id, type, config, children }, │  ← 扁平化节点字典
│   "n2": { id, type, config, children }, │
│   "n3": { id, type, config, children }  │
│ }                                       │
│ connections: [                          │
│   { parent_id: "n1", child_id: "n2" }, │  ← 连线列表
│   { parent_id: "n1", child_id: "n3" }  │
│ ]                                       │
│ root_node: "n1"                         │  ← 根节点标识
└─────────────────────────────────────────┘
```

### 3.2 反序列化方案

**反序列化流程：**

```
JSON 数据
    │
    │ 1. 解析 nodes 字典
    ▼
┌──────────────────────────────────────┐
│ for each node_data in nodes:         │
│   node_class = NodeRegistry.get(type)│  ← 查找注册的节点类
│   node = node_class.from_dict(data)  │  ← 创建节点实例
│   nodes[node_id] = node              │
└──────────────────────────────────────┘
    │
    │ 2. 重建连接关系
    ▼
┌──────────────────────────────────────┐
│ for each conn in connections:        │
│   parent = nodes[conn.parent_id]     │
│   child = nodes[conn.child_id]       │
│   parent.add_child(child)            │  ← 重建父子关系
└──────────────────────────────────────┘
    │
    │ 3. 返回元组
    ▼
return (nodes[root_id], canvas_state, editor_state)
```

**返回值说明：**

`Serializer.deserialize()` 返回三元组：

| 元素 | 类型 | 说明 |
|------|------|------|
| root_node | Node | 反序列化的根节点 |
| canvas_state | Dict | 画布状态（节点位置、视口偏移、缩放比例） |
| editor_state | Dict | 编辑器状态（选中节点、文件路径） |

**引擎与编辑器的不同处理：**

```python
# 引擎加载：仅需根节点
result = Serializer.deserialize(data)
if isinstance(result, tuple):
    root_node = result[0]
else:
    root_node = result  # 兼容旧格式

# 编辑器加载：使用完整元组
root_node, canvas_state, editor_state = Serializer.deserialize(data)
self.set_tree_data(root_node, canvas_state, editor_state)
```

### 3.3 编辑器数据格式

编辑器使用扩展格式，包含画布状态信息：

```json
{
  "version": "2.0",
  "format_type": "behavior_tree_editor",
  "canvas": {
    "name": "未命名",
    "description": "",
    "viewport": {
      "zoom": 1.0,
      "offset_x": 0,
      "offset_y": 0
    }
  },
  "root_node": "node_1",
  "nodes": {
    "node_1": {
      "id": "node_1",
      "type": "SequenceNode",
      "name": "顺序",
      "enabled": true,
      "config": {},
      "position": { "x": 400, "y": 100 }
    },
    "node_2": {
      "id": "node_2",
      "type": "OCRConditionNode",
      "name": "OCR检测",
      "enabled": true,
      "config": {
        "region": [0, 0, 800, 600],
        "keywords": "目标文字",
        "language": "chi_sim",
        "retry_count": 3
      },
      "position": { "x": 400, "y": 200 }
    }
  },
  "connections": [
    { "parent_id": "node_1", "child_id": "node_2" }
  ]
}
```

**与引擎格式的差异：**

| 特性 | 引擎格式 | 编辑器格式 |
|------|----------|------------|
| 节点位置 | 无 | `position: {x, y}` |
| 画布状态 | 无 | `canvas.viewport` |
| children 表示 | ID 列表 | 通过 connections 推导 |
| config 结构 | 扁平化 key-value（节点特有参数直接存储） | 扁平化 key-value |

**NodeConfig 扁平化序列化：**

`NodeConfig.to_dict()` 方法采用扁平化输出，将所有参数直接输出到 config 顶层：

```python
def to_dict(self) -> Dict[str, Any]:
    """序列化为字典（扁平化格式，与原项目兼容）"""
    result = {}
    if self.name:
        result["name"] = self.name
    if self.description:
        result["description"] = self.description
    result["enabled"] = self.enabled
    if self.retry_count != 0:
        result["retry_count"] = self.retry_count
    if self.repeat_count != 0:
        result["repeat_count"] = self.repeat_count
    if self.repeat_interval_ms != 100:
        result["repeat_interval_ms"] = self.repeat_interval_ms
    if self.timeout_ms != 0:
        result["timeout_ms"] = self.timeout_ms
    # 将 extra 中的参数合并到顶层
    if self.extra:
        for key, value in self.extra.items():
            result[key] = value
    return result
```

**节点序列化示例：**

```python
# OCRConditionNode.to_dict() 示例
def to_dict(self) -> Dict[str, Any]:
    data = super().to_dict()
    # 直接写入 config 顶层，不使用 extra 嵌套
    data["config"]["region"] = self.region
    data["config"]["keywords"] = self.keywords
    data["config"]["language"] = self.language
    data["config"]["position_key"] = self.position_key
    return data
```

### 3.4 版本兼容

当前版本号 `CURRENT_VERSION = "2.0"`，序列化数据包含 `version` 字段用于兼容性检查。未来版本升级时，可在反序列化中添加版本迁移逻辑。

---

## 4 节点注册机制

### 4.1 注册流程

节点类型通过 `NodeRegistry` 进行集中注册，在模块导入时自动完成：

```python
# bt_nodes/actions/__init__.py
from .keyboard import KeyPressNode
from .mouse import MouseClickNode, MouseMoveNode
# ...

NodeRegistry.register("KeyPressNode", KeyPressNode)
NodeRegistry.register("MouseClickNode", MouseClickNode)
# ...
```

```python
# bt_nodes/conditions/__init__.py
from .ocr import OCRConditionNode
# ...

NodeRegistry.register("OCRConditionNode", OCRConditionNode)
# ...
```

### 4.2 核心节点注册

```python
# bt_core/registry.py
def register_core_nodes():
    NodeRegistry.register("SequenceNode", SequenceNode)
    NodeRegistry.register("SelectorNode", SelectorNode)
    NodeRegistry.register("ParallelNode", ParallelNode)
```

### 4.3 自定义节点扩展

要添加新的节点类型，需要：

1. 创建节点类，继承 `ActionNode` 或 `ConditionNode`
2. 实现 `tick()` / `_execute_action()` / `_check_condition()` 方法
3. 实现 `to_dict()` 和 `from_dict()` 方法
4. 在 `__init__.py` 中注册到 `NodeRegistry`
5. 在 `constants.py` 中添加显示名称和分类
6. 在 `property.py` 中添加配置 Schema

---

## 5 输入系统实现

### 5.1 双模式输入架构

```
ExecutionContext
    │
    ├── execute_key_press(key, action, duration)
    │     │
    │     └── InputController (标准版)
    │           ├── key_press() → pyautogui.press/keyDown/keyUp
    │           └── 支持 duration 参数（按住时长）
    │
    └── [DD版] AUTODOOR_USE_DD=1
          └── DDVirtualInput
                ├── key_down() → DD_key(dd_code, 1)
                ├── key_up() → DD_key(dd_code, 2)
                └── VK → DD码转换: DD_todc(vk_code)
```

### 5.2 DD虚拟键盘实现细节

**DLL加载流程：**

```
1. 搜索路径优先级:
   a. 构造参数指定的 dll_path
   b. bt_utils/DD64.dll
   c. bt_utils/drivers/DD64.dll
   d. ../drivers/DD64.dll
   e. 项目根/drivers/DD64.dll

2. 加载验证:
   dd_dll = ctypes.windll.LoadLibrary(path)
   result = dd_dll.DD_btn(0)    // 测试调用
   if result == 1: available = True

3. 按键转换链:
   key_name → VK_CODE_MAP → vk_code → DD_todc(vk_code) → dd_code
   示例: "enter" → 0x0D → DD_todc(0x0D) → 28

4. 执行:
   DD_key(dd_code, 1)  // 按下
   DD_key(dd_code, 2)  // 抬起
```

**鼠标DD码映射：**

| 操作 | DD码 |
|------|------|
| 左键按下 | 1 |
| 左键抬起 | 2 |
| 右键按下 | 4 |
| 右键抬起 | 8 |
| 中键按下 | 16 |
| 中键抬起 | 32 |

### 5.3 PyInstaller 运行时钩子

DD版通过 `hooks/hook_dd_input.py` 在启动时设置环境变量：

```python
import os
os.environ['AUTODOOR_USE_DD'] = '1'
```

在 `autodoor_bt_dd.spec` 中配置：

```python
runtime_hooks=['hooks/hook_dd_input.py']
```

---

## 6 画布编辑器实现

### 6.1 坐标系统

画布使用三层坐标变换：

```
世界坐标 (World)          ← 节点的逻辑位置
    │
    │ zoom + pan
    ▼
屏幕坐标 (Screen)         ← Canvas 上的像素位置
```

**变换公式：**

```
screen_x = world_x * zoom + pan_x
screen_y = world_y * zoom + pan_y

world_x = (screen_x - pan_x) / zoom
world_y = (screen_y - pan_y) / zoom
```

### 6.2 节点渲染

NodeItem 在 Tkinter Canvas 上绘制，包含以下视觉元素：

```
┌─────────────────────────┐
│ ○  │  节点名称    [✓]   │  ← 输入端口(上) + 颜色条 + 名称 + 状态指示器
└─────────────────────────┘
              ○               ← 输出端口(下)
```

| 元素 | Canvas Tag | 说明 |
|------|------------|------|
| shadow | node_shadow | 阴影效果 |
| rect | node | 主体矩形 |
| color_bar | node_color | 左侧分类色条 |
| text | node_text | 节点名称（自动截断） |
| status_bg | node_status_bg | 状态指示背景 |
| status_icon | node_icon | 状态图标 (✓/✗/⋯) |
| input_port | node_port_in | 输入端口圆 |
| output_port | node_port_out | 输出端口圆 |

### 6.3 连线渲染

连线使用贝塞尔曲线风格的三段折线：

```
parent output_port (start_x, start_y)
        │
        │ 垂直段
        ▼
   (start_x, mid_y) ────── (end_x, mid_y)    ← 水平段
                                │
                                │ 垂直段
                                ▼
              child input_port (end_x, end_y)
```

### 6.4 连接验证机制

为防止子节点被多个父节点连接导致的数据不一致问题，画布在创建连接时进行严格的验证。

#### 6.4.1 问题背景

行为树的节点模型设计如下：

```python
class Node:
    def __init__(self):
        self.children: List[Node] = []      # 可以有多个子节点
        self.parent: Optional[Node] = None   # 只能有一个父节点
```

**问题**：当一个子节点被多个父节点连接时：

1. `add_child()` 方法会直接覆盖子节点的 `parent` 引用
2. 但之前的父节点的 `children` 列表中仍然保留该子节点
3. 导致数据不一致：子节点的 `parent` 指向新父节点，但旧父节点仍认为它是自己的子节点

#### 6.4.2 验证实现

**add_connection 方法实现：**

```python
def add_connection(self, parent_id: str, child_id: str) -> bool:
    """添加连接（带完整验证）"""
    from bt_utils.log_manager import LogManager
    
    # 1. 检查节点是否存在
    if parent_id not in self.nodes or child_id not in self.nodes:
        return False
    
    # 2. 检查是否已经存在相同的连接
    if any(c[0] == parent_id and c[1] == child_id for c in self.connections):
        return False
    
    # 3. 检查子节点是否已经有父节点
    existing_parents = [c[0] for c in self.connections if c[1] == child_id]
    if existing_parents:
        child_node = self.nodes.get(child_id)
        child_name = child_node.name if child_node else child_id
        LogManager.instance().log_info(
            "连接操作",
            child_name,
            "节点已有父节点，不能重复连接"
        )
        return False
    
    # 4. 检查是否会形成循环
    if self._would_create_cycle(parent_id, child_id):
        LogManager.instance().log_info(
            "连接操作",
            "循环检测",
            "不能形成循环连接"
        )
        return False
    
    # 5. 添加连接
    self.connections.append((parent_id, child_id))
    self._redraw_connections()
    return True
```

**循环检测算法：**

使用深度优先搜索（DFS）检测循环连接：

```python
def _would_create_cycle(self, parent_id: str, child_id: str) -> bool:
    """检查添加连接是否会形成循环"""
    visited = set()
    stack = [child_id]
    
    while stack:
        current = stack.pop()
        if current == parent_id:
            return True  # 找到循环
        if current in visited:
            continue
        visited.add(current)
        
        # 添加当前节点的所有子节点
        for p, c in self.connections:
            if p == current:
                stack.append(c)
    
    return False
```

#### 6.4.3 错误提示策略

连接失败时通过日志管理器输出提示信息，不使用弹窗干扰用户操作：

| 错误类型 | 日志输出示例 |
|---------|-------------|
| 多父节点 | `[时间] ℹ️ 连接操作 "动作节点" - 节点已有父节点，不能重复连接` |
| 循环连接 | `[时间] ℹ️ 连接操作 "循环检测" - 不能形成循环连接` |

**设计优势：**

1. **非阻塞式提示**：不使用弹窗，避免打断用户操作流程
2. **信息清晰**：明确指出错误原因和涉及的节点
3. **统一入口**：所有日志集中显示在日志面板，便于查看历史记录

### 6.5 自动布局

当加载的树数据不包含位置信息时，使用递归自动布局：

```
function auto_layout(root_id):
    positions = {}
    
    function calc_subtree_width(node_id):
        children = get_children(node_id)
        if no children: return NODE_WIDTH (180)
        return max(sum(calc_subtree_width(c) for c in children), NODE_WIDTH)
    
    function layout_node(node_id, x, y):
        positions[node_id] = (x, y)
        children = get_children(node_id)
        if children:
            total_width = sum(calc_subtree_width(c) for c in children)
            current_x = x - total_width / 2
            for child in children:
                child_width = calc_subtree_width(child)
                layout_node(child, current_x + child_width/2, y + NODE_HEIGHT + V_GAP)
                current_x += child_width
    
    layout_node(root_id, 400, 100)
    return positions
```

---

## 7 主题系统

### 7.1 主题配置

Theme 类定义了完整的视觉规范：

| 类别 | 配置项 |
|------|--------|
| 颜色 | COLORS (亮色) + DARK_COLORS (暗色) |
| 字体 | FONTS.family + FONTS.sizes |
| 尺寸 | DIMENSIONS (间距/宽度/高度/圆角) |
| 节点色 | NODE_COLORS (composite/condition/action/decorator) |

### 7.2 节点分类着色

| 分类 | 背景色 | 用途 |
|------|--------|------|
| composite | #1E40AF (蓝) | 组合节点 |
| condition | #BE185D (粉) | 条件节点 |
| action | #047857 (绿) | 动作节点 |
| decorator | #B45309 (橙) | 装饰器 |

---

## 8 OCR 管理器实现

### 8.1 RapidOCR 引擎初始化

OCRManager 封装 RapidOCR + ONNX Runtime，无需安装 Tesseract：

```python
from rapidocr import RapidOCR

@singleton
class OCRManager:
    _engine: Optional[RapidOCR] = None
    _available: bool = True
    
    def __init__(self):
        try:
            # v1.3.0: 禁用内部放大机制，由预处理层统一控制
            self._engine = RapidOCR(
                params={
                    "Det.limit_side_len": 736,
                    "Det.limit_type": "max"  # 只缩小大图，不放大小图
                }
            )
            self._auto_config_selector = AutoConfigSelector()
        except Exception as e:
            self._engine = None
            OCRManager._available = False
    
    @classmethod
    def is_available(cls) -> bool:
        return cls._available
```

### 8.2 图像预处理模式

| 模式 | 处理流程 | 适用场景 |
|------|---------|---------|
| normal (默认) | 灰度→对比度增强→锐化→二值化 | 标准文字识别 |
| game (复杂色彩) | 放大→中值滤波→灰度→对比度增强→锐化→二值化 | 游戏界面、小字体 |
| adaptive (自适应) | CLAHE对比度增强→自适应二值化 | 低对比度、渐变背景 |
| auto (自动调优) | 根据图像特征自动选择最佳预处理参数 | 所有场景，推荐使用 |

### 8.3 RapidOCR 优势

| 特性 | 说明 |
|------|------|
| 无需安装 | 基于 ONNX Runtime，无需安装 Tesseract |
| 中英文支持 | 内置中英文模型，支持混合识别 |
| 部署简单 | 打包后体积更小，依赖更少 |
| 自动过滤 | 自动处理低置信度结果 |

### 8.4 结果缓存机制

为提升性能，OCRManager 内置线程安全的结果缓存：

```python
class OCRManager:
    DEFAULT_CACHE_TTL = 1.0  # 缓存过期时间（秒）
    MAX_CACHE_SIZE = 50      # 最大缓存条目数
    
    def __init__(self):
        ...
        self._cache: Dict[str, Tuple[Any, float]] = {}
        self._cache_lock = threading.Lock()
    
    def _compute_cache_key(self, image, **kwargs) -> str:
        """计算缓存键（基于图像哈希和参数哈希）"""
        img_bytes = np.array(image).tobytes()
        img_hash = hashlib.md5(img_bytes).hexdigest()[:16]
        params_hash = hashlib.md5(str(sorted(kwargs.items())).encode()).hexdigest()[:8]
        return f"{img_hash}_{params_hash}"
    
    def clear_cache(self):
        """清空缓存"""
        with self._cache_lock:
            self._cache.clear()
```

**缓存策略：**

| 策略 | 说明 |
|------|------|
| TTL 过期 | 缓存超过 1 秒自动失效 |
| LRU 淘汰 | 缓存条目超过 50 条时淘汰最旧的 |
| 线程安全 | 使用 `threading.Lock` 保护缓存操作 |

### 8.5 图像特征分析器 (v1.3.0 新增)

ImageFeatureAnalyzer 自动分析图像特征，为预处理决策提供依据：

```python
@dataclass
class ImageFeatures:
    """图像特征分析结果"""
    width: int
    height: int
    min_dimension: int
    mean_brightness: float      # 平均亮度 (0-255)
    brightness_std: float       # 亮度标准差
    contrast: float             # 对比度 (0-1)
    is_low_contrast: bool       # 是否低对比度
    noise_level: float          # 噪点水平 (0-1)
    has_noise: bool             # 是否有噪点
    estimated_char_height: int  # 估计字符高度
    is_small_font: bool         # 是否小字体
    is_dark_background: bool    # 是否深色背景
    has_gradient: bool          # 是否有渐变背景

class ImageFeatureAnalyzer:
    """图像特征分析器"""
    
    # 可配置的阈值常量
    LOW_CONTRAST_THRESHOLD = 0.3
    NOISE_THRESHOLD = 0.5
    DARK_BACKGROUND_THRESHOLD = 128
    SMALL_FONT_HEIGHT_THRESHOLD = 15
    DEFAULT_CHAR_HEIGHT = 20
    
    def analyze(self, image: Image.Image) -> ImageFeatures:
        """分析图像特征"""
        # 转换为灰度图
        gray = cv2.cvtColor(np.array(image), cv2.COLOR_RGB2GRAY)
        
        # 计算亮度特征
        mean_brightness = np.mean(gray)
        
        # 计算对比度（Michelson对比度）
        contrast = (np.max(gray) - np.min(gray)) / (np.max(gray) + np.min(gray) + 1e-6)
        is_low_contrast = contrast < self.LOW_CONTRAST_THRESHOLD
        
        # 估计字符高度（使用轮廓检测）
        _, binary = cv2.threshold(gray, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
        contours, _ = cv2.findContours(binary, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
        # ...
        
        return ImageFeatures(...)
```

### 8.6 智能放大策略 (v1.3.0 新增)

SmartScaleStrategy 计算安全的放大倍数，避免过度失真：

```python
class SmartScaleStrategy:
    """智能放大策略"""
    
    TARGET_CHAR_HEIGHT = 15     # 目标字符高度
    MAX_SCALE_FACTOR = 4.0      # 最大放大倍数
    
    def calculate_scale_factor(self, features: ImageFeatures) -> float:
        """计算安全的放大倍数"""
        if not features.is_small_font:
            return 1.0
        
        # 基于字符高度计算放大倍数
        char_based_scale = self.TARGET_CHAR_HEIGHT / features.estimated_char_height
        
        # 限制最大放大倍数
        return min(char_based_scale, self.MAX_SCALE_FACTOR)
    
    def scale_image(self, image: Image.Image, scale_factor: float) -> Image.Image:
        """使用Lanczos高质量插值放大图像"""
        if scale_factor <= 1.0:
            return image
        new_size = (int(image.size[0] * scale_factor), int(image.size[1] * scale_factor))
        return image.resize(new_size, Image.LANCZOS)
```

### 8.7 自动配置选择器 (v1.3.0 新增)

AutoConfigSelector 根据图像特征自动选择最佳预处理参数：

```python
class AutoConfigSelector:
    """自动配置选择器"""
    
    def select_config(self, image: Image.Image) -> PreprocessConfig:
        """自动选择最佳预处理配置"""
        features = self._analyzer.analyze(image)
        config = PreprocessConfig()
        
        # 计算放大倍数
        config.scale_factor = self._scale_strategy.calculate_scale_factor(features)
        
        # 按优先级处理：噪点 > 低对比度 > 深色背景 > 渐变背景 > 小字体
        if features.is_small_font:
            config.sharpness_factor = 2.5
            config.sharpness_iterations = 3
            config.denoise_method = "bilateral"
        
        if features.has_gradient:
            config.contrast_method = "clahe"
            config.binarization_method = "adaptive"
        
        if features.is_dark_background:
            config.contrast_factor = 3.0
            config.binarization_method = "adaptive"
        
        if features.is_low_contrast:
            config.contrast_method = "clahe"
            config.clahe_clip_limit = 3.0
            config.binarization_method = "adaptive"
        
        if features.has_noise:
            config.denoise_method = "median"
            config.denoise_kernel_size = 5
        
        return config
```

### 8.8 精确关键词定位 (v1.3.0 新增)

基于字符实际宽度计算关键词位置，解决中英混合文本定位偏差问题：

```python
CHAR_WIDTH_FACTORS = {
    'chinese': 2.0,      # 中文字符
    'fullwidth': 2.0,    # 全角字符
    'uppercase': 1.2,    # 大写字母
    'lowercase': 1.0,    # 小写字母
    'digit': 1.0,        # 数字
    'space': 0.5,        # 空格
    'punctuation': 0.6,  # 标点符号
}

def get_char_width_factor(char: str) -> float:
    """获取字符宽度系数"""
    if '\u4e00' <= char <= '\u9fff':  # 中文字符
        return CHAR_WIDTH_FACTORS['chinese']
    if char.isupper():
        return CHAR_WIDTH_FACTORS['uppercase']
    # ...

def calculate_keyword_position(text: str, keyword: str, box: np.ndarray) -> Tuple[int, int]:
    """基于字符实际宽度精确计算关键词位置"""
    # 计算每个字符的相对宽度
    char_widths = [get_char_width_factor(char) for char in text]
    total_width = sum(char_widths)
    
    # 计算关键词位置
    keyword_idx = text.lower().find(keyword.lower())
    start_width = sum(char_widths[:keyword_idx])
    keyword_width = sum(get_char_width_factor(c) for c in keyword)
    
    # 计算中心位置
    center_ratio = (start_width + keyword_width / 2) / total_width
    x = int(box_left + box_width * center_ratio)
    
    return (x, y)
```

---

## 9 资源管理器实现

### 9.1 ResourceManager 单例模式

```python
class ResourceManager:
    _instance = None
    
    DEFAULT_ALARM_SOUND = "assets/sounds/alarm.mp3"
    DEFAULT_START_SOUND = "assets/sounds/alarm.mp3"
    DEFAULT_STOP_SOUND = "assets/sounds/temp_reversed.mp3"
    DEFAULT_ICON = "assets/icons/autodoor.ico"
    
    @classmethod
    def get_instance(cls):
        if cls._instance is None:
            cls._instance = cls()
        return cls._instance
    
    def get_resource_path(self, relative_path: str) -> str:
        # PyInstaller 打包环境
        if hasattr(sys, '_MEIPASS'):
            return os.path.join(sys._MEIPASS, relative_path)
        # 开发环境
        return os.path.join(os.path.dirname(os.path.dirname(__file__)), relative_path)
```

### 9.2 使用示例

```python
# 获取默认报警音效路径
alarm_path = ResourceManager.get_instance().get_resource_path(
    ResourceManager.DEFAULT_ALARM_SOUND
)

# 获取应用图标路径
icon_path = ResourceManager.get_instance().get_resource_path(
    ResourceManager.DEFAULT_ICON
)
```

---

## 10 快捷键实现细节

### 10.1 焦点保护机制

删除快捷键（Delete/Backspace）在输入框获得焦点时不触发节点删除：

```python
def _on_delete_key(self, event):
    """处理删除键，避免在输入框中误删节点"""
    focused = self.winfo_toplevel().focus_get()
    if focused:
        widget_type = str(type(focused).__name__)
        if widget_type in ("CTkEntry", "Entry", "CTkTextbox", "Text"):
            return None  # 允许输入框正常处理删除
    self._delete_selected()
    return "break"  # 阻止事件继续传播
```

### 10.2 快捷键绑定

**重要：快捷键必须绑定到顶级窗口**

快捷键绑定使用 `root.bind()` 而非 `self.bind()`，确保在画布获得焦点时快捷键仍能生效：

```python
def _bind_events(self):
    shortcuts = [
        ("<Control-z>", self.undo),
        ("<Control-y>", self.redo),
        ("<Control-Shift-Z>", self.redo),
        ("<Control-s>", lambda: self.save_tree()),
        ("<Control-Shift-S>", lambda: self.save_tree(save_as=True)),
        ("<Control-o>", lambda: self.load_tree()),
        ("<Control-n>", lambda: self.new_tree()),
        ("<Delete>", self._on_delete_key),
        ("<BackSpace>", self._on_delete_key),
        ("<Control-c>", self._copy_selected),
        ("<Control-v>", self._paste_selected),
        ("<Control-x>", self._cut_selected),
        ("<Control-d>", self._duplicate_selected),
    ]
    
    def make_handler(cb, key):
        def handler(e):
            if key in ("<Delete>", "<BackSpace>"):
                focused = self.winfo_toplevel().focus_get()
                if focused:
                    widget_type = str(type(focused).__name__)
                    if widget_type in ("CTkEntry", "Entry", "CTkTextbox", "Text"):
                        return None
            if callable(cb):
                cb()
            return "break"
        return handler
    
    root = self.winfo_toplevel()
    for key, callback in shortcuts:
        handler = make_handler(callback, key)
        root.bind(key, handler)
```

### 10.3 粘贴操作撤销支持

粘贴操作使用 `AddNodesCommand` 确保可撤销：

```python
def _paste_selected(self):
    if not self._clipboard_data:
        return
    
    # 创建节点副本并偏移位置
    new_nodes = []
    for node_data in self._clipboard_data:
        new_data = node_data.copy()
        new_data["position"] = {
            "x": node_data["position"]["x"] + 30,
            "y": node_data["position"]["y"] + 30
        }
        new_nodes.append(new_data)
    
    # 使用 CommandManager 支持撤销
    command = AddNodesCommand(self.canvas, new_nodes)
    self.command_manager.execute(command)
```

### 10.4 粘贴位置优化

粘贴节点时自动计算视口中心位置，并智能检测重叠避免遮挡：

**坐标转换：**

```python
def _calculate_paste_offset(self, relative_positions: Dict[str, tuple] = None) -> tuple:
    # 获取画布视口尺寸
    canvas_width = self.canvas.canvas.winfo_width() or 800
    canvas_height = self.canvas.canvas.winfo_height() or 600
    
    # 计算视口中心的屏幕坐标
    screen_center_x = canvas_width / 2
    screen_center_y = canvas_height / 2
    
    # 转换为画布坐标（考虑平移和缩放）
    canvas_center_x = (screen_center_x - self.canvas.pan_x) / self.canvas.zoom
    canvas_center_y = (screen_center_y - self.canvas.pan_y) / self.canvas.zoom
    
    # 计算粘贴组边界框
    paste_width = max(rel_x for rel_x, rel_y in relative_positions.values()) + 160
    paste_height = max(rel_y for rel_x, rel_y in relative_positions.values()) + 70
    
    # 智能偏移避免重叠
    offset_x, offset_y = canvas_center_x, canvas_center_y
    for attempt in range(50):
        has_overlap = False
        for existing_node in self.canvas.nodes.values():
            # 矩形重叠检测
            if (offset_x < existing_node.x + 160 and 
                offset_x + paste_width > existing_node.x and
                offset_y < existing_node.y + 70 and 
                offset_y + paste_height > existing_node.y):
                has_overlap = True
                break
        
        if not has_overlap:
            break
        
        offset_x += 80
        offset_y += 80
    
    return offset_x, offset_y
```

**关键实现细节：**

| 要点 | 说明 |
|------|------|
| 视口中心计算 | 使用 `(screen_center - pan) / zoom` 转换坐标 |
| 边界框计算 | 计算粘贴组的最大宽度和高度 |
| 重叠检测 | 使用标准矩形重叠算法检测整个边界框 |
| 偏移策略 | 每次偏移80像素，最多尝试50次 |

---

## 11 自动保存与崩溃恢复

### 11.1 存储路径配置

自动保存和崩溃恢复文件存储在用户数据目录中，确保打包后也能正常写入：

```python
def _get_user_data_dir() -> Path:
    """获取平台适用的用户数据目录"""
    if platform.system() == "Windows":
        base = Path(os.environ.get("APPDATA", os.path.expanduser("~")))
    else:
        base = Path.home()
    
    data_dir = base / "autodoor_behavior_tree" / "data"
    data_dir.mkdir(parents=True, exist_ok=True)
    return data_dir

class BehaviorTreeEditor(ctk.CTkFrame):
    BACKUP_DIR = str(_get_user_data_dir() / "backup")
    RECOVERY_DIR = str(_get_user_data_dir() / "recovery")
```

**存储路径位置：**

| 平台 | 路径 |
|------|------|
| Windows | `%APPDATA%/autodoor_behavior_tree/data/backup/` |
| Linux/Mac | `~/autodoor_behavior_tree/data/backup/` |

### 11.2 自动保存机制

```python
def _start_auto_save(self):
    """启动自动保存定时器"""
    self._auto_save_job = self.after(
        self._auto_save_interval * 1000,  # 默认 60 秒
        self._auto_save
    )

def _auto_save(self):
    """执行自动保存"""
    if self._has_unsaved_changes():
        auto_save_path = self._get_auto_save_path()
        self._save_to_file(auto_save_path, include_editor_state=True)
    
    # 继续下一次自动保存
    self._start_auto_save()
```

**自动保存初始化优化：**

自动保存管理器只在画布创建后初始化一次，避免重复初始化：

```python
def _create_canvas(self):
    # ... 创建画布 ...
    self._init_autosave()  # 只在这里调用一次
    self._start_autosave()
```

### 11.3 崩溃恢复机制

```python
def _check_crash_recovery(self):
    """检查崩溃恢复，包含自动清理"""
    if not hasattr(self, '_crash_recovery_handler'):
        return
    
    # 先清理超过7天的旧崩溃文件
    self._crash_recovery_handler.cleanup_old_crash_files(keep_days=7)
    
    # 然后检查是否有崩溃恢复
    if not self._crash_recovery_handler.has_crash_recovery():
        return
    
    # 静默恢复，无需用户确认
    self.load_tree(auto_save_path)
    self._crash_recovered = True
```

### 11.4 恢复流程

```
应用启动
    │
    │ _check_crash_recovery()
    ▼
┌──────────────────────────────────────┐
│ 1. 清理超过7天的旧崩溃文件             │
│ 2. 检查是否有崩溃恢复文件              │
│ 3. if exists(crash_file):            │
│      load_tree(crash_file)           │  ← 静默恢复
│      _crash_recovered = True         │
└──────────────────────────────────────┘
    │
    ▼
正常编辑流程
```

### 11.5 自动保存清理机制

手动保存成功后自动清理自动保存文件：

```python
def save_tree(self, file_path: Optional[str] = None, save_as: bool = False):
    # ... 保存逻辑 ...
    
    if self.project_root and self.project_manager and not save_as:
        self.project_manager.save_project(tree_data)
        
        # 清理自动保存文件
        if self._autosave_manager:
            self._autosave_manager.clear_autosaves()
        
        self._set_modified(False)
        return
```

### 11.6 撤销/重做后修改标记优化

撤销操作后正确更新修改标记：

```python
def undo(self):
    if self.command_manager.can_undo():
        self.command_manager.undo()
        self._update_toolbar()
        # 撤销到初始状态时清除修改标记
        if not self.command_manager.can_undo():
            self._set_modified(False)
        else:
            self._set_modified(True)

def redo(self):
    if self.command_manager.can_redo():
        self.command_manager.redo()
        self._update_toolbar()
        self._set_modified(True)
```

### 11.7 清理机制汇总

| 清理类型 | 触发时机 | 说明 |
|---------|---------|------|
| 崩溃文件清理 | 应用启动时 | 清理超过7天的崩溃恢复文件 |
| 自动保存清理 | 手动保存成功后 | 清理所有自动保存文件 |
| 备份轮转 | 每次自动保存时 | 保留最近3个自动保存备份 |
---

## 12 项目工程文件管理系统

### 12.1 PathResolver 路径解析器

PathResolver 负责相对路径与绝对路径之间的转换，确保项目的可移植性。

```python
class PathResolver:
    """资源路径解析器"""
    
    def __init__(self, project_root: str):
        self.project_root = os.path.abspath(project_root)
    
    def to_relative(self, absolute_path: str) -> str:
        """将绝对路径转换为相对路径"""
        abs_path = os.path.abspath(absolute_path)
        rel_path = os.path.relpath(abs_path, self.project_root)
        return f"./{rel_path.replace(os.sep, '/')}"
    
    def to_absolute(self, relative_path: str) -> str:
        """将相对路径转换为绝对路径"""
        if relative_path.startswith("./"):
            relative_path = relative_path[2:]
        
        abs_path = os.path.join(self.project_root, relative_path)
        return os.path.normpath(abs_path)
    
    def is_valid_relative_path(self, path: str) -> bool:
        """检查相对路径是否有效"""
        if not path.startswith("./"):
            return False
        
        abs_path = self.to_absolute(path)
        if not os.path.exists(abs_path):
            return False
        
        real_abs_path = os.path.realpath(abs_path)
        real_project_root = os.path.realpath(self.project_root)
        
        return real_abs_path.startswith(real_project_root)
```

**设计要点：**
- 使用 `os.path.relpath()` 计算相对路径
- 统一使用 `/` 作为路径分隔符（跨平台兼容）
- 路径有效性验证防止目录遍历攻击

### 12.2 ResourceImporter 资源导入器

ResourceImporter 自动导入资源文件到项目目录并生成相对路径引用。

```python
class ResourceImporter:
    """资源导入管理器"""
    
    def __init__(self, project_root: str):
        self.project_root = project_root
        self.resolver = PathResolver(project_root)
    
    def import_resource(self, source_path: str, resource_type: str = None) -> str:
        """导入资源文件到项目目录"""
        if resource_type is None:
            resource_type = self._detect_resource_type(source_path)
        
        target_dir = self._get_target_directory(resource_type)
        filename = os.path.basename(source_path)
        filename = self._handle_name_conflict(target_dir, filename)
        
        target_path = os.path.join(self.project_root, target_dir, filename)
        
        os.makedirs(os.path.dirname(target_path), exist_ok=True)
        shutil.copy2(source_path, target_path)
        
        return self.resolver.to_relative(target_path)
    
    def _detect_resource_type(self, path: str) -> str:
        """检测资源类型"""
        ext = os.path.splitext(path)[1].lower()
        
        if ext in ['.png', '.jpg', '.jpeg', '.bmp', '.gif']:
            return 'image'
        elif ext in ['.py', '.bat', '.sh', '.ps1']:
            return 'script'
        elif ext in ['.mp3', '.wav', '.ogg', '.flac']:
            return 'audio'
        elif ext in ['.json', '.yaml', '.xml', '.txt']:
            return 'data'
        else:
            return 'other'
```

**资源类型映射：**
| 节点属性 | 资源类型 | 目标目录 |
|----------|----------|----------|
| template_path | image | images/templates/ |
| script_path | script | scripts/script/ |
| code_path | code | scripts/code/ |
| sound_path | audio | audio/alarms/ |
| 其他数据文件 | data | data/config/ |

### 12.3 ProjectManager 项目管理器

ProjectManager 管理项目的完整生命周期，包括创建、加载、保存和验证。

```python
class ProjectManager:
    """项目管理器"""
    
    def __init__(self, project_root: str):
        self.project_root = project_root
        self.path_resolver = PathResolver(project_root)
        self.resource_importer = ResourceImporter(project_root)
    
    def create_project(self, name: str, description: str = "") -> None:
        """创建新项目"""
        os.makedirs(self.project_root, exist_ok=True)
        
        # 创建标准目录结构
        dirs = [
            "images/templates",
            "images/screenshots",
            "scripts/script",
            "scripts/code",
            "audio/alarms",
            "data/config",
            "cache",
            "docs"
        ]
        
        for dir_path in dirs:
            os.makedirs(os.path.join(self.project_root, dir_path), exist_ok=True)
        
        # 生成项目配置文件
        project_config = {
            "version": "1.0",
            "format_type": "behavior_tree_project",
            "project_info": {
                "name": name,
                "description": description,
                "author": "",
                "created_at": datetime.now().isoformat(),
                "modified_at": datetime.now().isoformat(),
                "app_version": "1.0.0"
            },
            "main_tree": "tree.json",
            "resources": {
                "images": [],
                "scripts": [],
                "audio": []
            }
        }
        
        with open(os.path.join(self.project_root, "project.json"), 'w', encoding='utf-8') as f:
            json.dump(project_config, f, indent=2, ensure_ascii=False)
    
    def save_project(self, tree_data: Dict[str, Any]) -> None:
        """保存项目"""
        # 创建备份
        self._create_backup()
        
        # 保存行为树文件
        tree_file = os.path.join(self.project_root, "tree.json")
        with open(tree_file, 'w', encoding='utf-8') as f:
            json.dump(tree_data, f, indent=2, ensure_ascii=False)
        
        # 更新项目配置
        project_config = self.load_project()
        project_config["project_info"]["modified_at"] = datetime.now().isoformat()
        
        with open(os.path.join(self.project_root, "project.json"), 'w', encoding='utf-8') as f:
            json.dump(project_config, f, indent=2, ensure_ascii=False)
    
    def _create_backup(self) -> None:
        """创建备份文件"""
        tree_file = os.path.join(self.project_root, "tree.json")
        
        if not os.path.exists(tree_file):
            return
        
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        backup_file = os.path.join(
            self.project_root,
            f"tree_backup_{timestamp}.json"
        )
        
        shutil.copy2(tree_file, backup_file)
        
        # 清理旧备份，只保留最近 5 个
        self._clean_old_backups()
```

**备份机制：**
- 每次保存时自动创建备份
- 备份文件名包含时间戳
- 自动清理旧备份，保留最近 5 个

### 12.4 PackageExporter 打包导出器

PackageExporter 将项目打包为 ZIP 文件，便于分享和分发。

```python
class PackageExporter:
    """项目打包导出器"""
    
    EXCLUDE_DIRS = ['.metadata', '__pycache__', '.git', 'node_modules', 'cache']
    EXCLUDE_FILES = ['tree_backup_', 'screenshot_', '.DS_Store', 'Thumbs.db']
    EXCLUDE_EXTENSIONS = ['.pyc', '.pyo', '.tmp', '.bak', '.log']
    
    def __init__(self, project_root: str):
        self.project_root = project_root
    
    def export_to_zip(self, output_path: str = None) -> str:
        """导出项目为 ZIP 文件"""
        if output_path is None:
            project_name = os.path.basename(self.project_root)
            output_path = f"{project_name}.zip"
        
        with zipfile.ZipFile(output_path, 'w', zipfile.ZIP_DEFLATED) as zipf:
            for root, dirs, files in os.walk(self.project_root):
                # 排除临时目录
                dirs[:] = [d for d in dirs if d not in self.EXCLUDE_DIRS]
                
                for file in files:
                    # 排除临时文件
                    if self._should_exclude_file(file):
                        continue
                    
                    file_path = os.path.join(root, file)
                    arcname = os.path.relpath(file_path, os.path.dirname(self.project_root))
                    zipf.write(file_path, arcname)
        
        # 生成使用说明
        self._generate_readme(output_path)
        
        return output_path
    
    def _generate_readme(self, zip_path: str):
        """生成使用说明"""
        project_name = os.path.basename(self.project_root)
        readme_content = f"""# {project_name} 使用说明

## 如何使用
1. 解压 ZIP 文件到任意目录
2. 打开 AutoDoor 行为树编辑器
3. 选择"文件" -> "打开项目"
4. 选择解压后的文件夹
5. 点击"开始运行"按钮

## 文件说明
- project.json: 项目配置
- tree.json: 行为树定义
- images/: 图像资源
- scripts/: 脚本文件
- audio/: 音频文件

## 注意事项
- 解压后请保持文件夹结构完整
- 不要单独移动或删除资源文件
"""
        readme_path = os.path.splitext(zip_path)[0] + "_使用说明.txt"
        with open(readme_path, 'w', encoding='utf-8') as f:
            f.write(readme_content)
```

**自动排除的文件：**
- Python 编译文件 (.pyc, .pyo)
- 缓存目录 (__pycache__, .metadata/, cache/)
- 临时文件 (.tmp, .bak, .log)
- 备份文件 (tree_backup_*, screenshot_*)
- 系统文件 (.DS_Store, Thumbs.db)

### 12.5 PackageImporter 项目导入器

PackageImporter 从 ZIP 压缩包导入项目到默认工作区。

```python
class PackageImporter:
    """项目导入器"""
    
    REQUIRED_FILES = ["project.json", "tree.json"]
    
    def __init__(self):
        pass
    
    def validate_package(self, zip_path: str) -> Tuple[bool, str]:
        """验证 ZIP 文件是否为有效的项目压缩包
        
        Args:
            zip_path: ZIP 文件路径
            
        Returns:
            Tuple[bool, str]: (是否有效, 错误信息)
        """
        if not os.path.exists(zip_path):
            return False, "文件不存在"
        
        if not zipfile.is_zipfile(zip_path):
            return False, "不是有效的 ZIP 文件"
        
        try:
            with zipfile.ZipFile(zip_path, 'r') as zipf:
                namelist = zipf.namelist()
                
                if not namelist:
                    return False, "ZIP 文件为空"
                
                found_project_json = False
                found_tree_json = False
                
                for name in namelist:
                    basename = os.path.basename(name)
                    if basename == "project.json":
                        found_project_json = True
                    elif basename == "tree.json":
                        found_tree_json = True
                
                if not found_project_json:
                    return False, "缺少 project.json 文件"
                
                if not found_tree_json:
                    return False, "缺少 tree.json 文件"
                
                return True, ""
                
        except zipfile.BadZipFile:
            return False, "ZIP 文件已损坏"
        except Exception as e:
            return False, f"读取 ZIP 文件失败: {str(e)}"
    
    def get_project_name(self, zip_path: str) -> Optional[str]:
        """从 ZIP 文件中获取项目名称"""
        try:
            with zipfile.ZipFile(zip_path, 'r') as zipf:
                namelist = zipf.namelist()
                
                if not namelist:
                    return None
                
                # 从第一个条目获取项目文件夹名
                first_entry = namelist[0]
                if '/' in first_entry:
                    project_folder = first_entry.split('/')[0]
                    return project_folder
                
                # 尝试从 project.json 获取项目名
                for name in namelist:
                    basename = os.path.basename(name)
                    if basename == "project.json":
                        import json
                        with zipf.open(name) as f:
                            project_config = json.load(f)
                            project_info = project_config.get("project_info", {})
                            return project_info.get("name", None)
                
                return None
                
        except Exception:
            return None
    
    def import_from_zip(
        self, 
        zip_path: str, 
        target_dir: str,
        overwrite: bool = False,
        new_name: Optional[str] = None
    ) -> Tuple[bool, str, Optional[str]]:
        """从 ZIP 文件导入项目
        
        Args:
            zip_path: ZIP 文件路径
            target_dir: 目标目录
            overwrite: 是否覆盖已存在的项目
            new_name: 新项目名称（用于重命名）
            
        Returns:
            Tuple[bool, str, Optional[str]]: (是否成功, 错误信息, 导入后的项目路径)
        """
        is_valid, error_msg = self.validate_package(zip_path)
        if not is_valid:
            return False, error_msg, None
        
        project_name = new_name or self.get_project_name(zip_path)
        if not project_name:
            project_name = os.path.splitext(os.path.basename(zip_path))[0]
        
        project_root = os.path.join(target_dir, project_name)
        
        if os.path.exists(project_root) and not overwrite:
            return False, "PROJECT_EXISTS", project_root
        
        try:
            os.makedirs(target_dir, exist_ok=True)
            
            if os.path.exists(project_root):
                shutil.rmtree(project_root)
            
            with zipfile.ZipFile(zip_path, 'r') as zipf:
                zip_root = self.get_project_root_in_zip(zip_path)
                
                for member in zipf.namelist():
                    if zip_root:
                        if not member.startswith(zip_root + "/"):
                            continue
                        relative_path = member[len(zip_root) + 1:]
                    else:
                        relative_path = member
                    
                    if not relative_path or relative_path.endswith("/"):
                        continue
                    
                    target_path = os.path.join(project_root, relative_path)
                    os.makedirs(os.path.dirname(target_path), exist_ok=True)
                    
                    with zipf.open(member) as source, open(target_path, 'wb') as target:
                        target.write(source.read())
            
            return True, "", project_root
            
        except Exception as e:
            return False, f"解压失败: {str(e)}", None
    
    def generate_new_name(self, base_name: str, target_dir: str) -> str:
        """生成不冲突的新项目名称"""
        if not os.path.exists(os.path.join(target_dir, base_name)):
            return base_name
        
        counter = 1
        while True:
            new_name = f"{base_name}_{counter}"
            if not os.path.exists(os.path.join(target_dir, new_name)):
                return new_name
            counter += 1
```

**验证规则：**
- 检查文件是否存在
- 检查是否为有效的 ZIP 文件
- 检查 ZIP 内是否包含 `project.json`
- 检查 ZIP 内是否包含 `tree.json`

**冲突处理策略：**
| 策略 | 说明 |
|------|------|
| 覆盖 | 删除现有项目目录，重新解压 |
| 重命名 | 自动添加后缀（如 MyProject_1, MyProject_2） |
| 取消 | 终止导入操作 |

**使用示例：**

```python
# 在编辑器中导入项目
def import_tree(self):
    from tkinter import filedialog, messagebox
    from bt_utils.package_importer import PackageImporter
    from config.settings_manager import SettingsManager
    
    # 选择 ZIP 文件
    zip_path = filedialog.askopenfilename(
        title="选择项目压缩包",
        filetypes=[("ZIP文件", "*.zip"), ("所有文件", "*.*")]
    )
    
    if not zip_path:
        return
    
    importer = PackageImporter()
    
    # 验证 ZIP 文件
    is_valid, error_msg = importer.validate_package(zip_path)
    if not is_valid:
        messagebox.showerror("错误", f"无效的项目压缩包:\n{error_msg}")
        return
    
    # 获取默认工作区路径
    workspace_path = SettingsManager.get_default_workspace_path()
    
    # 检查同名冲突
    project_name = importer.get_project_name(zip_path)
    target_project_path = os.path.join(workspace_path, project_name)
    
    if os.path.exists(target_project_path):
        # 询问用户处理方式
        result = messagebox.askyesnocancel("项目已存在", "...")
        if result is None:
            return  # 取消
        elif result:
            overwrite = True  # 覆盖
        else:
            new_name = importer.generate_new_name(project_name, workspace_path)
            overwrite = False  # 重命名
    
    # 执行导入
    success, error_msg, imported_path = importer.import_from_zip(
        zip_path, workspace_path, overwrite=overwrite, new_name=new_name
    )
    
    if success:
        # 打开导入的项目
        self.open_project(imported_path)
        messagebox.showinfo("成功", f"项目导入成功！")
```

### 12.6 ResourceService 资源管理服务

ResourceService 提供统一的资源管理功能，包括收集外部资源、导入资源到项目、缓存管理等。

```python
class ResourceService:
    """资源管理服务"""
    
    RESOURCE_TYPE_MAP = {
        'template_path': 'image',
        'script_path': 'script',
        'code_path': 'code',
        'sound_path': 'audio',
    }
    
    TYPE_DIR_MAP = {
        'image': 'images/templates',
        'script': 'scripts/script',
        'code': 'scripts/code',
        'audio': 'audio/alarms',
        'data': 'data/config',
        'other': 'data/other',
    }
    
    CACHE_DIR = 'cache'
    
    def __init__(self, project_root: str):
        self.project_root = project_root
        self.resolver = PathResolver(project_root)
    
    def collect_external_resources(self, tree_data: Dict) -> List[Dict]:
        """收集行为树中引用的外部资源"""
        external_resources = []
        nodes = tree_data.get('nodes', {})
        
        for node_id, node_data in nodes.items():
            for path_key, resource_type in self.RESOURCE_TYPE_MAP.items():
                path = node_data.get('config', {}).get(path_key)
                if path and not path.startswith('./'):
                    external_resources.append({
                        'node_id': node_id,
                        'path_key': path_key,
                        'path': path,
                        'resource_type': resource_type
                    })
        
        return external_resources
    
    def import_resources_to_project(self, resources: List[Dict]) -> Dict[str, str]:
        """批量导入资源到项目目录"""
        path_mapping = {}
        
        for resource in resources:
            new_path = self.import_single_file_to_project(
                resource['path'],
                resource['resource_type']
            )
            if new_path:
                path_mapping[resource['node_id'] + '.' + resource['path_key']] = new_path
        
        return path_mapping
    
    def import_single_file_to_project(
        self, 
        source_path: str, 
        resource_type: str,
        old_path: str = None
    ) -> str:
        """导入单个文件到项目目录"""
        target_dir = self.TYPE_DIR_MAP.get(resource_type, 'data/other')
        filename = os.path.basename(source_path)
        target_path = os.path.join(self.project_root, target_dir, filename)
        
        # 处理文件名冲突
        if os.path.exists(target_path):
            name, ext = os.path.splitext(filename)
            counter = 2
            while os.path.exists(target_path):
                filename = f"{name}_{counter}{ext}"
                target_path = os.path.join(self.project_root, target_dir, filename)
                counter += 1
        
        # 复制文件
        os.makedirs(os.path.dirname(target_path), exist_ok=True)
        shutil.copy2(source_path, target_path)
        
        # 如果有旧文件，移动到缓存目录
        if old_path:
            self.move_to_cache(old_path)
        
        return self.resolver.to_relative(target_path)
    
    def move_to_cache(self, file_path: str) -> bool:
        """将文件移动到缓存目录"""
        if not file_path or file_path.startswith('./'):
            return False
        
        if not os.path.exists(file_path):
            return False
        
        cache_dir = os.path.join(self.project_root, self.CACHE_DIR)
        os.makedirs(cache_dir, exist_ok=True)
        
        filename = os.path.basename(file_path)
        name, ext = os.path.splitext(filename)
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        cache_filename = f"{name}_{timestamp}{ext}"
        cache_path = os.path.join(cache_dir, cache_filename)
        
        shutil.move(file_path, cache_path)
        return True
    
    def update_tree_paths(self, tree_data: Dict, path_mapping: Dict[str, str]) -> Dict:
        """更新行为树中的路径引用"""
        nodes = tree_data.get('nodes', {})
        
        for key, new_path in path_mapping.items():
            node_id, path_key = key.rsplit('.', 1)
            if node_id in nodes:
                if 'config' not in nodes[node_id]:
                    nodes[node_id]['config'] = {}
                nodes[node_id]['config'][path_key] = new_path
        
        return tree_data
```

**使用示例：**

```python
# 在保存项目时导入外部资源
def save_tree(self):
    tree_data = self.canvas.get_tree_data()
    
    if self.project_root:
        from bt_utils.resource_service import ResourceService
        resource_service = ResourceService(self.project_root)
        
        # 收集外部资源
        external_resources = resource_service.collect_external_resources(tree_data)
        
        if external_resources:
            # 导入资源到项目目录
            path_mapping = resource_service.import_resources_to_project(external_resources)
            
            # 更新行为树中的路径引用
            tree_data = resource_service.update_tree_paths(tree_data, path_mapping)
    
    # 保存行为树
    self.project_manager.save_project(tree_data)
```

### 12.6 ExecutionContext 扩展

ExecutionContext 扩展了路径解析功能，支持相对路径的运行时解析。

```python
class ExecutionContext:
    """执行上下文（扩展）"""
    
    def __init__(self, project_root: str = None):
        self.blackboard = Blackboard()
        self.elapsed_time = 0.0
        self.tick_count = 0
        self.project_root = project_root or os.getcwd()
        self._path_resolver = None
        # ... 其他初始化
    
    def resolve_path(self, relative_path: str) -> str:
        """解析相对路径为绝对路径"""
        if self._path_resolver is None:
            from bt_utils.path_resolver import PathResolver
            self._path_resolver = PathResolver(self.project_root)
        
        if relative_path.startswith("./"):
            return self._path_resolver.to_absolute(relative_path)
        return relative_path
```

**使用示例：**

```python
# 在节点中使用相对路径
class ImageConditionNode(ConditionNode):
    def _check_condition(self, context: ExecutionContext) -> bool:
        template_path = self.config.get("template_path")
        
        # 解析相对路径为绝对路径
        absolute_path = context.resolve_path(template_path)
        
        # 加载图像资源
        template = Image.open(absolute_path)
        
        # ... 执行图像匹配逻辑
```

### 12.7 用户界面集成

#### 12.7.1 工具栏添加导出按钮

```python
# bt_gui/bt_editor/toolbar.py
class EditorToolbar(ctk.CTkFrame):
    def __init__(
        self,
        master,
        app,
        on_new: Optional[Callable] = None,
        on_load: Optional[Callable] = None,
        on_save: Optional[Callable] = None,
        on_export: Optional[Callable] = None,  # 新增
        # ...
    ):
        # ...
        
        # 添加导出按钮
        ctk.CTkButton(
            file_frame,
            text="导出",
            width=70,
            fg_color=self._dark_colors['bg_tertiary'],
            hover_color=self._dark_colors['border'],
            text_color=self._dark_colors['text_primary'],
            command=self._on_export_click,
            **btn_config
        ).pack(side="left", padx=Theme.DIMENSIONS['spacing_xs'])
```

#### 12.7.2 编辑器添加导出功能

```python
# bt_gui/bt_editor/editor.py
class BehaviorTreeEditor(ctk.CTkFrame):
    def export_tree(self):
        """导出行为树项目为 ZIP 文件"""
        from tkinter import filedialog, messagebox
        
        if not self.file_path:
            messagebox.showwarning("提示", "请先保存项目")
            return
        
        project_root = os.path.dirname(self.file_path)
        
        output_path = filedialog.asksaveasfilename(
            title="导出项目",
            defaultextension=".zip",
            filetypes=[("ZIP文件", "*.zip"), ("所有文件", "*.*")]
        )
        
        if not output_path:
            return
        
        try:
            from bt_utils.package_exporter import PackageExporter
            exporter = PackageExporter(project_root)
            zip_path = exporter.export_to_zip(output_path)
            
            messagebox.showinfo("成功", f"项目已导出到:\n{zip_path}")
            
        except Exception as e:
            messagebox.showerror("错误", f"导出失败: {str(e)}")
```

### 12.8 Bug 修复

#### 12.8.1 脚本文件过滤器修复

**问题**: 脚本文件浏览时默认只显示 .txt 文件，无法选择 .json 文件

**修复**: 修改 `bt_gui/bt_editor/property.py` 中的文件类型过滤器

```python
# 修改前
"ScriptNode": [
    {"key": "script_path", "label": "脚本路径", "type": "file", "width": 120, 
     "filetypes": [("脚本文件", "*.txt"), ("所有文件", "*.*")]},
    ...
]

# 修改后
"ScriptNode": [
    {"key": "script_path", "label": "脚本路径", "type": "file", "width": 120, 
     "filetypes": [("所有文件", "*.*")]},
    ...
]
```

#### 12.8.2 报警节点默认值修复

**问题**: 报警节点添加时没有使用设置中的默认音效路径和音量

**修复**: 修改 `bt_nodes/actions/alarm.py` 从设置中读取默认值

```python
# 修改前
def __init__(self, node_id: str = None, config: NodeConfig = None):
    super().__init__(node_id, config)
    self.sound_path = self.config.get("sound_path", "")
    self.volume = self.config.get_int("volume", 70)
    ...

# 修改后
def __init__(self, node_id: str = None, config: NodeConfig = None):
    super().__init__(node_id, config)
    
    from bt_utils.resource_manager import ResourceManager
    from config.settings_manager import SettingsManager
    
    default_sound = ResourceManager().get_alarm_sound_path()
    default_volume = SettingsManager().get("alarm_volume", 70)
    
    self.sound_path = self.config.get("sound_path", default_sound)
    self.volume = self.config.get_int("volume", default_volume)
    ...
```

**效果**: 报警节点创建时自动使用设置中配置的默认音效路径和音量。

---

## 13 工具栏路径显示优化

### 13.1 自适应布局实现

工具栏中的文件路径显示区域采用自适应布局，自动扩展填充剩余空间：

```python
# bt_gui/bt_editor/toolbar.py

def _create_ui(self):
    main_container = ctk.CTkFrame(self, fg_color="transparent")
    main_container.pack(fill="x", padx=spacing_md, pady=spacing_sm)
    
    left_section = ctk.CTkFrame(main_container, fg_color="transparent")
    left_section.pack(side="left")
    
    # 左侧按钮组
    self._create_file_buttons(left_section)
    self._create_separator(left_section)
    self._create_edit_buttons(left_section)
    self._create_separator(left_section)
    self._create_run_buttons(left_section)
    
    # 路径显示区域 - 放在中间，自动扩展
    self._create_path_display(main_container)
    
    # 右侧按钮组
    right_section = ctk.CTkFrame(main_container, fg_color="transparent")
    right_section.pack(side="right")
    
    self._create_open_folder_button(right_section)
    self._create_reset_view_button(right_section)

def _create_path_display(self, parent):
    path_frame = ctk.CTkFrame(parent, fg_color="transparent")
    # 关键：fill="x" 和 expand=True 实现自适应宽度
    path_frame.pack(side="left", fill="x", expand=True, padx=(spacing_md, 0))
    
    self.file_path_var = StringVar(value="未保存")
    
    self.file_path_entry = ctk.CTkEntry(
        path_frame,
        textvariable=self.file_path_var,
        font=Theme.get_font('sm'),
        text_color=self._dark_colors['text_muted'],
        fg_color=self._dark_colors['bg_secondary'],
        border_width=0,
        height=button_height,
        state="readonly"
    )
    # 关键：fill="x" 和 expand=True 让 Entry 填充整个 frame
    self.file_path_entry.pack(fill="x", expand=True)

def set_project_path(self, project_root: Optional[str]):
    """设置项目路径，显示完整路径"""
    if project_root:
        self.file_path_var.set(project_root)  # 显示完整路径，不再截断
        self.file_path_entry.tooltip = project_root
    else:
        self.file_path_var.set("未保存")
        self.file_path_entry.tooltip = None
```

### 13.2 布局原理

```
┌─────────────────────────────────────────────────────────────────────┐
│                          main_container                              │
│  ┌──────────────┐  ┌────────────────────────────┐  ┌──────────────┐ │
│  │ left_section │  │      path_frame            │  │right_section │ │
│  │  (固定宽度)   │  │  (fill="x", expand=True)  │  │  (固定宽度)   │ │
│  │              │  │                            │  │              │ │
│  │ [新建][打开] │  │  D:/Projects/MyProject/... │  │ [打开][重置] │ │
│  │ [保存][导出] │  │  ← 自动扩展填充剩余空间 →   │  │ [文件][视图] │ │
│  │ [撤销][回退] │  │  ← 超出部分可拖动查看 →     │  │              │ │
│  │ [开始][停止] │  │                            │  │              │ │
│  └──────────────┘  └────────────────────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 13.3 窗口标题管理

窗口标题采用统一格式，从 `main.py` 获取版本号：

```python
# bt_gui/app.py

def _get_app_title() -> str:
    """获取应用标题，包含版本号"""
    try:
        from main import VERSION
        return f"autodoor - 行为树 {VERSION}"
    except ImportError:
        return "autodoor - 行为树"

class BehaviorTreeApp(ctk.CTk):
    def __init__(self):
        # ...
        self.title(_get_app_title())
    
    def _restore_last_file(self):
        """恢复上次打开的文件"""
        last_file = self._settings.get_last_file_path()
        if last_file and os.path.exists(last_file):
            try:
                if hasattr(self, 'behavior_tree') and self.behavior_tree:
                    self.behavior_tree.load_tree(last_file)
                    self._update_window_title()  # 更新窗口标题
                    print(f"[OK] 已自动加载上次文件: {last_file}")
            except Exception as e:
                print(f"[WARN] 自动加载上次文件失败: {e}")
    
    def _update_window_title(self):
        """更新窗口标题，显示项目名称"""
        project_name = None
        if hasattr(self.behavior_tree, 'project_root') and self.behavior_tree.project_root:
            project_name = os.path.basename(self.behavior_tree.project_root)
        
        if project_name:
            try:
                from main import VERSION
                self.title(f"autodoor - 行为树 {VERSION} - {project_name}")
            except ImportError:
                self.title(f"autodoor - 行为树 - {project_name}")
        else:
            self.title(_get_app_title())
```

### 13.4 编辑器标题更新

编辑器中打开项目时同步更新窗口标题：

```python
# bt_gui/bt_editor/editor.py

def _update_title(self, project_name: str):
    """更新窗口标题"""
    try:
        from main import VERSION
        self.winfo_toplevel().title(f"autodoor - 行为树 {VERSION} - {project_name}")
    except ImportError:
        self.winfo_toplevel().title(f"autodoor - 行为树 - {project_name}")
```

### 13.5 标题显示场景

| 场景 | 触发时机 | 标题格式 |
|------|----------|----------|
| 应用启动 | `BehaviorTreeApp.__init__()` | `autodoor - 行为树 V1.1.3` |
| 打开项目 | `open_project()` → `_update_title()` | `autodoor - 行为树 V1.1.3 - 项目名` |
| 新建项目 | `new_project()` → `_update_title()` | `autodoor - 行为树 V1.1.3 - 项目名` |
| 自动加载 | `_restore_last_file()` → `_update_window_title()` | `autodoor - 行为树 V1.1.3 - 项目名` |

---

## 14 UI 更新分发器实现（防止假卡死）

### 14.1 问题背景

tkinter 的 GUI 操作必须在主线程执行，但行为树引擎在独立线程中运行。如果直接从后台线程调用 tkinter 的 `update()` 或 `event_generate()`，可能导致：

1. **界面假卡死**：点击"开始运行"后，界面看起来卡住，节点状态不更新
2. **更新延迟**：需要鼠标点击才能触发界面更新
3. **再次运行异常**：手动停止后再次运行，界面节点高亮和状态灯显示异常

### 14.2 核心解决方案

采用**定时轮询 + 后台守护**混合模式：

```python
# bt_utils/ui_dispatcher.py

class UIUpdateDispatcher:
    def __init__(self):
        self._task_queue: Queue = Queue()  # 线程安全队列
        self._widget = None
        self._polling_active = False
        self._polling_interval_ms = 30  # 30ms 轮询间隔
        self._node_status_cache: Dict[str, tuple] = {}  # 节点状态缓存
    
    def dispatch_node_status(self, node_id: str, status: str, callback: Callable = None):
        """从后台线程调用，分发节点状态更新"""
        if node_id:
            self._node_status_cache[node_id] = ({"node_id": node_id, "status": status}, callback)
        
        task = UpdateTask(UpdateType.NODE_STATUS, {"node_id": node_id, "status": status}, callback)
        self._task_queue.put(task)  # 线程安全
        # 注意：不调用 after(0, ...)，完全依赖轮询机制
```

### 14.3 轮询机制实现

**关键：轮询必须在编辑器初始化时启动，而不是运行时启动。**

```python
# bt_gui/bt_editor/editor.py

def _init_ui_dispatcher(self):
    """初始化UI更新分发器"""
    from bt_utils.ui_dispatcher import UIUpdateDispatcher
    self._dispatcher = UIUpdateDispatcher.get_instance()
    self._dispatcher.attach(self)
    self._dispatcher.start_polling()  # 必须在初始化时启动！

def _start_ui_polling(self):
    """启动UI轮询 - 已在初始化时启动，此处为空"""
    pass

def _stop_ui_polling(self):
    """停止UI轮询 - 不要停止！让轮询持续运行"""
    pass
```

### 14.4 轮询守护机制

当 `_widget` 为 None 时，使用后台线程继续尝试调度：

```python
# bt_utils/ui_dispatcher.py

def _poll(self):
    """主线程轮询"""
    if not self._polling_active:
        return
    
    widget = self._widget
    
    try:
        if widget is not None:
            pending = self._task_queue.qsize()
            if pending > 0:
                self._process_updates()
    except Exception:
        pass
    
    if self._polling_active:
        if self._widget is not None:
            # 正常情况：主线程调度下一次轮询
            self._widget.after(self._polling_interval_ms, self._poll)
        else:
            # 异常情况：后台线程守护，等待 _widget 可用
            threading.Timer(self._polling_interval_ms / 1000, self._poll_daemon).start()

def _poll_daemon(self):
    """后台线程轮询，用于在 _widget 为 None 时继续尝试"""
    if self._polling_active and self._widget is not None:
        # _widget 可用，切换回主线程轮询
        self._widget.after(0, self._poll)
    elif self._polling_active:
        # 继续等待
        threading.Timer(self._polling_interval_ms / 1000, self._poll_daemon).start()
```

### 14.5 防止假卡死的关键要点

| 问题 | 错误做法 | 正确做法 |
|------|---------|---------|
| 启动时假卡死 | 在 `_start_running()` 中启动轮询 | 在 `_init_ui_dispatcher()` 中启动轮询 |
| 手动停止后异常 | 停止时调用 `stop_polling()` | **不要停止轮询**，让它持续运行 |
| 轮询永久停止 | `_widget` 为 None 时直接返回 | 使用后台线程守护，等待 `_widget` 可用 |
| `after(0, ...)` 不可靠 | 从后台线程调用 `after(0, ...)` | 完全依赖定时轮询机制 |

### 14.6 完整流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           防止假卡死的完整流程                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 编辑器初始化                                                            │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ _init_ui_dispatcher()                                             │   │
│     │   ├── UIUpdateDispatcher.get_instance()                          │   │
│     │   ├── dispatcher.attach(self)                                    │   │
│     │   └── dispatcher.start_polling()  ← 轮询启动                     │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  2. 轮询持续运行 (30ms 间隔)                                                │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ _poll()                                                           │   │
│     │   ├── 检查队列是否有任务                                          │   │
│     │   ├── 有任务 → _process_updates()                                 │   │
│     │   └── after(30ms, _poll)  → 继续轮询                              │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  3. 用户点击"开始运行"                                                      │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ _start_running()                                                  │   │
│     │   ├── engine.start() → 后台线程启动                               │   │
│     │   └── _start_ui_polling() → pass (轮询已在运行)                   │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  4. 后台线程执行节点                                                        │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ Engine.tick() (后台线程)                                          │   │
│     │   ├── node.tick() → 返回状态                                      │   │
│     │   └── dispatch_node_status() → Queue.put(task)                   │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  5. 轮询检测到任务并处理                                                    │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ _poll() (主线程)                                                  │   │
│     │   ├── pending > 0 → _process_updates()                           │   │
│     │   └── callback(node_id, status) → UI 更新                        │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  6. 用户手动停止                                                            │
│     ┌──────────────────────────────────────────────────────────────────┐   │
│     │ _stop_running_in_main_thread()                                    │   │
│     │   ├── engine.stop()                                               │   │
│     │   └── _stop_ui_polling() → pass (轮询继续运行！)                  │   │
│     └──────────────────────────────────────────────────────────────────┘   │
│                              ↓                                              │
│  7. 再次运行 → 正常工作 (轮询一直在运行)                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 15 放大镜功能实现

### 15.1 放大镜窗口设计

MagnifierWindow 提供屏幕放大镜功能，用于区域选择、位置选择时的精确定位：

```python
# bt_utils/magnifier.py

class MagnifierWindow:
    """放大镜窗口"""
    
    def __init__(self, zoom_factor: int = 4, size: int = 150):
        self.zoom_factor = zoom_factor
        self.size = size
        self.window = None
        self.canvas = None
        self.info_label = None
        self._selection_rect = None
    
    def show(self, x: int, y: int):
        """显示放大镜窗口"""
        if self.window is not None:
            return
        
        self.window = tk.Toplevel()
        self.window.overrideredirect(True)  # 无边框窗口
        self.window.attributes('-topmost', True)
        
        # 创建画布显示放大内容
        self.canvas = tk.Canvas(
            self.window,
            width=self.size,
            height=self.size,
            bg='#2b2b2b',
            highlightthickness=0
        )
        self.canvas.pack()
        
        # 创建信息标签
        self.info_label = tk.Label(
            self.window,
            text="",
            font=("Consolas", 9),
            bg="#2b2b2b",
            fg="#ffffff",
            anchor="w",
            padx=5,
            pady=2,
            justify="left",
            highlightthickness=0
        )
        self.info_label.pack(fill="x")
        
        self.update(x, y)
    
    def update(self, x: int, y: int):
        """更新放大镜内容"""
        if self.window is None:
            return
        
        # 截取屏幕区域并放大
        # ... 截图和放大逻辑 ...
        
        # 更新信息显示
        color = self._get_pixel_color(x, y)
        r, g, b = color[0], color[1], color[2]
        hex_color = f"#{r:02X}{g:02X}{b:02X}"
        
        info_text = f"坐标: ({x}, {y})\n颜色: {hex_color}\nRGB({r}, {g}, {b})"
        self.info_label.config(text=info_text)
        
        # 更新窗口位置
        self.window.geometry(f"+{x + 20}+{y + 20}")
    
    def hide(self):
        """隐藏放大镜窗口"""
        if self.window:
            self.window.destroy()
            self.window = None
```

### 15.2 放大镜在属性面板中的集成

放大镜功能集成在属性面板的选择字段中：

```python
# bt_gui/bt_editor/property.py

class RegionField(FieldWidget):
    """区域选择字段"""
    
    def _select_region(self):
        """选择屏幕区域"""
        magnifier = MagnifierWindow(zoom_factor=4, size=150)
        magnifier_shown = [False]
        
        def on_mouse_move(event):
            if not magnifier_shown[0]:
                magnifier.show(event.x_root, event.y_root)
                magnifier_shown[0] = True
            else:
                magnifier.update(event.x_root, event.y_root)
        
        def on_mouse_press(event):
            self._start_pos = (event.x_root, event.y_root)
        
        def on_mouse_release(event):
            magnifier.hide()
            # 计算区域并更新值
            # ...
        
        # 绑定事件
        root.bind('<Motion>', on_mouse_move)
        root.bind('<ButtonPress-1>', on_mouse_press)
        root.bind('<ButtonRelease-1>', on_mouse_release)
```

### 15.3 重要注意事项

**必须先调用 show() 再调用 update()：**

```python
# 错误做法：直接调用 update()
magnifier = MagnifierWindow()
magnifier.update(x, y)  # 无效！window 为 None，直接返回

# 正确做法：先调用 show()
magnifier = MagnifierWindow()
magnifier.show(x, y)    # 创建窗口
magnifier.update(x, y)  # 更新内容
```

**使用标志位控制首次显示：**

```python
magnifier_shown = [False]

def on_mouse_move(event):
    if not magnifier_shown[0]:
        magnifier.show(event.x_root, event.y_root)
        magnifier_shown[0] = True
    else:
        magnifier.update(event.x_root, event.y_root)
```

### 15.4 多屏显示器兼容性

放大镜完全支持多显示器环境，使用虚拟桌面坐标系统：

**获取虚拟桌面边界：**

```python
def _get_virtual_screen_bounds(self) -> Tuple[int, int, int, int]:
    """获取虚拟桌面边界（支持多显示器）"""
    try:
        import screeninfo
        monitors = screeninfo.get_monitors()
        if monitors:
            min_x = min(monitor.x for monitor in monitors)
            min_y = min(monitor.y for monitor in monitors)
            max_x = max(monitor.x + monitor.width for monitor in monitors)
            max_y = max(monitor.y + monitor.height for monitor in monitors)
            return (min_x, min_y, max_x, max_y)
    except ImportError:
        pass
    
    # Windows API 备用方案
    try:
        import ctypes
        user32 = ctypes.windll.user32
        virtual_left = user32.GetSystemMetrics(76)   # SM_XVIRTUALSCREEN
        virtual_top = user32.GetSystemMetrics(77)    # SM_YVIRTUALSCREEN
        virtual_width = user32.GetSystemMetrics(78)  # SM_CXVIRTUALSCREEN
        virtual_height = user32.GetSystemMetrics(79) # SM_CYVIRTUALSCREEN
        return (virtual_left, virtual_top, 
                virtual_left + virtual_width, 
                virtual_top + virtual_height)
    except Exception:
        pass
    
    return (0, 0, 1920, 1080)
```

**多屏截图支持：**

```python
# 使用 all_screens=True 参数支持多屏截图
screenshot = ImageGrab.grab(bbox=(left, top, right, bottom), all_screens=True)
```

**虚拟桌面坐标系统：**

```
多显示器坐标系统示意：

                副显示器2 (负坐标区域)
                ┌─────────────────┐
                │ (-1920, 0)      │
                │                 │
                └─────────────────┘
                                主显示器 (原点)
                                ┌─────────────────┐
                                │ (0, 0)          │
                                │                 │
                                └─────────────────┘
                                                副显示器1
                                                ┌─────────────────┐
                                                │ (1920, 0)       │
                                                │                 │
                                                └─────────────────┘
```

### 15.5 选择框绘制

选择框使用黑色边框确保可见性：

```python
def set_selection_rect(self, x1: int, y1: int, x2: int, y2: int):
    """设置选择框区域"""
    if self._selection_rect:
        self.canvas.delete(self._selection_rect)
    
    self._selection_rect = self.canvas.create_rectangle(
        x1, y1, x2, y2,
        outline='#000000',  # 黑色边框
        width=2
    )
```

### 15.6 ESC 取消选择功能

区域选择、位置选择等操作支持按 ESC 键取消：

**关键实现：**

使用 `grab_set()` 确保选择窗口能接收键盘事件：

```python
def _start_selection(self):
    # 创建选择窗口
    select_window = tk.Toplevel(self.app)
    select_window.overrideredirect(True)
    select_window.attributes("-topmost", True)
    
    # 绑定 ESC 键取消选择
    def on_escape(e):
        magnifier.hide()
        select_window.destroy()
        self.app.deiconify()
    
    select_window.bind("<Escape>", on_escape)
    select_window.focus_set()
    select_window.grab_set()  # 关键：强制窗口获取所有输入事件
```

**重要说明：**

当使用 `overrideredirect(True)` 创建无边框窗口时，窗口管理器不会处理该窗口，导致键盘事件可能无法正常接收。`grab_set()` 会强制窗口获取所有输入事件（包括键盘事件），确保 ESC 键能被正确捕获。

### 15.7 按键捕获字段 (KeyField)

KeyField 用于捕获用户按下的按键，支持再次点击取消：

**实现要点：**

```python
class KeyField(FieldWidget):
    def __init__(self, master, label: str, key: str, on_change: Callable, **kwargs):
        self._listening = False
        self._bound_keys = []  # 记录所有绑定的事件
        super().__init__(master, label, key, on_change, **kwargs)
    
    def _start_listening(self):
        if self._listening:
            self._cancel_listening()  # 再次点击时取消
            return
        
        self._listening = True
        self._bound_keys = []
        self.btn.configure(text="取消", fg_color=self._dark_colors['warning'])
        
        toplevel = self.winfo_toplevel()
        
        # 绑定通用按键事件
        toplevel.bind("<KeyPress>", on_key_press)
        self._bound_keys.append("<KeyPress>")
        
        # 绑定特定按键事件（阻止快捷键触发）
        toplevel.bind("<Delete>", lambda e: on_special_key(e, "delete"))
        self._bound_keys.append("<Delete>")
        
        toplevel.bind("<BackSpace>", lambda e: on_special_key(e, "backspace"))
        self._bound_keys.append("<BackSpace>")
    
    def _cancel_listening(self):
        self._listening = False
        self._unbind_all_keys()
        self.btn.configure(text="修改", fg_color=self._dark_colors['primary'])
    
    def _unbind_all_keys(self):
        toplevel = self.winfo_toplevel()
        for key in self._bound_keys:
            toplevel.unbind(key)
        self._bound_keys = []
```

**Delete/Backspace 特殊处理：**

在 Tkinter 中，`<KeyPress>` 和 `<Delete>` 是不同的事件类型，按下 Delete 键时两个事件会分别触发。需要同时绑定 `<Delete>` 和 `<BackSpace>` 事件，并在这些事件中也返回 `"break"` 来阻止快捷键触发。

**事件触发流程：**

```
用户按下 Delete 键：
┌─────────────────────────────────────────────────────────────────────┐
│  事件1: <KeyPress>                                                   │
│      └── KeyField.on_key_press() 被调用                              │
│          ├── 捕获按键 "delete"                                        │
│          └── 返回 "break" ← 阻止 <KeyPress> 事件传播                  │
│                                                                      │
│  事件2: <Delete>（独立事件，不受 <KeyPress> 的 "break" 影响）         │
│      └── KeyField.on_special_key() 被调用                            │
│          └── 返回 "break" ← 阻止快捷键触发                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 16 坐标偏移测量工具实现

### 16.1 OffsetField 设计

OffsetField 提供可视化偏移测量功能：

```python
# bt_gui/bt_editor/property.py

class OffsetField(FieldWidget):
    """坐标偏移字段，支持可视化测量"""
    
    def __init__(self, master, label: str, key: str, on_change: Callable, app, **kwargs):
        self.app = app
        super().__init__(master, label, key, on_change, **kwargs)
        self._create_widget()
    
    def _create_widget(self):
        # 创建输入框
        self.entry = ctk.CTkEntry(self.frame, width=100)
        self.entry.pack(side="left")
        
        # 创建测量按钮
        self.measure_btn = ctk.CTkButton(
            self.frame,
            text="测量",
            width=50,
            command=self._measure_offset
        )
        self.measure_btn.pack(side="left", padx=5)
    
    def _measure_offset(self):
        """可视化偏移测量"""
        magnifier = MagnifierWindow(zoom_factor=4, size=150)
        magnifier_shown = [False]
        reference_point = [None]
        
        def on_mouse_move(event):
            if not magnifier_shown[0]:
                magnifier.show(event.x_root, event.y_root)
                magnifier_shown[0] = True
            else:
                magnifier.update(event.x_root, event.y_root)
        
        def on_click(event):
            if reference_point[0] is None:
                # 第一步：记录参考点
                reference_point[0] = (event.x_root, event.y_root)
            else:
                # 第二步：计算偏移
                target_point = (event.x_root, event.y_root)
                offset_x = target_point[0] - reference_point[0][0]
                offset_y = target_point[1] - reference_point[0][1]
                
                # 更新值
                self.entry.delete(0, "end")
                self.entry.insert(0, f"{offset_x}, {offset_y}")
                
                # 清理
                magnifier.hide()
                self._unbind_events()
        
        # 绑定事件
        self._bind_events(on_mouse_move, on_click)
```

### 16.2 测量流程

```
┌─────────────────────────────────────────────────────────────┐
│                     偏移测量流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 点击【测量】按钮                                         │
│       ↓                                                     │
│  2. 放大镜显示，点击参考点（检测到的原始位置）                │
│       ↓                                                     │
│  3. 放大镜继续显示，点击目标点（实际需要点击的位置）          │
│       ↓                                                     │
│  4. 自动计算偏移值：(目标X-参考X, 目标Y-参考Y)               │
│       ↓                                                     │
│  5. 偏移值填入输入框                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 17 执行统计与性能分析

### 17.1 统计模块设计

执行统计功能仅在 Debug 环境启用，Release 环境使用空操作实现以避免性能影响：

```python
# bt_utils/stats.py

@dataclass
class NodeStatistics:
    node_id: str
    node_type: str
    node_name: str
    total_executions: int = 0
    success_count: int = 0
    failure_count: int = 0
    total_time_ms: float = 0.0
    max_time_ms: float = 0.0
    min_time_ms: float = float('inf')
    
    @property
    def success_rate(self) -> float:
        if self.total_executions == 0:
            return 0.0
        return self.success_count / self.total_executions

class StatsCollector:
    """完整统计收集器（Debug 环境）"""
    DEFAULT_CACHE_TTL = 1.0
    MAX_CACHE_SIZE = 50

class NoOpStatsCollector:
    """空操作收集器（Release 环境，零开销）"""
    def record_node(self, *args, **kwargs): pass
    def get_report(self): return {}

def is_debug_mode() -> bool:
    """检查是否为 debug 模式"""
    import sys
    if not getattr(sys, 'frozen', False):
        return True  # 开发环境
    from bt_utils.version_checker import load_build_info
    return load_build_info().get('debug', {}).get('enable_debug_mode', False)

def get_stats_collector():
    """根据模式自动选择收集器"""
    if is_debug_mode():
        return StatsCollector()
    return NoOpStatsCollector()
```

### 17.2 统计报告示例

```
📊 执行统计报告
  执行时长: 5230ms
  Tick次数: 523
  节点执行总数: 1569
  成功率: 85.2%
  耗时最多的节点:
    1. OCR检测 (OCRConditionNode): 2340ms, 156次
    2. 图像匹配 (ImageConditionNode): 1200ms, 89次
  执行最多的节点:
    1. 条件判断 (ConditionNode): 234次, 成功率92.3%
```

### 17.3 条件加载机制

| 功能 | Debug 环境 | Release 环境 |
|------|-----------|-------------|
| 执行统计 | `StatsCollector`（完整统计） | `NoOpStatsCollector`（零开销） |
| 终端日志 | 启用 | 禁用 |

---

## 18 日志管理器实现

### 18.1 日志类型分离

日志分为两类，互不影响：

| 类型 | 方法 | 存储位置 | Debug | Release |
|------|------|---------|-------|---------|
| 前端日志 | `log_success()`, `log_failure()`, `log_info()` | `_buffer` → 前端面板 | ✅ 显示 | ✅ 显示 |
| 终端日志 | `debug_print()` | 仅终端 | ✅ 输出 | ❌ 不输出 |

### 18.2 终端日志条件加载

```python
# bt_utils/log_manager.py

def _is_console_output_enabled() -> bool:
    """检查是否启用终端输出"""
    import sys
    if not getattr(sys, 'frozen', False):
        return True  # 开发环境默认启用
    
    from bt_utils.version_checker import load_build_info
    return load_build_info().get('debug', {}).get('enable_debug_mode', False)

class LogManager:
    @classmethod
    def debug_print(cls, message: str) -> None:
        """终端调试输出（仅 Debug 环境）
        
        独立于前端日志，仅输出到终端，不添加到 buffer。
        """
        if not cls._console_output_enabled:
            return
        print(message)
    
    def log(self, entry: LogEntry) -> None:
        """记录日志（仅前端显示，不输出到终端）"""
        with self._buffer_lock:
            self._buffer.append(entry)
        # 触发前端更新
        ...
```

### 18.3 使用示例

```python
# 前端日志（始终显示）
LogManager().log_success(node_type="OCRConditionNode", node_name="文字检测")

# 终端日志（仅 Debug 环境）
LogManager.debug_print("📊 执行统计报告")
```

---

## 19 安全机制实现

### 19.1 Zip Slip 漏洞防护

```python
# bt_utils/package_importer.py

def validate_package(self, zip_path):
    with zipfile.ZipFile(zip_path, 'r') as zipf:
        for name in zipf.namelist():
            normalized = os.path.normpath(name)
            if normalized.startswith('..') or os.path.isabs(normalized):
                return False, "ZIP 包含路径遍历条目，可能是恶意文件"
    return True, ""
```

### 19.2 路径遍历防护

```python
# bt_utils/path_resolver.py

def to_absolute(self, relative_path: str) -> str:
    abs_path = os.path.normpath(os.path.join(self.project_root, relative_path))
    normalized_root = os.path.normpath(self.project_root)
    if not (abs_path.startswith(normalized_root + os.sep) or abs_path == normalized_root):
        raise ValueError(f"路径逃逸: '{relative_path}' 解析后超出项目根目录")
    return abs_path
```

### 19.3 CodeNode 安全沙箱

```python
# bt_nodes/actions/code.py

class CodeSecurityChecker:
    ALLOWED_NODES = {
        ast.Module, ast.Expr, ast.Assign, ast.AugAssign,
        ast.If, ast.While, ast.For, ast.BinOp, ast.UnaryOp,
        ast.Compare, ast.Call, ast.Name, ast.Constant, ...
    }
    FORBIDDEN_NAMES = {
        'import', '__import__', 'eval', 'exec', 'open',
        'os', 'sys', 'subprocess', 'socket', ...
    }
    
    @classmethod
    def check(cls, code: str) -> Tuple[bool, str]:
        try:
            tree = ast.parse(code)
            for node in ast.walk(tree):
                if type(node) not in cls.ALLOWED_NODES:
                    return False, f"禁止的语法: {type(node).__name__}"
                if isinstance(node, ast.Name) and node.id in cls.FORBIDDEN_NAMES:
                    return False, f"禁止的名称: {node.id}"
            return True, ""
        except SyntaxError as e:
            return False, f"语法错误: {e}"
```

---

## 20 窗口绑定功能实现

### 20.1 WindowManager 窗口管理器

WindowManager 提供窗口枚举、查找、切换等功能，使用 ctypes 调用 Windows API：

```python
# bt_utils/window_manager.py

import ctypes
from ctypes import wintypes
from typing import List, Tuple, Optional

class WindowManager:
    """窗口管理器，提供窗口枚举、查找、切换等功能"""
    
    @staticmethod
    def enum_all_windows() -> List[Tuple[int, str]]:
        """枚举所有可见窗口
        
        Returns:
            List[Tuple[int, str]]: 窗口句柄和标题的列表
        """
        user32 = ctypes.windll.user32
        
        windows = []
        
        def enum_windows_proc(hwnd, lparam):
            if user32.IsWindowVisible(hwnd):
                length = user32.GetWindowTextLengthW(hwnd)
                if length > 0:
                    buffer = ctypes.create_unicode_buffer(length + 1)
                    user32.GetWindowTextW(hwnd, buffer, length + 1)
                    title = buffer.value
                    if title:
                        windows.append((hwnd, title))
            return True
        
        WNDENUMPROC = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
        user32.EnumWindows(WNDENUMPROC(enum_windows_proc), 0)
        
        return windows
    
    @staticmethod
    def find_window_by_title(keyword: str) -> Optional[int]:
        """根据标题关键字查找窗口
        
        Args:
            keyword: 窗口标题关键字（不区分大小写）
            
        Returns:
            Optional[int]: 窗口句柄，未找到返回 None
        """
        windows = WindowManager.enum_all_windows()
        keyword_lower = keyword.lower()
        
        for hwnd, title in windows:
            if keyword_lower in title.lower():
                return hwnd
        
        return None
    
    @staticmethod
    def get_window_rect(hwnd: int) -> Optional[Tuple[int, int, int, int]]:
        """获取窗口矩形区域
        
        Args:
            hwnd: 窗口句柄
            
        Returns:
            Optional[Tuple[int, int, int, int]]: (left, top, right, bottom)
        """
        user32 = ctypes.windll.user32
        rect = wintypes.RECT()
        
        if user32.GetWindowRect(hwnd, ctypes.byref(rect)):
            return (rect.left, rect.top, rect.right, rect.bottom)
        
        return None
    
    @staticmethod
    def switch_to_window(hwnd: int) -> bool:
        """切换到指定窗口（置顶并激活）
        
        Args:
            hwnd: 窗口句柄
            
        Returns:
            bool: 是否成功
        """
        user32 = ctypes.windll.user32
        
        # 先恢复窗口（如果最小化）
        SW_RESTORE = 9
        user32.ShowWindow(hwnd, SW_RESTORE)
        
        # 设置窗口位置和大小
        SWP_NOMOVE = 0x0002
        SWP_NOSIZE = 0x0001
        SWP_SHOWWINDOW = 0x0040
        user32.SetWindowPos(hwnd, -1, 0, 0, 0, 0, SWP_NOMOVE | SWP_NOSIZE | SWP_SHOWWINDOW)
        
        # 设置前台窗口
        return user32.SetForegroundWindow(hwnd) != 0
    
    @staticmethod
    def restore_window(hwnd: int) -> bool:
        """恢复窗口（取消置顶）
        
        Args:
            hwnd: 窗口句柄
            
        Returns:
            bool: 是否成功
        """
        user32 = ctypes.windll.user32
        
        SWP_NOMOVE = 0x0002
        SWP_NOSIZE = 0x0001
        SWP_SHOWWINDOW = 0x0040
        SWP_NOZORDER = 0x0004
        
        return user32.SetWindowPos(hwnd, 0, 0, 0, 0, 0, 
                                   SWP_NOMOVE | SWP_NOSIZE | SWP_SHOWWINDOW | SWP_NOZORDER) != 0
    
    @staticmethod
    def get_foreground_window() -> Optional[int]:
        """获取当前前台窗口句柄"""
        user32 = ctypes.windll.user32
        return user32.GetForegroundWindow()
    
    @staticmethod
    def is_foreground_window(hwnd: int) -> bool:
        """检查指定窗口是否为前台窗口"""
        return WindowManager.get_foreground_window() == hwnd
    
    @staticmethod
    def find_window_by_pid(pid: int) -> Optional[int]:
        """通过进程ID查找窗口
        
        Args:
            pid: 进程ID
            
        Returns:
            Optional[int]: 窗口句柄，未找到返回 None
        """
        results = []
        
        WNDENUMPROC = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
        
        def enum_windows_callback(hwnd, _):
            if user32.IsWindowVisible(hwnd):
                window_pid = wintypes.DWORD()
                user32.GetWindowThreadProcessId(hwnd, ctypes.byref(window_pid))
                if window_pid.value == pid:
                    length = user32.GetWindowTextLengthW(hwnd)
                    if length > 0:
                        buffer = ctypes.create_unicode_buffer(length + 1)
                        user32.GetWindowTextW(hwnd, buffer, length + 1)
                        if buffer.value:
                            results.append(hwnd)
            return True
        
        user32.EnumWindows(WNDENUMPROC(enum_windows_callback), 0)
        
        if results:
            return results[0]
        return None
    
    @staticmethod
    def get_window_pid(hwnd: int) -> Optional[int]:
        """获取窗口的进程ID
        
        Args:
            hwnd: 窗口句柄
            
        Returns:
            Optional[int]: 进程ID，获取失败返回 None
        """
        try:
            pid = wintypes.DWORD()
            user32.GetWindowThreadProcessId(hwnd, ctypes.byref(pid))
            return pid.value if pid.value else None
        except Exception:
            return None
    
    @staticmethod
    def find_window_smart(pid: Optional[int], title_keyword: str) -> Tuple[Optional[int], str]:
        """智能查找窗口：优先PID，其次关键字
        
        Args:
            pid: 进程ID（可选）
            title_keyword: 窗口标题关键字
            
        Returns:
            Tuple[Optional[int], str]: (窗口句柄, 查找方式说明)
            查找方式说明: "pid" / "title" / "not_found"
        """
        # 优先通过PID查找
        if pid:
            hwnd = WindowManager.find_window_by_pid(pid)
            if hwnd:
                return hwnd, "pid"
        
        # PID查找失败，通过标题关键字查找
        if title_keyword:
            hwnd = WindowManager.find_window_by_title(title_keyword)
            if hwnd:
                return hwnd, "title"
        
        return None, "not_found"
```

### 20.2 CoordinateConverter 坐标转换扩展

CoordinateConverter 扩展了区域坐标转换功能：

```python
# bt_utils/coordinate.py (扩展)

class CoordinateConverter:
    # ... 原有方法 ...
    
    @staticmethod
    def screen_region_to_window(screen_region: tuple, hwnd: int) -> tuple:
        """将屏幕绝对区域转换为窗口相对区域
        
        Args:
            screen_region: 屏幕绝对坐标区域 (x1, y1, x2, y2)
            hwnd: 窗口句柄
            
        Returns:
            tuple: 窗口相对坐标区域 (x1, y1, x2, y2)
        """
        rect = CoordinateConverter.get_window_rect(hwnd)
        if not rect:
            return (0, 0, 0, 0)
        
        return (
            screen_region[0] - rect[0],
            screen_region[1] - rect[1],
            screen_region[2] - rect[0],
            screen_region[3] - rect[1]
        )
    
    @staticmethod
    def window_region_to_screen(window_region: tuple, hwnd: int) -> tuple:
        """将窗口相对区域转换为屏幕绝对区域
        
        Args:
            window_region: 窗口相对坐标区域 (x1, y1, x2, y2)
            hwnd: 窗口句柄
            
        Returns:
            tuple: 屏幕绝对坐标区域 (x1, y1, x2, y2)
        """
        rect = CoordinateConverter.get_window_rect(hwnd)
        if not rect:
            return (0, 0, 0, 0)
        
        return (
            window_region[0] + rect[0],
            window_region[1] + rect[1],
            window_region[2] + rect[0],
            window_region[3] + rect[1]
        )
```

### 20.3 WindowCapture 后台截图扩展

WindowCapture 扩展了区域截图功能：

```python
# bt_utils/window_capture.py (扩展)

class WindowCapture:
    # ... 原有方法 ...
    
    @staticmethod
    def capture_window_region(hwnd: int, region: Tuple[int, int, int, int]) -> Optional[Image.Image]:
        """截取窗口指定区域
        
        Args:
            hwnd: 窗口句柄
            region: 窗口相对区域 (x1, y1, x2, y2)
            
        Returns:
            Optional[Image.Image]: 截图图像
        """
        full_image = WindowCapture.capture_window(hwnd)
        if full_image is None:
            return None
        
        try:
            return full_image.crop((region[0], region[1], region[2], region[3]))
        except Exception:
            return None
```

### 20.4 ExecutionContext 窗口绑定扩展

ExecutionContext 扩展了窗口绑定功能：

```python
# bt_core/context.py (扩展)

class ExecutionContext:
    def __init__(self, ...):
        # ... 原有初始化 ...
        self._bound_window: Optional[int] = None
        self._saved_foreground_window: Optional[int] = None
    
    def bind_window(self, hwnd: int) -> None:
        """绑定目标窗口"""
        self._bound_window = hwnd
    
    def get_bound_window(self) -> Optional[int]:
        """获取绑定的窗口句柄"""
        return self._bound_window
    
    def get_screenshot(self, region: tuple = None) -> Optional[Image.Image]:
        """截图（支持后台截图）"""
        if self._bound_window:
            from bt_utils.window_capture import WindowCapture
            from bt_utils.coordinate import CoordinateConverter
            
            if region:
                window_region = CoordinateConverter.screen_region_to_window(
                    region, self._bound_window
                )
                return WindowCapture.capture_window_region(self._bound_window, window_region)
            else:
                return WindowCapture.capture_window(self._bound_window)
        else:
            # 原有截图逻辑
            ...
    
    def convert_to_screen_coords(self, region: tuple) -> tuple:
        """将窗口相对坐标转换为屏幕绝对坐标"""
        if self._bound_window:
            from bt_utils.coordinate import CoordinateConverter
            return CoordinateConverter.window_region_to_screen(region, self._bound_window)
        return region
    
    def convert_to_window_coords(self, region: tuple) -> tuple:
        """将屏幕绝对坐标转换为窗口相对坐标"""
        if self._bound_window:
            from bt_utils.coordinate import CoordinateConverter
            return CoordinateConverter.screen_region_to_window(region, self._bound_window)
        return region
    
    def smart_switch_to_bound_window(self) -> bool:
        """智能切换到绑定窗口"""
        if not self._bound_window:
            return False
        
        from bt_utils.window_manager import WindowManager
        
        if WindowManager.is_foreground_window(self._bound_window):
            return True
        
        self._saved_foreground_window = WindowManager.get_foreground_window()
        return WindowManager.switch_to_window(self._bound_window)
    
    def smart_restore_foreground_window(self) -> bool:
        """智能恢复前台窗口"""
        if not self._bound_window or not self._saved_foreground_window:
            return False
        
        from bt_utils.window_manager import WindowManager
        
        if self._saved_foreground_window != self._bound_window:
            return WindowManager.switch_to_window(self._saved_foreground_window)
        
        return True
```

### 20.5 StartNode 窗口绑定实现

StartNode 在执行时绑定窗口，支持PID优先查找：

```python
# bt_core/nodes.py (StartNode 扩展)

class StartNode(CompositeNode):
    def __init__(self, node_id=None, config=None):
        super().__init__(node_id, config)
        self.bind_window = self.config.get_bool("bind_window", False)
        self.window_title = self.config.get("window_title", "")
        self.window_pid = self.config.get_int("window_pid", 0)
    
    def _tick_internal(self, context: ExecutionContext) -> NodeStatus:
        # 窗口绑定
        if self.bind_window and self.window_title:
            self._bind_window_to_context(context)
        
        # 原有执行逻辑
        ...
    
    def _bind_window_to_context(self, context: ExecutionContext) -> None:
        """绑定窗口到执行上下文（PID优先查找）"""
        from bt_utils.window_manager import WindowManager
        from bt_utils.log_manager import LogManager
        
        # 智能查找：优先PID，其次标题
        hwnd, find_method = WindowManager.find_window_smart(
            pid=self.window_pid if self.window_pid > 0 else None,
            title_keyword=self.window_title
        )
        
        if hwnd:
            context.bind_window(hwnd)
            rect = WindowManager.get_window_rect(hwnd)
            title = WindowManager.get_window_title(hwnd)
            actual_pid = WindowManager.get_window_pid(hwnd)
            
            if find_method == "pid":
                print(f"[DEBUG] StartNode 通过PID绑定窗口: pid={self.window_pid}, hwnd={hwnd}, title='{title}', rect={rect}")
            else:
                print(f"[DEBUG] StartNode 通过标题绑定窗口: title='{self.window_title}', hwnd={hwnd}, actual_title='{title}', pid={actual_pid}, rect={rect}")
                if actual_pid and actual_pid != self.window_pid:
                    print(f"[DEBUG] StartNode 提示: 窗口PID已变更 ({self.window_pid} -> {actual_pid})，建议重新选择窗口")
        else:
            LogManager.instance().log_failure(
                node_type="开始节点",
                node_name=self.name,
                reason=f"未找到窗口: pid={self.window_pid}, title='{self.window_title}'"
            )
            print(f"[DEBUG] StartNode 未找到窗口: pid={self.window_pid}, title='{self.window_title}'")
```

### 20.6 ActionNode 智能窗口切换

ActionNode 在执行动作前自动切换窗口，支持跳过不需要窗口操作的节点：

```python
# bt_core/nodes.py (ActionNode 扩展)

class ActionNode(Node):
    NODE_TYPE = "ActionNode"
    SKIP_WINDOW_SWITCH = False  # 基类默认不跳过窗口切换

    def __init__(self, node_id: str = None, config: NodeConfig = None):
        super().__init__(node_id, config)
        self._window_switched = False

    def _tick_internal(self, context: ExecutionContext) -> NodeStatus:
        bound_window = context.get_bound_window()

        if bound_window and not self.SKIP_WINDOW_SWITCH:
            if not self._window_switched:
                print(f"[DEBUG] ActionNode '{self.name}' 检测到绑定窗口: hwnd={bound_window}")
                context.smart_switch_to_bound_window()
                self._window_switched = True
            
            print(f"[DEBUG] ActionNode '{self.name}' 执行动作...")
            status = self._execute_action(context)
            
            if status != NodeStatus.RUNNING:
                context.smart_restore_foreground_window()
                self._window_switched = False
            
            self.status = status
            # ... 子节点执行逻辑 ...
        else:
            status = self._execute_action(context)
            # ... 子节点执行逻辑 ...

    def reset(self, reset_counters: bool = True) -> None:
        super().reset(reset_counters)
        self._window_switched = False
```

**跳过窗口切换的节点：**

```python
# bt_nodes/actions/delay.py
class DelayNode(ActionNode):
    NODE_TYPE = "DelayNode"
    SKIP_WINDOW_SWITCH = True  # 延时节点不需要窗口操作

# bt_nodes/actions/variable.py
class SetVariableNode(ActionNode):
    NODE_TYPE = "SetVariableNode"
    SKIP_WINDOW_SWITCH = True  # 变量操作不需要窗口

# bt_nodes/actions/alarm.py
class AlarmNode(ActionNode):
    NODE_TYPE = "AlarmNode"
    SKIP_WINDOW_SWITCH = True  # 播放声音不需要窗口

# bt_nodes/actions/code.py
class CodeNode(ActionNode):
    NODE_TYPE = "CodeNode"
    SKIP_WINDOW_SWITCH = True  # 执行外部代码不需要窗口

# bt_nodes/actions/script.py
class ScriptNode(ActionNode):
    NODE_TYPE = "ScriptNode"
    SKIP_WINDOW_SWITCH = True  # 脚本执行不需要窗口
```

### 20.7 WindowSelectField GUI 组件

WindowSelectField 提供窗口选择功能，选择窗口时自动保存PID：

```python
# bt_gui/bt_editor/property.py

class WindowSelectField(FieldWidget):
    """窗口选择字段"""
    
    def __init__(self, master, label: str, key: str, on_change: Callable, app, **kwargs):
        self._app = app
        self._window_titles = []
        self._window_hwnds = {}
        self._window_pids = {}  # 新增：保存窗口PID
        super().__init__(master, label, key, on_change, **kwargs)
    
    def _create_widget(self):
        # 下拉选择框
        self.combo = ctk.CTkComboBox(
            self.frame,
            values=[""],
            width=150,
            command=self._on_window_selected
        )
        self.combo.pack(side="left", fill="x", expand=True)
        
        # 清空按钮
        self.clear_btn = ctk.CTkButton(
            self.frame,
            text="清空",
            width=50,
            command=self._clear_selection
        )
        self.clear_btn.pack(side="left", padx=5)
        
        # 点击下拉框时自动刷新窗口列表
        self.combo.bind("<Button-1>", lambda e: self._refresh_window_list())
        
        # 初始化窗口列表
        self._refresh_window_list()
    
    def _on_window_selected(self, choice: str):
        """窗口选择回调"""
        self.on_change(self.key, choice)
        # 自动保存窗口PID到隐藏字段
        if choice in self._window_pids:
            pid = self._window_pids[choice]
            if pid:
                self.on_change("window_pid", pid)
                print(f"[DEBUG] WindowSelectField: 选择窗口 '{choice}', PID={pid}")
    
    def _clear_selection(self):
        """清空窗口选择"""
        self.var.set("")
        self.on_change(self.key, "")
        self.on_change("window_pid", 0)
        print(f"[DEBUG] WindowSelectField: 清空窗口选择")
    
    def _refresh_window_list(self):
        """刷新窗口列表"""
        from bt_utils.window_manager import WindowManager
        
        windows = WindowManager.enum_all_windows()
        self._window_titles = [title for hwnd, title in windows]
        self._window_hwnds = {title: hwnd for hwnd, title in windows}
        self._window_pids = {}
        for hwnd, title in windows:
            pid = WindowManager.get_window_pid(hwnd)
            if pid:
                self._window_pids[title] = pid
        
        self.combo.configure(values=self._window_titles)
    
    def get_value(self) -> str:
        return self.combo.get()
    
    def set_value(self, value: str):
        self.combo.set(value)
```

### 20.8 RegionField 坐标转换集成

RegionField 在选择区域时自动进行坐标转换：

```python
# bt_gui/bt_editor/property.py (RegionField 扩展)

class RegionField(FieldWidget):
    def _get_bound_window(self) -> Optional[int]:
        """获取绑定的窗口句柄"""
        editor = self._app.behavior_tree
        if not editor:
            return None
        
        start_node = editor.get_start_node()
        if start_node and hasattr(start_node, 'bind_window') and start_node.bind_window:
            from bt_utils.window_manager import WindowManager
            return WindowManager.find_window_by_title(start_node.window_title)
        
        return None
    
    def _on_mouse_up(self, event):
        """鼠标释放事件处理"""
        # ... 计算区域 ...
        
        bound_window = self._get_bound_window()
        if bound_window:
            from bt_utils.coordinate import CoordinateConverter
            original_region = region
            region = CoordinateConverter.screen_region_to_window(region, bound_window)
            print(f"[DEBUG] RegionField: 坐标转换 屏幕绝对{original_region} -> 窗口相对{region}, hwnd={bound_window}")
        
        self.entry.delete(0, "end")
        self.entry.insert(0, f"{region[0]}, {region[1]}, {region[2]}, {region[3]}")
        self._on_change(self._key, self.entry.get())
```

### 20.9 执行流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           窗口绑定执行流程                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. StartNode 初始化                                                        │
│     ├── 读取 bind_window = True                                            │
│     ├── 读取 window_title = "记事本"                                        │
│     └── WindowManager.find_window_by_title("记事本") → hwnd=12345          │
│                                                                             │
│  2. 窗口绑定                                                                 │
│     └── context.bind_window(12345)                                         │
│                                                                             │
│  3. 条件节点执行（OCR检测）                                                   │
│     ├── context.get_screenshot(region)                                     │
│     │   └── 检测到绑定窗口 → WindowCapture.capture_window_region()         │
│     ├── 坐标转换：窗口相对 → 屏幕绝对                                         │
│     └── 执行 OCR 检测                                                        │
│                                                                             │
│  4. 动作节点执行（鼠标点击）                                                   │
│     ├── context.smart_switch_to_bound_window()                             │
│     │   └── 保存前台窗口 → 切换到绑定窗口                                     │
│     ├── 执行点击动作                                                         │
│     └── context.smart_restore_foreground_window()                          │
│         └── 恢复原前台窗口                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```
