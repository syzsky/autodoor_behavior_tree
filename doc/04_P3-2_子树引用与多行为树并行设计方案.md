# P3-2 子树引用与多行为树并行 — 设计方案

> 文档生成时间：2026-05-02
> 基于 P3 优化方案深度设计

---

## 1 概述

### 1.1 设计目标

本方案旨在为 AutoDoor 行为树系统实现两大核心能力：

1. **子树引用机制**：允许行为树引用其他行为树文件作为子树，实现行为树的模块化复用
2. **多行为树并行**：支持多个独立行为树实例同时运行，由统一管理器协调

### 1.2 设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 并行场景 | 场景C（两者都需要） | 最大灵活性，覆盖所有使用场景 |
| 黑板策略 | 混合模式 | 支持继承/隔离/命名空间三种模式，按需配置 |
| 循环引用处理 | 加载时检测+拒绝执行 | 安全可靠，避免运行时问题 |
| GUI呈现 | 可切换模式 | 默认折叠，双击展开只读预览 |

### 1.3 影响范围

| 文件 | 改动类型 | 改动量 |
|------|----------|--------|
| `bt_core/nodes.py` | 新增 SubtreeNode (~120行) | 中 |
| `bt_core/engine.py` | 方法签名调整 | 小 |
| `bt_core/serializer.py` | 子树引用格式支持 | 中 |
| `bt_core/blackboard.py` | 新增 NamespacedBlackboard (~50行) | 小 |
| **新增** `bt_core/cycle_detector.py` | CycleDetector (~80行) | 中 |
| **新增** `bt_core/tree_manager.py` | MultiTreeManager (~200行) | 大 |
| `bt_core/registry.py` | 注册 SubtreeNode | 小 |
| `bt_core/config.py` | 子树配置字段 | 小 |
| `bt_gui/bt_editor/canvas.py` | SubtreeNodeItem 渲染+展开/折叠 | 中 |
| `bt_gui/bt_editor/property.py` | 子树属性面板 | 中 |
| `bt_gui/bt_editor/palette.py` | 子树节点添加到面板 | 小 |
| `bt_gui/bt_editor/undo_redo.py` | 子树相关Command | 小 |
| `bt_gui/bt_editor/constants.py` | SubtreeNode 类型常量 | 小 |

---

## 2 架构设计

### 2.1 新增核心组件

| 组件 | 文件 | 职责 |
|------|------|------|
| **SubtreeNode** | `bt_core/nodes.py` | 子树引用节点，继承CompositeNode，运行时加载外部树并代理tick |
| **CycleDetector** | `bt_core/cycle_detector.py` | DFS检测引用链中的循环，加载时拒绝执行 |
| **MultiTreeManager** | `bt_core/tree_manager.py` | 管理多个独立行为树实例的并行运行 |
| **NamespacedBlackboard** | `bt_core/blackboard.py` | 命名空间隔离的黑板代理 |

### 2.2 组件关系图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           应用层 (Application)                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      MultiTreeManager                            │   │
│  │  ├── trees: Dict[str, TreeInstance]                             │   │
│  │  ├── shared_blackboard: Optional[Blackboard]                    │   │
│  │  ├── start_all() / stop_all() / pause_tree() / resume_tree()   │   │
│  │  └── get_all_status() -> Dict[str, TreeStatus]                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│                            领域层 (Domain)                               │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────────────┐   │
│  │   SubtreeNode  │  │ CycleDetector  │  │  NamespacedBlackboard  │   │
│  │  (CompositeNode)│  │  (静态检测)     │  │  (Blackboard代理)       │   │
│  │                │  │                │  │                        │   │
│  │ - subtree_path │  │ - check(refs)  │  │ - parent: Blackboard   │   │
│  │ - blackboard_  │  │ - detect_cycle │  │ - namespace: str       │   │
│  │   mode         │  │ - build_dag    │  │ - get/set (自动前缀)    │   │
│  │ - _subtree_ctx │  │                │  │                        │   │
│  └────────────────┘  └────────────────┘  └────────────────────────┘   │
│           │                  │                      │                   │
│           ▼                  ▼                      ▼                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                     ExecutionContext                             │   │
│  │  - blackboard: Blackboard                                        │   │
│  │  - parent_context: Optional[ExecutionContext]  (子树引用时)       │   │
│  │  - subtree_stack: List[str]  (引用链追踪)                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│                         基础设施层 (Infrastructure)                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                        Serializer (扩展)                          │   │
│  │  - serialize_with_subtrees() -> 包含子树引用元数据                 │   │
│  │  - resolve_subtree_paths() -> 解析所有子树路径                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3 场景A：子树内嵌引用

### 3.1 执行流程

```
┌─────────────────────────────────────────────────────────────────┐
│  主行为树 (main.json)                                            │
│  ┌──────────────────────────────────────────────────────┐       │
│  │ StartNode                                             │       │
│  │  ├── SequenceNode                                     │       │
│  │  │   ├── OCRConditionNode                             │       │
│  │  │   ├── SubtreeNode ─────► login.json (子树引用)     │       │
│  │  │   └── MouseClickNode                               │       │
│  │  └── ParallelNode                                     │       │
│  │      ├── SubtreeNode ─────► monitor_a.json            │       │
│  │      └── SubtreeNode ─────► monitor_b.json            │       │
│  └──────────────────────────────────────────────────────┘       │
│                                                                  │
│  SubtreeNode 内部流程:                                           │
│  1. 首次 tick → 解析 subtree_path → 加载树文件                    │
│  2. CycleDetector 检测引用链 (A→B→A? → 拒绝)                     │
│  3. 根据 blackboard_mode 创建 Blackboard                         │
│     - inherit: 直接使用父 context.blackboard                     │
│     - isolated: 新建独立 Blackboard                              │
│     - namespaced: NamespacedBlackboard(parent, "login")          │
│  4. 代理 tick 到子树根节点                                       │
│  5. 子树完成后返回 SUCCESS/FAILURE                                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 SubtreeNode 类设计

```python
class SubtreeNode(CompositeNode):
    """子树引用节点
    
    配置参数:
        subtree_path: 子树文件路径（相对于项目根目录）
        blackboard_mode: "inherit" | "isolated" | "namespaced"
        namespace: 命名空间前缀（namespaced模式使用）
        auto_reload: 每次执行前重新加载子树（默认False，缓存加载）
    """
    NODE_TYPE = "SubtreeNode"
    
    def __init__(self, node_id: str = None, config: NodeConfig = None):
        super().__init__(node_id, config)
        self.subtree_path = self.config.get("subtree_path", "")
        self.blackboard_mode = self.config.get("blackboard_mode", "inherit")
        self.namespace = self.config.get("namespace", "")
        self.auto_reload = self.config.get_bool("auto_reload", False)
        
        # 运行时状态
        self._subtree_root: Optional[Node] = None
        self._subtree_context: Optional[ExecutionContext] = None
        self._loaded_path: Optional[str] = None
    
    def tick(self, context: ExecutionContext) -> NodeStatus:
        return self._execute_with_decorators(context, self._tick_internal)
    
    def _tick_internal(self, context: ExecutionContext) -> NodeStatus:
        # 1. 确保子树已加载
        if not self._ensure_loaded(context):
            return NodeStatus.FAILURE
        
        # 2. 代理到子树根节点
        status = self._subtree_root.tick(self._subtree_context)
        
        # 3. 子树完成后处理
        if status != NodeStatus.RUNNING:
            self._on_subtree_complete(status)
        
        return status
    
    def _ensure_loaded(self, context: ExecutionContext) -> bool:
        """确保子树已加载，返回是否成功"""
        # 检查是否需要重新加载
        if self._subtree_root and not self.auto_reload:
            if self._loaded_path == self._resolve_path(context):
                return True
        
        # 解析路径
        resolved_path = self._resolve_path(context)
        if not resolved_path:
            LogManager.instance().log_failure(
                node_type="子树节点",
                node_name=self.name,
                reason=f"无法解析子树路径: {self.subtree_path}"
            )
            return False
        
        # 循环引用检测
        if not CycleDetector.check(context, resolved_path):
            LogManager.instance().log_failure(
                node_type="子树节点",
                node_name=self.name,
                reason=f"检测到循环引用: {resolved_path}"
            )
            return False
        
        # 加载子树
        try:
            self._subtree_root, _, _ = Serializer.load_from_file(resolved_path)
            self._loaded_path = resolved_path
        except Exception as e:
            LogManager.instance().log_failure(
                node_type="子树节点",
                node_name=self.name,
                reason=f"加载子树失败: {e}"
            )
            return False
        
        # 创建子树上下文
        self._subtree_context = self._create_subtree_context(context)
        
        return True
    
    def _create_subtree_context(self, parent_context: ExecutionContext) -> ExecutionContext:
        """根据 blackboard_mode 创建子树上下文"""
        if self.blackboard_mode == "inherit":
            # 直接继承父上下文的黑板
            return parent_context
        
        elif self.blackboard_mode == "isolated":
            # 创建完全独立的上下文
            sub_context = ExecutionContext(parent_context.project_root)
            return sub_context
        
        elif self.blackboard_mode == "namespaced":
            # 创建命名空间代理黑板
            sub_context = ExecutionContext(parent_context.project_root)
            namespace = self.namespace or self.name or self.node_id
            sub_context.blackboard = NamespacedBlackboard(
                parent_context.blackboard, 
                namespace
            )
            sub_context._parent_context = parent_context
            return sub_context
        
        return parent_context
    
    def _resolve_path(self, context: ExecutionContext) -> Optional[str]:
        """解析子树路径（相对路径→绝对路径）"""
        if not self.subtree_path:
            return None
        
        if os.path.isabs(self.subtree_path):
            return self.subtree_path
        
        # 相对于项目根目录
        return os.path.join(context.project_root, self.subtree_path)
    
    def reset(self, reset_counters: bool = True) -> None:
        super().reset(reset_counters)
        if self._subtree_root:
            self._subtree_root.reset(reset_counters)
    
    def abort(self, context: ExecutionContext) -> None:
        if self._subtree_root and self._subtree_context:
            self._subtree_root.abort(self._subtree_context)
        super().abort(context)
```

### 3.3 NamespacedBlackboard 类设计

```python
class NamespacedBlackboard:
    """命名空间黑板代理
    
    自动为所有键添加命名空间前缀，实现子树黑板隔离。
    对使用者透明，get("x") 实际读写 parent["namespace.x"]
    """
    
    def __init__(self, parent: Blackboard, namespace: str):
        self._parent = parent
        self._namespace = namespace
        self._prefix = f"{namespace}."
    
    def _wrap_key(self, key: str) -> str:
        """添加命名空间前缀"""
        if key.startswith(self._prefix):
            return key
        return f"{self._prefix}{key}"
    
    def get(self, key: str, default: Any = None) -> Any:
        return self._parent.get(self._wrap_key(key), default)
    
    def set(self, key: str, value: Any) -> None:
        self._parent.set(self._wrap_key(key), value)
    
    def increment(self, key: str, amount: int = 1) -> None:
        self._parent.increment(self._wrap_key(key), amount)
    
    def delete(self, key: str) -> None:
        self._parent.delete(self._wrap_key(key))
    
    def exists(self, key: str) -> bool:
        return self._parent.exists(self._wrap_key(key))
    
    def subscribe(self, key: str, callback: Callable) -> None:
        self._parent.subscribe(self._wrap_key(key), callback)
    
    def unsubscribe(self, key: str, callback: Callable) -> None:
        self._parent.unsubscribe(self._wrap_key(key), callback)
    
    def get_all_keys(self) -> List[str]:
        """获取当前命名空间下的所有键（去除前缀）"""
        all_keys = self._parent.get_all_keys()
        return [k[len(self._prefix):] for k in all_keys if k.startswith(self._prefix)]
```

### 3.4 CycleDetector 类设计

```python
class CycleDetector:
    """循环引用检测器
    
    使用 DFS 检测行为树引用链中的循环依赖。
    在子树加载时执行检测，发现循环直接拒绝。
    """
    
    @staticmethod
    def check(context: ExecutionContext, new_path: str) -> bool:
        """检查添加新路径是否会形成循环
        
        Args:
            context: 当前执行上下文（包含引用链）
            new_path: 即将加载的子树路径
            
        Returns:
            True 表示无循环，可以加载；False 表示有循环，拒绝加载
        """
        # 获取当前引用链
        subtree_stack = getattr(context, '_subtree_stack', [])
        
        # 标准化路径进行比较
        normalized_new = os.path.normpath(os.path.abspath(new_path))
        
        for existing_path in subtree_stack:
            normalized_existing = os.path.normpath(os.path.abspath(existing_path))
            if normalized_existing == normalized_new:
                return False
        
        return True
    
    @staticmethod
    def build_reference_graph(root_path: str, project_root: str) -> Dict[str, List[str]]:
        """构建引用关系图（用于可视化调试）
        
        Args:
            root_path: 根行为树路径
            project_root: 项目根目录
            
        Returns:
            {文件路径: [引用的子树路径列表]}
        """
        graph = {}
        visited = set()
        
        def traverse(path: str):
            normalized = os.path.normpath(os.path.abspath(path))
            if normalized in visited:
                return
            visited.add(normalized)
            
            try:
                node, _, _ = Serializer.load_from_file(path)
                subtree_refs = CycleDetector._extract_subtree_refs(node)
                
                resolved_refs = []
                for ref in subtree_refs:
                    if not os.path.isabs(ref):
                        ref = os.path.join(project_root, ref)
                    resolved_refs.append(os.path.normpath(os.path.abspath(ref)))
                
                graph[normalized] = resolved_refs
                
                for ref in resolved_refs:
                    traverse(ref)
                    
            except Exception:
                pass
        
        traverse(root_path)
        return graph
    
    @staticmethod
    def _extract_subtree_refs(node: Node) -> List[str]:
        """从节点树中提取所有子树引用路径"""
        refs = []
        
        if isinstance(node, SubtreeNode) and node.subtree_path:
            refs.append(node.subtree_path)
        
        for child in node.children:
            refs.extend(CycleDetector._extract_subtree_refs(child))
        
        return refs
```

---

## 4 场景B：多行为树并行管理

### 4.1 执行流程

```
┌─────────────────────────────────────────────────────────────────┐
│  MultiTreeManager 多引擎并行                                      │
│                                                                  │
│  MultiTreeManager                                                │
│  ├── shared_blackboard (可选)                                    │
│  ├── trees: {                                                    │
│  │     "main_task":  TreeInstance(engine, context, blackboard)   │
│  │     "health_check": TreeInstance(...)                         │
│  │     "auto_loot": TreeInstance(...)                            │
│  │   }                                                           │
│  ├── start_all()   → 每个 tree 独立线程                          │
│  ├── stop_all()    → 全部停止                                    │
│  ├── pause_tree("auto_loot")                                     │
│  └── get_all_status()                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 TreeInstance 数据类

```python
@dataclass
class TreeInstance:
    """行为树实例"""
    name: str
    engine: BehaviorTreeEngine
    context: ExecutionContext
    blackboard: Blackboard
    status: str = "idle"  # idle, running, paused, completed, error
    error_message: Optional[str] = None
```

### 4.3 MultiTreeManager 类设计

```python
class MultiTreeManager:
    """多行为树管理器
    
    管理多个独立行为树实例的并行运行。
    每个树实例拥有独立的引擎、上下文和黑板。
    可选共享黑板实现树间通信。
    """
    
    def __init__(self, shared_blackboard: bool = False):
        self._trees: Dict[str, TreeInstance] = {}
        self._shared_blackboard = Blackboard() if shared_blackboard else None
        self._lock = threading.Lock()
        self._on_tree_status: Optional[Callable] = None
        self._on_node_status: Optional[Callable] = None
    
    def add_tree(self, name: str, root_node: Node, 
                 blackboard: Blackboard = None,
                 tick_interval: float = 0.01) -> TreeInstance:
        """添加行为树实例
        
        Args:
            name: 实例名称（唯一标识）
            root_node: 根节点
            blackboard: 黑板实例（None则使用共享黑板或新建）
            tick_interval: tick间隔（秒）
            
        Returns:
            TreeInstance 实例
        """
        with self._lock:
            if name in self._trees:
                raise ValueError(f"树实例 '{name}' 已存在")
            
            # 创建黑板
            if blackboard is None:
                blackboard = self._shared_blackboard or Blackboard()
            
            # 创建上下文
            context = ExecutionContext()
            context.blackboard = blackboard
            
            # 创建引擎
            engine = BehaviorTreeEngine(root_node)
            engine._tick_interval = tick_interval
            
            # 设置回调
            engine._on_status_change = lambda status, *args: self._on_engine_status(name, status, *args)
            
            instance = TreeInstance(
                name=name,
                engine=engine,
                context=context,
                blackboard=blackboard
            )
            
            self._trees[name] = instance
            return instance
    
    def add_tree_from_file(self, name: str, filepath: str,
                           blackboard: Blackboard = None,
                           tick_interval: float = 0.01) -> TreeInstance:
        """从文件添加行为树实例"""
        root_node, _, _ = Serializer.load_from_file(filepath)
        return self.add_tree(name, root_node, blackboard, tick_interval)
    
    def remove_tree(self, name: str) -> bool:
        """移除行为树实例"""
        with self._lock:
            if name not in self._trees:
                return False
            
            instance = self._trees[name]
            if instance.status == "running":
                instance.engine.stop()
            
            del self._trees[name]
            return True
    
    def start_tree(self, name: str) -> bool:
        """启动指定行为树"""
        with self._lock:
            instance = self._trees.get(name)
            if not instance:
                return False
            
            if instance.status == "running":
                return True
            
            instance.engine.start(instance.context)
            instance.status = "running"
            return True
    
    def stop_tree(self, name: str) -> bool:
        """停止指定行为树"""
        with self._lock:
            instance = self._trees.get(name)
            if not instance:
                return False
            
            instance.engine.stop()
            instance.status = "stopped"
            return True
    
    def pause_tree(self, name: str) -> bool:
        """暂停指定行为树"""
        with self._lock:
            instance = self._trees.get(name)
            if not instance or instance.status != "running":
                return False
            
            instance.engine.pause()
            instance.status = "paused"
            return True
    
    def resume_tree(self, name: str) -> bool:
        """恢复指定行为树"""
        with self._lock:
            instance = self._trees.get(name)
            if not instance or instance.status != "paused":
                return False
            
            instance.engine.resume()
            instance.status = "running"
            return True
    
    def start_all(self) -> None:
        """启动所有行为树"""
        for name in list(self._trees.keys()):
            self.start_tree(name)
    
    def stop_all(self) -> None:
        """停止所有行为树"""
        for name in list(self._trees.keys()):
            self.stop_tree(name)
    
    def get_tree_status(self, name: str) -> Optional[Dict[str, Any]]:
        """获取指定行为树状态"""
        instance = self._trees.get(name)
        if not instance:
            return None
        
        return {
            "name": name,
            "status": instance.status,
            "error": instance.error_message,
            "engine": instance.engine.get_status() if instance.engine else None
        }
    
    def get_all_status(self) -> Dict[str, Dict[str, Any]]:
        """获取所有行为树状态"""
        return {name: self.get_tree_status(name) for name in self._trees}
    
    def _on_engine_status(self, name: str, status: str, *args) -> None:
        """引擎状态变化回调"""
        instance = self._trees.get(name)
        if instance:
            if status == "completed":
                instance.status = "completed"
            elif status == "stopped":
                instance.status = "stopped"
            
            if self._on_tree_status:
                self._on_tree_status(name, status, *args)
    
    def set_on_tree_status(self, callback: Callable) -> None:
        """设置树状态变化回调"""
        self._on_tree_status = callback
    
    def set_on_node_status(self, callback: Callable) -> None:
        """设置节点状态变化回调（应用到所有树）"""
        self._on_node_status = callback
        for instance in self._trees.values():
            instance.engine._on_node_status = callback
```

---

## 5 序列化格式扩展

### 5.1 JSON 格式 v2.1

```json
{
  "version": "2.1",
  "format_type": "behavior_tree_with_subtrees",
  "metadata": {
    "created_at": "2026-05-02T12:00:00",
    "modified_at": "2026-05-02T12:00:00",
    "app_version": "1.4.0"
  },
  "root_node": "n1",
  "nodes": {
    "n1": {
      "id": "n1",
      "type": "StartNode",
      "config": { "name": "主流程" },
      "children": ["n2"]
    },
    "n2": {
      "id": "n2",
      "type": "SubtreeNode",
      "config": {
        "name": "登录流程",
        "enabled": true,
        "subtree_path": "subtrees/login.json",
        "blackboard_mode": "namespaced",
        "namespace": "login",
        "auto_reload": false
      },
      "children": []
    }
  },
  "connections": [
    { "parent_id": "n1", "child_id": "n2" }
  ],
  "subtree_references": {
    "n2": {
      "path": "subtrees/login.json",
      "resolved_path": "D:/project/subtrees/login.json",
      "node_count": 12,
      "last_modified": "2026-05-02T11:30:00"
    }
  },
  "canvas": {
    "viewport": { "pan_x": 0, "pan_y": 0, "zoom": 1.0 }
  }
}
```

### 5.2 Serializer 扩展

```python
class Serializer:
    # ... 现有方法 ...
    
    @staticmethod
    def serialize_with_subtrees(root_node: Node, 
                                  project_root: str,
                                  metadata: Dict[str, Any] = None,
                                  canvas_state: Dict[str, Any] = None,
                                  editor_state: Dict[str, Any] = None) -> Dict[str, Any]:
        """序列化行为树（包含子树引用元数据）"""
        data = Serializer.serialize(root_node, metadata, canvas_state, editor_state)
        data["version"] = "2.1"
        data["format_type"] = "behavior_tree_with_subtrees"
        
        # 收集子树引用
        subtree_refs = {}
        Serializer._collect_subtree_refs(root_node, subtree_refs, project_root)
        
        if subtree_refs:
            data["subtree_references"] = subtree_refs
        
        return data
    
    @staticmethod
    def _collect_subtree_refs(node: Node, refs: Dict[str, Any], project_root: str) -> None:
        """递归收集子树引用信息"""
        if isinstance(node, SubtreeNode) and node.subtree_path:
            resolved_path = node.subtree_path
            if not os.path.isabs(resolved_path):
                resolved_path = os.path.join(project_root, resolved_path)
            
            refs[node.node_id] = {
                "path": node.subtree_path,
                "resolved_path": os.path.normpath(os.path.abspath(resolved_path)),
                "node_count": Serializer._count_nodes_in_file(resolved_path) if os.path.exists(resolved_path) else 0
            }
        
        for child in node.children:
            Serializer._collect_subtree_refs(child, refs, project_root)
    
    @staticmethod
    def _count_nodes_in_file(filepath: str) -> int:
        """计算文件中的节点数量"""
        try:
            node, _, _ = Serializer.load_from_file(filepath)
            return Serializer._count_nodes(node)
        except:
            return 0
    
    @staticmethod
    def _count_nodes(node: Node) -> int:
        """递归计算节点数量"""
        count = 1
        for child in node.children:
            count += Serializer._count_nodes(child)
        return count
```

---

## 6 GUI 设计

### 6.1 可切换模式视觉呈现

```
折叠态（默认）:
  ┌──────────────────────┐
  │ 🔗 登录流程           │  ← SubtreeNode，虚线边框+子树图标
  │    subtrees/login…   │  ← 灰色小字显示路径
  │    [展开预览]         │  ← 按钮或双击
  └──────┬───────────────┘
         │ 子节点连接点（输出端口）

展开态（双击后）:
  ┌──────────────────────┐
  │ 🔗 登录流程           │
  │    subtrees/login…   │
  │    [折叠预览]         │
  │  ┌─ 只读预览区 ────┐ │
  │  │ OCR检测(灰色边框) │ │  ← 只读节点，灰色/半透明
  │  │  ├─ 鼠标点击      │ │  ← 不可拖拽编辑
  │  │  └─ 延时等待      │ │
  │  └─────────────────┘ │
  └──────┬───────────────┘
```

### 6.2 SubtreeNodeItem 类设计

```python
class SubtreeNodeItem(NodeItem):
    """子树节点视觉项
    
    特殊渲染：
    - 虚线边框
    - 子树图标
    - 路径标签
    - 展开/折叠按钮
    """
    
    SUBTREE_ICON = "🔗"
    
    def __init__(self, canvas, node: Node, x: float, y: float):
        super().__init__(canvas, node, x, y)
        self._expanded = False
        self._preview_items: List[int] = []  # 预览节点 canvas item IDs
        self._subtree_data: Optional[Dict] = None
    
    def _draw(self):
        """绘制子树节点（覆盖父类）"""
        # 虚线边框
        self._border = self._canvas.create_rectangle(
            self._x - self._width/2,
            self._y - self._height/2,
            self._x + self._width/2,
            self._y + self._height/2,
            outline=self._get_border_color(),
            width=2,
            dash=(5, 3),  # 虚线
            tags=self._tags
        )
        
        # 子树图标
        self._icon = self._canvas.create_text(
            self._x - self._width/2 + 15,
            self._y - 10,
            text=self.SUBTREE_ICON,
            font=("Segoe UI Emoji", 12),
            tags=self._tags
        )
        
        # 节点名称
        self._name_text = self._canvas.create_text(
            self._x,
            self._y - 5,
            text=self._node.name or "子树",
            font=self._font,
            tags=self._tags
        )
        
        # 路径标签（灰色小字）
        path_display = self._get_short_path()
        self._path_text = self._canvas.create_text(
            self._x,
            self._y + 12,
            text=path_display,
            font=("Segoe UI", 8),
            fill="gray",
            tags=self._tags
        )
        
        # 展开/折叠按钮
        self._toggle_btn = self._canvas.create_text(
            self._x + self._width/2 - 15,
            self._y - 10,
            text="▶" if not self._expanded else "▼",
            font=("Segoe UI", 8),
            tags=self._tags
        )
        
        self._draw_ports()
    
    def _get_short_path(self) -> str:
        """获取短路径显示"""
        path = getattr(self._node, 'subtree_path', '')
        if len(path) > 20:
            return "…" + path[-17:]
        return path
    
    def toggle_preview(self):
        """切换预览状态"""
        if self._expanded:
            self._collapse_preview()
        else:
            self._expand_preview()
        self._expanded = not self._expanded
    
    def _expand_preview(self):
        """展开预览"""
        # 加载子树数据
        if self._subtree_data is None:
            self._load_subtree_data()
        
        if self._subtree_data is None:
            return
        
        # 绘制预览节点（只读）
        self._draw_preview_nodes()
        
        # 更新按钮图标
        self._canvas.itemconfig(self._toggle_btn, text="▼")
    
    def _collapse_preview(self):
        """折叠预览"""
        # 删除预览节点
        for item_id in self._preview_items:
            self._canvas.delete(item_id)
        self._preview_items.clear()
        
        # 更新按钮图标
        self._canvas.itemconfig(self._toggle_btn, text="▶")
    
    def _load_subtree_data(self):
        """加载子树数据"""
        # 实现加载逻辑
        pass
    
    def _draw_preview_nodes(self):
        """绘制预览节点（只读）"""
        # 实现预览绘制
        pass
```

### 6.3 属性面板扩展

```python
class SubtreePropertyPanel:
    """子树节点属性面板"""
    
    def __init__(self, parent, node: SubtreeNode, on_change: Callable):
        self._node = node
        self._on_change = on_change
        
        # 基础属性
        self._create_base_fields(parent)
        
        # 子树特有属性
        self._create_subtree_fields(parent)
    
    def _create_subtree_fields(self, parent):
        """创建子树特有属性字段"""
        # 子树路径
        self._path_entry = self._create_file_field(
            parent, "子树路径", 
            self._node.subtree_path,
            filetypes=[("JSON", "*.json"), ("YAML", "*.yaml")],
            on_change=self._on_path_change
        )
        
        # 黑板模式
        self._mode_menu = self._create_option_menu(
            parent, "黑板模式",
            ["inherit", "isolated", "namespaced"],
            self._node.blackboard_mode,
            on_change=self._on_mode_change
        )
        
        # 命名空间（仅 namespaced 模式显示）
        if self._node.blackboard_mode == "namespaced":
            self._namespace_entry = self._create_text_field(
                parent, "命名空间",
                self._node.namespace or self._node.name,
                on_change=self._on_namespace_change
            )
        
        # 自动重载
        self._auto_reload_switch = self._create_switch(
            parent, "自动重载",
            self._node.auto_reload,
            on_change=self._on_auto_reload_change
        )
        
        # 子树信息
        self._create_info_section(parent)
    
    def _create_info_section(self, parent):
        """创建子树信息区域"""
        info_frame = ctk.CTkFrame(parent)
        info_frame.pack(fill="x", pady=10)
        
        # 加载子树信息
        if self._node.subtree_path:
            try:
                # 显示节点数量、最后修改时间等
                pass
            except:
                pass
```

---

## 7 实现计划

### 7.1 阶段划分

```
第一阶段（核心功能）：
├── bt_core/cycle_detector.py    — 循环引用检测器
├── bt_core/blackboard.py        — NamespacedBlackboard 扩展
├── bt_core/nodes.py             — SubtreeNode 实现
└── bt_core/registry.py          — 注册 SubtreeNode

第二阶段（多树管理）：
├── bt_core/tree_manager.py      — MultiTreeManager 实现
└── bt_core/engine.py            — 引擎接口调整

第三阶段（序列化）：
├── bt_core/serializer.py        — 子树引用格式支持
└── bt_core/config.py            — 子树配置字段

第四阶段（GUI集成）：
├── bt_gui/bt_editor/canvas.py   — SubtreeNodeItem 渲染
├── bt_gui/bt_editor/property.py — 子树属性面板
├── bt_gui/bt_editor/palette.py  — 节点面板添加
└── bt_gui/bt_editor/constants.py — 类型常量

第五阶段（测试与文档）：
├── tests/test_subtree.py        — 单元测试
├── tests/test_tree_manager.py   — 多树管理测试
└── doc/04_子树引用使用指南.md    — 用户文档
```

### 7.2 预计工作量

| 阶段 | 工作量 | 优先级 |
|------|--------|--------|
| 第一阶段 | 3-4 天 | 高 |
| 第二阶段 | 2-3 天 | 高 |
| 第三阶段 | 1-2 天 | 中 |
| 第四阶段 | 4-5 天 | 中 |
| 第五阶段 | 2-3 天 | 低 |

**总计：12-17 天**

---

## 8 架构兼容性分析

### 8.1 发现的潜在冲突

#### 冲突1：ExecutionContext 缺少子树引用链追踪

**现状分析：**
当前 `ExecutionContext` 类没有 `_subtree_stack` 属性，无法追踪子树引用链。

**现有代码位置：** [bt_core/context.py](file:///d:/workspace/autodoor_behavior_tree/bt_core/context.py)

**冲突影响：**
- CycleDetector 无法正确检测循环引用
- 无法限制子树嵌套深度

**解决方案：**
```python
class ExecutionContext:
    def __init__(self, project_root: str = None):
        # ... 现有代码 ...
        self._subtree_stack: List[str] = []  # 新增：子树引用链
        self._parent_context: Optional[ExecutionContext] = None  # 新增：父上下文引用
    
    def push_subtree(self, subtree_path: str) -> None:
        """进入子树时压栈"""
        self._subtree_stack.append(subtree_path)
    
    def pop_subtree(self) -> Optional[str]:
        """退出子树时出栈"""
        return self._subtree_stack.pop() if self._subtree_stack else None
    
    def get_subtree_depth(self) -> int:
        """获取当前子树嵌套深度"""
        return len(self._subtree_stack)
    
    def is_in_subtree(self, path: str) -> bool:
        """检查路径是否已在引用链中"""
        return path in self._subtree_stack
```

---

#### 冲突2：GUI NodeItem 与 SubtreeNode 视觉呈现差异

**现状分析：**
当前 `NodeItem` 类是扁平的矩形设计，不支持虚线边框、子树图标、展开/折叠等特殊渲染。

**现有代码位置：** [bt_gui/bt_editor/node_item.py](file:///d:/workspace/autodoor_behavior_tree/bt_gui/bt_editor/node_item.py)

**冲突影响：**
- SubtreeNode 无法在画布上正确渲染
- 无法实现展开/折叠交互

**解决方案：**
方案A（推荐）—— 继承扩展：
```python
class SubtreeNodeItem(NodeItem):
    """子树节点视觉项 - 继承自 NodeItem"""
    
    def __init__(self, canvas, node_id, node_type, x, y, ...):
        super().__init__(canvas, node_id, node_type, x, y, ...)
        self._expanded = False
        self._preview_items: List[int] = []
    
    def _create_visuals(self):
        # 覆盖父类方法，创建虚线边框、子树图标等
        # ...
    
    def toggle_preview(self):
        # 切换展开/折叠状态
        # ...
```

方案B —— NodeItem 内部判断：
```python
class NodeItem:
    def _create_visuals(self):
        if self.node_type == "SubtreeNode":
            self._create_subtree_visuals()
        else:
            self._create_normal_visuals()
```

**推荐方案A**，保持代码清晰分离。

---

#### 冲突3：属性面板配置 Schema 缺失

**现状分析：**
`NODE_CONFIG_SCHEMAS` 字典没有包含 SubtreeNode 的配置定义。

**现有代码位置：** [bt_gui/bt_editor/property.py:13-150](file:///d:/workspace/autodoor_behavior_tree/bt_gui/bt_editor/property.py#L13-L150)

**冲突影响：**
- SubtreeNode 在属性面板中无法显示配置字段
- 用户无法编辑子树路径、黑板模式等参数

**解决方案：**
在 `NODE_CONFIG_SCHEMAS` 中添加：
```python
NODE_CONFIG_SCHEMAS = {
    # ... 现有配置 ...
    "SubtreeNode": [
        {"key": "subtree_path", "label": "子树路径", "type": "file", 
         "filetypes": [("JSON", "*.json"), ("YAML", "*.yaml"), ("所有文件", "*.*")]},
        {"key": "blackboard_mode", "label": "黑板模式", "type": "select", 
         "options": ["inherit", "isolated", "namespaced"], "default": "inherit"},
        {"key": "namespace", "label": "命名空间", "type": "text", 
         "hide_if": {"field": "blackboard_mode", "value": ["inherit", "isolated"]}},
        {"key": "auto_reload", "label": "自动重载", "type": "bool", "default": False},
    ],
}
```

---

#### 冲突4：节点面板分类缺失

**现状分析：**
`NODE_CATEGORY_MAP` 和 `NODE_DISPLAY_NAMES` 没有包含 SubtreeNode。

**现有代码位置：** [bt_gui/bt_editor/constants.py:1-78](file:///d:/workspace/autodoor_behavior_tree/bt_gui/bt_editor/constants.py#L1-L78)

**冲突影响：**
- SubtreeNode 无法在节点面板中显示
- 节点分类和显示名称缺失

**解决方案：**
```python
NODE_CATEGORY_MAP = {
    # ... 现有映射 ...
    "SubtreeNode": "composite",  # 或新建 "subtree" 分类
}

NODE_DISPLAY_NAMES = {
    # ... 现有名称 ...
    "SubtreeNode": "子树引用",
}

NODE_DESCRIPTIONS = {
    # ... 现有描述 ...
    "SubtreeNode": "引用外部行为树文件作为子树",
}

# 如果新建 subtree 分类，需要更新：
COMPOSITE_NODES = [..., "SubtreeNode"]
# 或新建：
SUBTREE_NODES = ["SubtreeNode"]
```

---

#### 冲突5：Canvas 节点操作不处理子树内部节点

**现状分析：**
当前 Canvas 的复制、粘贴、删除操作只处理顶层节点，不处理子树展开后的内部预览节点。

**现有代码位置：** [bt_gui/bt_editor/canvas.py](file:///d:/workspace/autodoor_behavior_tree/bt_gui/bt_editor/canvas.py)

**冲突影响：**
- 用户可能误操作子树预览节点
- 复制粘贴可能包含子树内部节点导致混乱

**解决方案：**
1. 子树展开预览的内部节点标记为 `_is_preview=True`
2. 节点操作时过滤掉预览节点：
```python
def _copy_selected_nodes_to_clipboard(self):
    # 过滤预览节点
    real_nodes = [nid for nid in self.selected_nodes 
                  if not self.nodes[nid]._is_preview]
    # ... 只复制真实节点 ...
```

---

#### 冲突6：运行时状态通知不区分主树和子树

**现状分析：**
当前 Engine 的 `_on_node_status` 回调不区分节点来自主树还是子树。

**现有代码位置：** [bt_core/engine.py:223-224](file:///d:/workspace/autodoor_behavior_tree/bt_core/engine.py#L223-L224)

**冲突影响：**
- GUI 无法正确高亮子树内部节点
- 日志输出可能混淆节点来源

**解决方案：**
扩展节点状态通知，添加子树上下文：
```python
def notify_node_status(self, node_id: str, status: str, subtree_path: str = None) -> None:
    """通知节点状态变化（支持子树上下文）"""
    if self._on_node_status:
        self._on_node_status(node_id, status, subtree_path)
```

---

### 8.2 未考虑到的内容

#### 遗漏1：子树文件变更监听

**问题描述：**
当外部修改了被引用的子树文件时，主树无法感知变更，可能导致运行时使用过期的子树定义。

**解决方案：**
新增 `SubtreeFileWatcher` 类：
```python
class SubtreeFileWatcher:
    """子树文件变更监听器"""
    
    def __init__(self):
        self._watched_files: Dict[str, Set[str]] = {}  # {文件路径: 引用该文件的节点ID集合}
        self._observer: Optional[Observer] = None
    
    def watch(self, filepath: str, node_id: str):
        """开始监听文件"""
        if filepath not in self._watched_files:
            self._watched_files[filepath] = set()
        self._watched_files[filepath].add(node_id)
    
    def on_file_changed(self, filepath: str):
        """文件变更回调"""
        for node_id in self._watched_files.get(filepath, []):
            # 通知节点重新加载
            pass
```

---

#### 遗漏2：子树缺失处理

**问题描述：**
当子树文件不存在或损坏时，需要优雅的错误处理，而不是直接崩溃。

**解决方案：**
SubtreeNode 加载失败时的处理流程：
```
1. 文件不存在 → 返回 FAILURE，日志记录 "子树文件不存在: {path}"
2. 文件格式错误 → 返回 FAILURE，日志记录 "子树文件格式错误: {error}"
3. 循环引用 → 返回 FAILURE，日志记录 "检测到循环引用"
4. 加载超时 → 返回 FAILURE，日志记录 "子树加载超时"
```

---

#### 遗漏3：子树节点 ID 冲突

**问题描述：**
子树内部的节点 ID 可能与主树中的节点 ID 冲突，导致状态通知混乱。

**解决方案：**
加载子树时对内部节点 ID 进行重映射：
```python
def _remap_node_ids(self, root_node: Node, prefix: str) -> Node:
    """重映射子树节点 ID"""
    def remap(node: Node):
        node.node_id = f"{prefix}_{node.node_id}"
        for child in node.children:
            remap(child)
    remap(root_node)
    return root_node
```

---

#### 遗漏4：子树预览的只读状态

**问题描述：**
展开预览时，内部节点应该是只读的，不能编辑属性、不能拖拽移动。

**解决方案：**
1. NodeItem 添加 `_is_readonly` 标志
2. 属性面板加载时检查只读状态
3. Canvas 拖拽时检查只读状态

```python
class NodeItem:
    def __init__(self, ...):
        # ...
        self._is_readonly = False  # 预览节点设为 True
    
    def is_readonly(self) -> bool:
        return self._is_readonly
```

---

#### 遗漏5：子树引用的相对路径处理

**问题描述：**
子树文件移动后，引用路径可能失效。需要考虑路径更新机制。

**解决方案：**
1. 存储相对路径而非绝对路径
2. 项目管理器维护路径映射表
3. 文件移动时更新所有引用

```python
class ProjectManager:
    def move_subtree_file(self, old_path: str, new_path: str):
        """移动子树文件并更新所有引用"""
        # 遍历所有行为树，更新引用
        pass
```

---

#### 遗漏6：MultiTreeManager 与 Editor 的集成

**问题描述：**
当前设计只考虑了 MultiTreeManager 的核心逻辑，未考虑如何在 GUI 中管理多个行为树。

**解决方案：**
新增 `MultiTreePanel` GUI 组件：
```
┌─────────────────────────────────────┐
│ 多树管理面板                          │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 🌲 主任务    [运行中] [⏸] [⏹]   │ │
│ │ └─ 文件: main.json              │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ 🌲 自动拾取  [已停止] [▶] [⏹]   │ │
│ │ └─ 文件: auto_loot.json         │ │
│ └─────────────────────────────────┘ │
│ [+ 添加行为树]                       │
└─────────────────────────────────────┘
```

---

#### 遗漏7：子树执行统计汇总

**问题描述：**
子树内部节点的执行统计需要汇总到主树，否则统计报告不完整。

**解决方案：**
ExecutionContext 的统计收集器需要支持层级汇总：
```python
class ExecutionContext:
    def record_node_stats(self, node_id, node_type, node_name, status, duration_ms):
        # 记录到当前上下文的统计
        if self._stats_collector:
            self._stats_collector.record_node(node_id, node_type, node_name, status, duration_ms)
        
        # 如果是子树上下文，同时汇总到父上下文
        if self._parent_context:
            prefixed_id = f"{self._namespace}.{node_id}"
            self._parent_context.record_node_stats(
                prefixed_id, node_type, node_name, status, duration_ms
            )
```

---

#### 遗漏8：子树中止传播

**问题描述：**
当主树中止时，需要正确传播到所有正在执行的子树。

**解决方案：**
SubtreeNode.abort() 需要递归传播：
```python
class SubtreeNode(CompositeNode):
    def abort(self, context: ExecutionContext) -> None:
        # 中止子树
        if self._subtree_root and self._subtree_context:
            self._subtree_root.abort(self._subtree_context)
        
        # 清理状态
        self._subtree_context = None
        self._children_running = False
        
        # 调用父类
        super().abort(context)
```

---

#### 遗漏9：最大嵌套深度限制

**问题描述：**
虽然选择了"加载时检测拒绝执行"策略，但仍需要设置最大嵌套深度作为额外保护。

**解决方案：**
```python
MAX_SUBTREE_DEPTH = 10  # 最大子树嵌套深度

class ExecutionContext:
    def can_enter_subtree(self) -> bool:
        """检查是否可以进入新的子树"""
        return len(self._subtree_stack) < MAX_SUBTREE_DEPTH
```

---

#### 遗漏10：子树热重载

**问题描述：**
开发阶段可能需要子树文件变更后自动重新加载，而不是每次重启应用。

**解决方案：**
SubtreeNode 支持 `auto_reload=True` 配置：
```python
class SubtreeNode:
    def _ensure_loaded(self, context: ExecutionContext) -> bool:
        # 如果 auto_reload=True，每次执行前检查文件修改时间
        if self.auto_reload:
            if self._has_file_changed():
                self._subtree_root = None  # 强制重新加载
        # ...
```

---

### 8.3 更新后的影响范围

| 文件 | 改动类型 | 改动量 | 新增/调整 |
|------|----------|--------|-----------|
| `bt_core/context.py` | 添加子树追踪属性 | 中 | **新增** |
| `bt_core/nodes.py` | 新增 SubtreeNode | 中 | |
| `bt_core/engine.py` | 状态通知扩展 | 小 | |
| `bt_core/serializer.py` | 子树引用格式支持 | 中 | |
| `bt_core/blackboard.py` | 新增 NamespacedBlackboard | 小 | |
| **新增** `bt_core/cycle_detector.py` | CycleDetector | 中 | |
| **新增** `bt_core/tree_manager.py` | MultiTreeManager | 大 | |
| **新增** `bt_core/file_watcher.py` | SubtreeFileWatcher | 中 | **新增** |
| `bt_core/registry.py` | 注册 SubtreeNode | 小 | |
| `bt_core/config.py` | 子树配置字段 | 小 | |
| `bt_gui/bt_editor/node_item.py` | SubtreeNodeItem 扩展 | 中 | **新增** |
| `bt_gui/bt_editor/canvas.py` | 预览节点处理 | 中 | |
| `bt_gui/bt_editor/property.py` | 子树属性面板 | 中 | |
| `bt_gui/bt_editor/palette.py` | 子树节点添加到面板 | 小 | |
| `bt_gui/bt_editor/undo_redo.py` | 子树相关Command | 小 | |
| `bt_gui/bt_editor/constants.py` | SubtreeNode 类型常量 | 小 | |
| **新增** `bt_gui/bt_editor/multi_tree_panel.py` | 多树管理面板 | 大 | **新增** |

---

## 9 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 循环引用检测遗漏 | 运行时无限循环 | 严格的单元测试覆盖 |
| 子树加载失败 | 执行中断 | 完善的错误处理和日志 |
| 黑板命名冲突 | 数据混乱 | 命名空间自动前缀 |
| GUI预览性能 | 大型子树卡顿 | 节点数量限制+懒加载 |
| 多树并行资源竞争 | 数据不一致 | 线程安全锁保护 |

---

## 9 附录：类图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              类继承关系                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Node (ABC)                                                              │
│      │                                                                   │
│      ├── CompositeNode                                                   │
│      │       │                                                           │
│      │       ├── SequenceNode                                            │
│      │       ├── SelectorNode                                            │
│      │       ├── ParallelNode                                            │
│      │       ├── RandomNode                                              │
│      │       └── SubtreeNode ◄── 新增                                    │
│      │                │                                                   │
│      │                ├── subtree_path: str                              │
│      │                ├── blackboard_mode: str                           │
│      │                ├── namespace: str                                 │
│      │                ├── _subtree_root: Node                            │
│      │                ├── _subtree_context: ExecutionContext             │
│      │                │                                                   │
│      │                ├── tick(context) -> NodeStatus                    │
│      │                ├── _ensure_loaded(context) -> bool                │
│      │                ├── _create_subtree_context(parent) -> Context     │
│      │                └── _resolve_path(context) -> str                  │
│      │                                                                   │
│      ├── ConditionNode (ABC)                                             │
│      └── ActionNode (ABC)                                                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  Blackboard                                                              │
│      │                                                                   │
│      └── NamespacedBlackboard ◄── 新增                                   │
│                  │                                                       │
│                  ├── _parent: Blackboard                                 │
│                  ├── _namespace: str                                     │
│                  ├── _prefix: str                                        │
│                  │                                                       │
│                  ├── get(key, default) -> Any                            │
│                  ├── set(key, value)                                     │
│                  ├── _wrap_key(key) -> str                               │
│                  └── get_all_keys() -> List[str]                         │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  CycleDetector ◄── 新增 (静态类)                                         │
│      │                                                                   │
│      ├── check(context, new_path) -> bool                                │
│      ├── build_reference_graph(root_path) -> Dict                        │
│      └── _extract_subtree_refs(node) -> List[str]                        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  MultiTreeManager ◄── 新增                                               │
│      │                                                                   │
│      ├── _trees: Dict[str, TreeInstance]                                 │
│      ├── _shared_blackboard: Optional[Blackboard]                        │
│      │                                                                   │
│      ├── add_tree(name, root_node) -> TreeInstance                       │
│      ├── add_tree_from_file(name, filepath) -> TreeInstance              │
│      ├── remove_tree(name) -> bool                                       │
│      ├── start_tree(name) -> bool                                        │
│      ├── stop_tree(name) -> bool                                         │
│      ├── pause_tree(name) -> bool                                        │
│      ├── resume_tree(name) -> bool                                       │
│      ├── start_all()                                                     │
│      ├── stop_all()                                                      │
│      ├── get_tree_status(name) -> Dict                                   │
│      └── get_all_status() -> Dict                                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

> 文档生成时间：2026-05-02
> 版本：v1.0
