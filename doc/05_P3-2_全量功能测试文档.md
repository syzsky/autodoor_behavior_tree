# 子树引用与多行为树并行 — 全量功能测试文档

> 版本：v1.0
> 日期：2026-05-02
> 测试范围：P3-2 子树引用机制 + 多行为树并行管理

---

## 测试环境准备

### 前置条件

1. Python 3.10+ 环境已安装
2. 项目依赖已安装（`pip install -r requirements.txt`）
3. 项目根目录：`d:\workspace\autodoor_behavior_tree`

### 测试数据准备

在项目根目录下创建测试用的子树文件：

**步骤1：创建 `subtrees` 目录**

```
d:\workspace\autodoor_behavior_tree\subtrees\
```

**步骤2：创建简单子树文件 `subtrees/simple.json`**

```json
{
  "version": "2.0",
  "format_type": "behavior_tree_standalone",
  "root_node": "delay_1",
  "nodes": {
    "delay_1": {
      "id": "delay_1",
      "type": "DelayNode",
      "name": "等待100ms",
      "config": {
        "duration_ms": 100
      }
    }
  },
  "connections": []
}
```

**步骤3：创建含变量的子树文件 `subtrees/set_var.json`**

```json
{
  "version": "2.0",
  "format_type": "behavior_tree_standalone",
  "root_node": "set_1",
  "nodes": {
    "set_1": {
      "id": "set_1",
      "type": "SetVariableNode",
      "name": "设置变量",
      "config": {
        "variable_name": "subtree_executed",
        "operation": "set",
        "value": "true"
      }
    }
  },
  "connections": []
}
```

**步骤4：创建顺序子树文件 `subtrees/sequence_sub.json`**

```json
{
  "version": "2.0",
  "format_type": "behavior_tree_standalone",
  "root_node": "seq_1",
  "nodes": {
    "seq_1": {
      "id": "seq_1",
      "type": "SequenceNode",
      "name": "子树顺序",
      "config": {}
    },
    "delay_1": {
      "id": "delay_1",
      "type": "DelayNode",
      "name": "等待50ms",
      "config": {
        "duration_ms": 50
      }
    },
    "delay_2": {
      "id": "delay_2",
      "type": "DelayNode",
      "name": "等待50ms",
      "config": {
        "duration_ms": 50
      }
    }
  },
  "connections": [
    {"parent_id": "seq_1", "child_id": "delay_1"},
    {"parent_id": "seq_1", "child_id": "delay_2"}
  ]
}
```

---

## 一、ExecutionContext 子树追踪

### TC-1.1 子树栈初始化

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-1.1 |
| **测试目标** | 验证 ExecutionContext 初始化时子树栈为空 |
| **前置条件** | 无 |
| **测试步骤** | 1. 打开终端，进入项目目录<br>2. 执行：`python -c "from bt_core.context import ExecutionContext; ctx = ExecutionContext(); print('stack:', ctx._subtree_stack); print('depth:', ctx.get_subtree_depth())"` |
| **预期结果** | 输出：`stack: []` 和 `depth: 0` |
| **实际结果** | |
| **通过/失败** | |

### TC-1.2 子树压栈与出栈

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-1.2 |
| **测试目标** | 验证 push/pop 子树路径正确工作 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.context import ExecutionContext; ctx = ExecutionContext(); ctx.push_subtree('a.json'); ctx.push_subtree('b.json'); print('depth:', ctx.get_subtree_depth()); print('in_a:', ctx.is_in_subtree('a.json')); print('in_c:', ctx.is_in_subtree('c.json')); popped = ctx.pop_subtree(); print('popped:', popped); print('depth:', ctx.get_subtree_depth())"` |
| **预期结果** | `depth: 2`，`in_a: True`，`in_c: False`，`popped: b.json`，`depth: 1` |
| **实际结果** | |
| **通过/失败** | |

### TC-1.3 子树嵌套深度限制

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-1.3 |
| **测试目标** | 验证最大嵌套深度限制为10 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.context import ExecutionContext; ctx = ExecutionContext(); for i in range(10): ctx.push_subtree(f's{i}.json'); print('can_enter_at_10:', ctx.can_enter_subtree()); print('depth:', ctx.get_subtree_depth())"` |
| **预期结果** | `can_enter_at_10: False`，`depth: 10` |
| **实际结果** | |
| **通过/失败** | |

### TC-1.4 空栈弹出

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-1.4 |
| **测试目标** | 验证空栈弹出返回 None |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.context import ExecutionContext; ctx = ExecutionContext(); result = ctx.pop_subtree(); print('result:', result)"` |
| **预期结果** | `result: None` |
| **实际结果** | |
| **通过/失败** | |

---

## 二、NamespacedBlackboard 命名空间黑板

### TC-2.1 命名空间键前缀

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.1 |
| **测试目标** | 验证命名空间自动添加前缀 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns = NamespacedBlackboard(parent, 'login'); ns.set('user', 'admin'); print('ns.get:', ns.get('user')); print('parent.get:', parent.get('login.user'))"` |
| **预期结果** | `ns.get: admin`，`parent.get: admin` |
| **实际结果** | |
| **通过/失败** | |

### TC-2.2 不同命名空间隔离

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.2 |
| **测试目标** | 验证不同命名空间之间数据隔离 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns1 = NamespacedBlackboard(parent, 'a'); ns2 = NamespacedBlackboard(parent, 'b'); ns1.set('val', 1); ns2.set('val', 2); print('ns1.val:', ns1.get('val')); print('ns2.val:', ns2.get('val')); print('parent.a.val:', parent.get('a.val')); print('parent.b.val:', parent.get('b.val'))"` |
| **预期结果** | `ns1.val: 1`，`ns2.val: 2`，`parent.a.val: 1`，`parent.b.val: 2` |
| **实际结果** | |
| **通过/失败** | |

### TC-2.3 命名空间键列表过滤

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.3 |
| **测试目标** | 验证 get_all_keys 只返回当前命名空间的键 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns = NamespacedBlackboard(parent, 'test'); ns.set('a', 1); ns.set('b', 2); parent.set('other.c', 3); print('ns keys:', sorted(ns.get_all_keys()))"` |
| **预期结果** | `ns keys: ['a', 'b']`（不包含 `other.c`） |
| **实际结果** | |
| **通过/失败** | |

### TC-2.4 命名空间递增操作

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.4 |
| **测试目标** | 验证 increment 操作正确映射到父黑板 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns = NamespacedBlackboard(parent, 'counter'); ns.set('count', 0); ns.increment('count', 5); print('ns.count:', ns.get('count')); print('parent.counter.count:', parent.get('counter.count'))"` |
| **预期结果** | `ns.count: 5`，`parent.counter.count: 5` |
| **实际结果** | |
| **通过/失败** | |

### TC-2.5 命名空间存在性检查

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.5 |
| **测试目标** | 验证 exists 检查在命名空间内工作 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns = NamespacedBlackboard(parent, 'test'); print('before:', ns.exists('key')); ns.set('key', 'val'); print('after:', ns.exists('key'))"` |
| **预期结果** | `before: False`，`after: True` |
| **实际结果** | |
| **通过/失败** | |

### TC-2.6 命名空间删除操作

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-2.6 |
| **测试目标** | 验证 delete 操作正确删除命名空间内键 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.blackboard import Blackboard, NamespacedBlackboard; parent = Blackboard(); ns = NamespacedBlackboard(parent, 'test'); ns.set('key', 'val'); ns.delete('key'); print('exists:', ns.exists('key')); print('parent exists:', parent.exists('test.key'))"` |
| **预期结果** | `exists: False`，`parent exists: False` |
| **实际结果** | |
| **通过/失败** | |

---

## 三、CycleDetector 循环引用检测

### TC-3.1 无循环引用通过

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.1 |
| **测试目标** | 验证无循环引用时检测通过 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); print(d.check(['main.json', 'a.json'], 'b.json'))"` |
| **预期结果** | `True` |
| **实际结果** | |
| **通过/失败** | |

### TC-3.2 直接循环引用检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.2 |
| **测试目标** | 验证直接循环引用被检测到 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); print(d.check(['main.json', 'a.json'], 'main.json'))"` |
| **预期结果** | `False` |
| **实际结果** | |
| **通过/失败** | |

### TC-3.3 间接循环引用检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.3 |
| **测试目标** | 验证间接循环引用被检测到 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); print(d.check(['a.json', 'b.json', 'c.json'], 'a.json'))"` |
| **预期结果** | `False` |
| **实际结果** | |
| **通过/失败** | |

### TC-3.4 路径标准化后循环检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.4 |
| **测试目标** | 验证不同路径表示（相对/绝对）仍能检测循环 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; import os; d = CycleDetector(); print(d.check(['./subtrees/login.json'], os.path.join('subtrees', 'login.json')))"` |
| **预期结果** | `False`（检测到循环，因为路径标准化后相同） |
| **实际结果** | |
| **通过/失败** | |

### TC-3.5 空栈检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.5 |
| **测试目标** | 验证空栈时任何路径都通过 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); print(d.check([], 'any.json'))"` |
| **预期结果** | `True` |
| **实际结果** | |
| **通过/失败** | |

### TC-3.6 图中循环检测（无循环）

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.6 |
| **测试目标** | 验证 DAG 图中无循环时返回 None |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); graph = {'a': ['b'], 'b': ['c'], 'c': []}; print(d.detect_cycle_in_graph(graph))"` |
| **预期结果** | `None` |
| **实际结果** | |
| **通过/失败** | |

### TC-3.7 图中循环检测（有循环）

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.7 |
| **测试目标** | 验证有循环的图被正确检测 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; d = CycleDetector(); graph = {'a': ['b'], 'b': ['c'], 'c': ['a']}; result = d.detect_cycle_in_graph(graph); print('has_cycle:', result is not None); print('cycle:', result)"` |
| **预期结果** | `has_cycle: True`，`cycle` 包含循环路径 |
| **实际结果** | |
| **通过/失败** | |

### TC-3.8 结合 ExecutionContext 检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-3.8 |
| **测试目标** | 验证 check_with_context 方法正确工作 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.cycle_detector import CycleDetector; from bt_core.context import ExecutionContext; d = CycleDetector(); ctx = ExecutionContext(); ctx.push_subtree('main.json'); print('no_cycle:', d.check_with_context(ctx, 'sub.json')); print('has_cycle:', d.check_with_context(ctx, 'main.json'))"` |
| **预期结果** | `no_cycle: True`，`has_cycle: False` |
| **实际结果** | |
| **通过/失败** | |

---

## 四、SubtreeNode 子树引用节点

### TC-4.1 SubtreeNode 初始化

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.1 |
| **测试目标** | 验证 SubtreeNode 正确初始化 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; cfg = NodeConfig(name='登录'); cfg.set('subtree_path', 'login.json'); cfg.set('blackboard_mode', 'namespaced'); node = SubtreeNode(config=cfg); print('type:', node.NODE_TYPE); print('path:', node.subtree_path); print('mode:', node.blackboard_mode)"` |
| **预期结果** | `type: SubtreeNode`，`path: login.json`，`mode: namespaced` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.2 空路径 tick 返回 FAILURE

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.2 |
| **测试目标** | 验证子树路径为空时 tick 返回 FAILURE |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; from bt_core.status import NodeStatus; node = SubtreeNode(config=NodeConfig(name='空')); ctx = ExecutionContext(); status = node.tick(ctx); print('status:', status.name)"` |
| **预期结果** | `status: FAILURE` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.3 文件不存在 tick 返回 FAILURE

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.3 |
| **测试目标** | 验证子树文件不存在时 tick 返回 FAILURE |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(name='不存在'); cfg.set('subtree_path', 'nonexistent.json'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(); status = node.tick(ctx); print('status:', status.name)"` |
| **预期结果** | `status: FAILURE` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.4 inherit 模式黑板共享

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.4 |
| **测试目标** | 验证 inherit 模式下子树共享父黑板 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(); cfg.set('blackboard_mode', 'inherit'); node = SubtreeNode(config=cfg); parent = ExecutionContext(); parent.blackboard.set('test', 'parent_val'); sub = node._create_subtree_context(parent); print('same:', sub is parent); print('val:', sub.blackboard.get('test'))"` |
| **预期结果** | `same: True`，`val: parent_val` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.5 isolated 模式黑板隔离

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.5 |
| **测试目标** | 验证 isolated 模式下子树黑板完全隔离 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(); cfg.set('blackboard_mode', 'isolated'); node = SubtreeNode(config=cfg); parent = ExecutionContext(); parent.blackboard.set('test', 'parent_val'); sub = node._create_subtree_context(parent); print('different:', sub is not parent); print('val:', sub.blackboard.get('test'))"` |
| **预期结果** | `different: True`，`val: None`（隔离，无法读取父黑板） |
| **实际结果** | |
| **通过/失败** | |

### TC-4.6 namespaced 模式黑板命名空间

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.6 |
| **测试目标** | 验证 namespaced 模式下子树黑板自动添加前缀 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; from bt_core.blackboard import NamespacedBlackboard; cfg = NodeConfig(); cfg.set('blackboard_mode', 'namespaced'); cfg.set('namespace', 'login'); node = SubtreeNode(config=cfg); parent = ExecutionContext(); sub = node._create_subtree_context(parent); print('is_ns:', isinstance(sub.blackboard, NamespacedBlackboard)); sub.blackboard.set('user', 'admin'); print('parent_val:', parent.blackboard.get('login.user'))"` |
| **预期结果** | `is_ns: True`，`parent_val: admin` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.7 加载真实子树文件并执行

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.7 |
| **测试目标** | 验证 SubtreeNode 能加载真实子树文件并执行 |
| **前置条件** | 已创建 `subtrees/simple.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; from bt_core.status import NodeStatus; import os; cfg = NodeConfig(name='简单子树'); cfg.set('subtree_path', 'subtrees/simple.json'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(project_root=r'd:\workspace\autodoor_behavior_tree'); status = node.tick(ctx); print('status:', status.name); print('loaded:', node._subtree_root is not None)"` |
| **预期结果** | `status: SUCCESS`，`loaded: True` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.8 子树执行后黑板变量验证

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.8 |
| **测试目标** | 验证子树执行后黑板变量正确设置 |
| **前置条件** | 已创建 `subtrees/set_var.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(name='设变量子树'); cfg.set('subtree_path', 'subtrees/set_var.json'); cfg.set('blackboard_mode', 'namespaced'); cfg.set('namespace', 'sub'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(project_root=r'd:\workspace\autodoor_behavior_tree'); node.tick(ctx); print('sub_executed:', ctx.blackboard.get('sub.subtree_executed'))"` |
| **预期结果** | `sub_executed: true` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.9 子树节点序列化

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.9 |
| **测试目标** | 验证 SubtreeNode 的 to_dict 包含子树配置 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; cfg = NodeConfig(name='登录'); cfg.set('subtree_path', 'login.json'); cfg.set('blackboard_mode', 'namespaced'); cfg.set('namespace', 'login'); node = SubtreeNode(config=cfg); data = node.to_dict(); print('type:', data['type']); print('path:', data['config']['subtree_path']); print('mode:', data['config']['blackboard_mode']); print('ns:', data['config']['namespace'])"` |
| **预期结果** | `type: SubtreeNode`，`path: login.json`，`mode: namespaced`，`ns: login` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.10 子树节点重置传播

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.10 |
| **测试目标** | 验证 reset 正确传播到子树 |
| **前置条件** | 已创建 `subtrees/simple.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(name='子树'); cfg.set('subtree_path', 'subtrees/simple.json'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(project_root=r'd:\workspace\autodoor_behavior_tree'); node.tick(ctx); print('before_reset_current_index:', node.current_index); node.reset(); print('after_reset_current_index:', node.current_index)"` |
| **预期结果** | `after_reset_current_index: 0` |
| **实际结果** | |
| **通过/失败** | |

### TC-4.11 循环引用被拒绝

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-4.11 |
| **测试目标** | 验证循环引用时 SubtreeNode 返回 FAILURE |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; from bt_core.status import NodeStatus; cfg = NodeConfig(name='循环'); cfg.set('subtree_path', 'main.json'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(project_root=r'd:\workspace\autodoor_behavior_tree'); ctx.push_subtree('main.json'); status = node.tick(ctx); print('status:', status.name)"` |
| **预期结果** | `status: FAILURE`（检测到循环引用） |
| **实际结果** | |
| **通过/失败** | |

---

## 五、SubtreeNode 注册与反序列化

### TC-5.1 注册表包含 SubtreeNode

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-5.1 |
| **测试目标** | 验证注册表中包含 SubtreeNode |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.registry import register_all_nodes, NodeRegistry; register_all_nodes(); cls = NodeRegistry.get('SubtreeNode'); print('registered:', cls is not None); print('type:', cls.NODE_TYPE if cls else 'N/A')"` |
| **预期结果** | `registered: True`，`type: SubtreeNode` |
| **实际结果** | |
| **通过/失败** | |

### TC-5.2 通过注册表创建 SubtreeNode

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-5.2 |
| **测试目标** | 验证通过注册表创建 SubtreeNode 实例 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.registry import register_all_nodes, NodeRegistry; register_all_nodes(); data = {'id': 'sub1', 'type': 'SubtreeNode', 'name': '测试', 'config': {'subtree_path': 'test.json', 'blackboard_mode': 'inherit'}}; node = NodeRegistry.create_node(data); print('type:', node.NODE_TYPE); print('path:', node.subtree_path)"` |
| **预期结果** | `type: SubtreeNode`，`path: test.json` |
| **实际结果** | |
| **通过/失败** | |

### TC-5.3 Serializer 反序列化 SubtreeNode

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-5.3 |
| **测试目标** | 验证 Serializer 能反序列化包含 SubtreeNode 的数据 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.serializer import Serializer; from bt_core.registry import register_all_nodes; register_all_nodes(); data = {'version': '2.0', 'format_type': 'behavior_tree_standalone', 'root_node': 's1', 'nodes': {'s1': {'id': 's1', 'type': 'SubtreeNode', 'name': '登录', 'config': {'subtree_path': 'login.json', 'blackboard_mode': 'namespaced', 'namespace': 'login'}}}, 'connections': []}; root, _, _ = Serializer.deserialize(data); print('type:', root.NODE_TYPE); print('path:', root.subtree_path); print('mode:', root.blackboard_mode)"` |
| **预期结果** | `type: SubtreeNode`，`path: login.json`，`mode: namespaced` |
| **实际结果** | |
| **通过/失败** | |

---

## 六、Serializer 子树引用支持

### TC-6.1 序列化包含子树引用元数据

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-6.1 |
| **测试目标** | 验证 serialize_with_subtrees 输出包含子树引用信息 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.serializer import Serializer; from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; cfg = NodeConfig(name='登录'); cfg.set('subtree_path', 'login.json'); node = SubtreeNode(config=cfg); data = Serializer.serialize_with_subtrees(node, project_root='/project'); print('version:', data['version']); print('format_type:', data['format_type']); print('has_refs:', 'subtree_references' in data)"` |
| **预期结果** | `version: 2.1`，`format_type: behavior_tree_with_subtrees`，`has_refs: True` |
| **实际结果** | |
| **通过/失败** | |

### TC-6.2 收集子树引用信息

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-6.2 |
| **测试目标** | 验证 _collect_subtree_refs 正确收集引用 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.serializer import Serializer; from bt_core.nodes import SubtreeNode, SequenceNode; from bt_core.config import NodeConfig; cfg1 = NodeConfig(name='A'); cfg1.set('subtree_path', 'a.json'); cfg2 = NodeConfig(name='B'); cfg2.set('subtree_path', 'b.json'); s1 = SubtreeNode(config=cfg1); s2 = SubtreeNode(config=cfg2); seq = SequenceNode(config=NodeConfig(name='主')); seq.add_child(s1); seq.add_child(s2); refs = {}; Serializer._collect_subtree_refs(seq, refs, '/project'); print('count:', len(refs)); print('paths:', [r['path'] for r in refs.values()])"` |
| **预期结果** | `count: 2`，`paths: ['a.json', 'b.json']` |
| **实际结果** | |
| **通过/失败** | |

### TC-6.3 节点计数

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-6.3 |
| **测试目标** | 验证 _count_nodes 正确计算节点数 |
| **前置条件** | 已创建 `subtrees/sequence_sub.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.serializer import Serializer; node, _, _ = Serializer.load_from_file(r'd:\workspace\autodoor_behavior_tree\subtrees\sequence_sub.json'); count = Serializer._count_nodes(node); print('count:', count)"` |
| **预期结果** | `count: 3`（1个SequenceNode + 2个DelayNode） |
| **实际结果** | |
| **通过/失败** | |

---

## 七、MultiTreeManager 多行为树并行

### TC-7.1 添加行为树实例

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.1 |
| **测试目标** | 验证 add_tree 正确添加行为树实例 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(); root = SequenceNode(config=NodeConfig(name='test')); inst = mgr.add_tree('tree1', root); print('name:', inst.name); print('status:', inst.status); print('count:', len(mgr._trees))"` |
| **预期结果** | `name: tree1`，`status: idle`，`count: 1` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.2 重名添加抛出异常

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.2 |
| **测试目标** | 验证添加重名行为树抛出 ValueError |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(); root = SequenceNode(config=NodeConfig(name='test')); mgr.add_tree('tree1', root); try: mgr.add_tree('tree1', root); print('ERROR: no exception'); except ValueError as e: print('ValueError:', str(e))"` |
| **预期结果** | 输出 `ValueError: 树实例 'tree1' 已存在` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.3 移除行为树实例

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.3 |
| **测试目标** | 验证 remove_tree 正确移除 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(); root = SequenceNode(config=NodeConfig(name='test')); mgr.add_tree('tree1', root); result = mgr.remove_tree('tree1'); print('removed:', result); print('count:', len(mgr._trees))"` |
| **预期结果** | `removed: True`，`count: 0` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.4 移除不存在的行为树

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.4 |
| **测试目标** | 验证移除不存在的树返回 False |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; mgr = MultiTreeManager(); print('result:', mgr.remove_tree('nonexistent'))"` |
| **预期结果** | `result: False` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.5 获取行为树状态

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.5 |
| **测试目标** | 验证 get_tree_status 返回正确状态 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(); root = SequenceNode(config=NodeConfig(name='test')); mgr.add_tree('tree1', root); status = mgr.get_tree_status('tree1'); print('name:', status['name']); print('status:', status['status'])"` |
| **预期结果** | `name: tree1`，`status: idle` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.6 获取所有行为树状态

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.6 |
| **测试目标** | 验证 get_all_status 返回所有树状态 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(); mgr.add_tree('t1', SequenceNode(config=NodeConfig(name='a'))); mgr.add_tree('t2', SequenceNode(config=NodeConfig(name='b'))); all_s = mgr.get_all_status(); print('count:', len(all_s)); print('keys:', sorted(all_s.keys()))"` |
| **预期结果** | `count: 2`，`keys: ['t1', 't2']` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.7 共享黑板通信

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.7 |
| **测试目标** | 验证共享黑板下不同树实例可通信 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.nodes import SequenceNode; from bt_core.config import NodeConfig; mgr = MultiTreeManager(shared_blackboard=True); i1 = mgr.add_tree('t1', SequenceNode(config=NodeConfig(name='a'))); i2 = mgr.add_tree('t2', SequenceNode(config=NodeConfig(name='b'))); i1.blackboard.set('shared', 'hello'); print('i2_sees:', i2.blackboard.get('shared'))"` |
| **预期结果** | `i2_sees: hello` |
| **实际结果** | |
| **通过/失败** | |

### TC-7.8 从文件添加行为树

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-7.8 |
| **测试目标** | 验证 add_tree_from_file 正确加载 |
| **前置条件** | 已创建 `subtrees/simple.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.registry import register_all_nodes; register_all_nodes(); mgr = MultiTreeManager(); inst = mgr.add_tree_from_file('test_tree', r'd:\workspace\autodoor_behavior_tree\subtrees\simple.json'); print('name:', inst.name); print('status:', inst.status)"` |
| **预期结果** | `name: test_tree`，`status: idle` |
| **实际结果** | |
| **通过/失败** | |

---

## 八、SubtreeFileWatcher 文件变更监听

### TC-8.1 监听文件注册

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-8.1 |
| **测试目标** | 验证 watch 正确注册监听 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.file_watcher import SubtreeFileWatcher; w = SubtreeFileWatcher(); w.watch('test.json', 'n1'); w.watch('test.json', 'n2'); nodes = w.get_watching_nodes('test.json'); print('count:', len(nodes)); print('has_n1:', 'n1' in nodes); print('has_n2:', 'n2' in nodes)"` |
| **预期结果** | `count: 2`，`has_n1: True`，`has_n2: True` |
| **实际结果** | |
| **通过/失败** | |

### TC-8.2 取消监听

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-8.2 |
| **测试目标** | 验证 unwatch 正确取消监听 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`python -c "from bt_core.file_watcher import SubtreeFileWatcher; w = SubtreeFileWatcher(); w.watch('test.json', 'n1'); w.watch('test.json', 'n2'); w.unwatch('test.json', 'n1'); nodes = w.get_watching_nodes('test.json'); print('count:', len(nodes)); print('has_n1:', 'n1' in nodes)"` |
| **预期结果** | `count: 1`，`has_n1: False` |
| **实际结果** | |
| **通过/失败** | |

### TC-8.3 文件变更检测

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-8.3 |
| **测试目标** | 验证文件修改后 check_changes 正确检测 |
| **前置条件** | 需要可写临时目录 |
| **测试步骤** | 执行：<br>`python -c "import tempfile, os, time; from bt_core.file_watcher import SubtreeFileWatcher; f = tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False); f.write('{\"a\":1}'); f.close(); w = SubtreeFileWatcher(); w.watch(f.name, 'n1'); print('no_change:', w.check_changes()); time.sleep(0.1); with open(f.name, 'w') as fh: fh.write('{\"a\":2}'); changed = w.check_changes(); print('has_change:', f.name in changed); os.unlink(f.name)"` |
| **预期结果** | `no_change: {}`，`has_change: True` |
| **实际结果** | |
| **通过/失败** | |

### TC-8.4 变更通知回调

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-8.4 |
| **测试目标** | 验证 notify_changes 触发回调 |
| **前置条件** | 需要可写临时目录 |
| **测试步骤** | 执行：<br>`python -c "import tempfile, os, time; from bt_core.file_watcher import SubtreeFileWatcher; calls = []; f = tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False); f.write('{\"a\":1}'); f.close(); w = SubtreeFileWatcher(); w.watch(f.name, 'n1'); w.set_on_file_changed(lambda fp, nid: calls.append((fp, nid))); time.sleep(0.1); with open(f.name, 'w') as fh: fh.write('{\"a\":2}'); w.notify_changes(); print('callback_count:', len(calls)); print('node_id:', calls[0][1] if calls else 'N/A'); os.unlink(f.name)"` |
| **预期结果** | `callback_count: 1`，`node_id: n1` |
| **实际结果** | |
| **通过/失败** | |

---

## 九、GUI 集成测试

### TC-9.1 节点面板显示子树引用

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.1 |
| **测试目标** | 验证节点面板中显示"子树引用"节点 |
| **前置条件** | 启动 GUI 应用 |
| **测试步骤** | 1. 启动应用：`python main.py`<br>2. 查看左侧节点面板<br>3. 在"组合节点"分类下查找"子树引用" |
| **预期结果** | 组合节点分类下显示"子树引用"节点，描述为"引用外部行为树文件作为子树" |
| **实际结果** | |
| **通过/失败** | |

### TC-9.2 拖拽添加子树引用节点

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.2 |
| **测试目标** | 验证从面板拖拽添加 SubtreeNode 到画布 |
| **前置条件** | 启动 GUI 应用 |
| **测试步骤** | 1. 从节点面板拖拽"子树引用"到画布<br>2. 观察画布上新增的节点 |
| **预期结果** | 画布上显示虚线边框的子树节点，带有🔗图标，紫色颜色条 |
| **实际结果** | |
| **通过/失败** | |

### TC-9.3 子树节点属性面板

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.3 |
| **测试目标** | 验证选中子树节点后属性面板显示正确字段 |
| **前置条件** | 画布上已有 SubtreeNode |
| **测试步骤** | 1. 点击选中画布上的子树节点<br>2. 查看右侧属性面板 |
| **预期结果** | 属性面板显示：<br>- 子树路径（文件选择器）<br>- 黑板模式（下拉：inherit/isolated/namespaced）<br>- 命名空间（选择namespaced时显示）<br>- 自动重载（开关） |
| **实际结果** | |
| **通过/失败** | |

### TC-9.4 黑板模式切换

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.4 |
| **测试目标** | 验证黑板模式切换时命名空间字段显隐 |
| **前置条件** | 画布上已有 SubtreeNode 并已选中 |
| **测试步骤** | 1. 在属性面板选择黑板模式为 "inherit"<br>2. 确认命名空间字段隐藏<br>3. 切换为 "namespaced"<br>4. 确认命名空间字段显示 |
| **预期结果** | inherit/isolated 时命名空间隐藏，namespaced 时显示 |
| **实际结果** | |
| **通过/失败** | |

### TC-9.5 子树路径文件选择

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.5 |
| **测试目标** | 验证子树路径文件选择器工作正常 |
| **前置条件** | 画布上已有 SubtreeNode 并已选中 |
| **测试步骤** | 1. 点击"子树路径"旁的浏览按钮<br>2. 选择一个 JSON 文件<br>3. 确认路径已填入 |
| **预期结果** | 文件选择器打开，支持 JSON/YAML 过滤，选择后路径正确填入 |
| **实际结果** | |
| **通过/失败** | |

### TC-9.6 子树节点保存与加载

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.6 |
| **测试目标** | 验证包含子树节点的行为树能正确保存和加载 |
| **前置条件** | 画布上已有含 SubtreeNode 的行为树 |
| **测试步骤** | 1. 配置子树节点（设置路径、黑板模式等）<br>2. 保存文件（Ctrl+S）<br>3. 关闭文件<br>4. 重新打开文件<br>5. 检查子树节点配置是否保留 |
| **预期结果** | 重新加载后子树节点的所有配置参数保留不变 |
| **实际结果** | |
| **通过/失败** | |

### TC-9.7 子树节点展开/折叠预览

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-9.7 |
| **测试目标** | 验证双击子树节点可展开/折叠预览 |
| **前置条件** | 画布上已有配置了有效路径的 SubtreeNode |
| **测试步骤** | 1. 双击子树节点<br>2. 观察是否展开预览区域<br>3. 再次双击<br>4. 观察是否折叠 |
| **预期结果** | 双击切换展开/折叠状态，展开时显示子树内部节点预览（只读） |
| **实际结果** | |
| **通过/失败** | |

---

## 十、端到端集成测试

### TC-10.1 完整子树引用执行流程

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-10.1 |
| **测试目标** | 验证从 GUI 创建子树引用到执行完成的完整流程 |
| **前置条件** | 1. 启动 GUI 应用<br>2. 已创建 `subtrees/simple.json` |
| **测试步骤** | 1. 创建新行为树<br>2. 添加 StartNode → SequenceNode → SubtreeNode<br>3. 配置 SubtreeNode 的 subtree_path 为 `subtrees/simple.json`<br>4. 保存文件<br>5. 点击运行<br>6. 观察执行结果 |
| **预期结果** | 行为树执行成功，SubtreeNode 加载并执行子树，状态显示 SUCCESS |
| **实际结果** | |
| **通过/失败** | |

### TC-10.2 多行为树并行执行

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-10.2 |
| **测试目标** | 验证 MultiTreeManager 并行执行多个行为树 |
| **前置条件** | 已创建 `subtrees/simple.json` 和 `subtrees/set_var.json` |
| **测试步骤** | 执行：<br>`python -c "from bt_core.tree_manager import MultiTreeManager; from bt_core.registry import register_all_nodes; register_all_nodes(); mgr = MultiTreeManager(shared_blackboard=True); mgr.add_tree_from_file('t1', r'd:\workspace\autodoor_behavior_tree\subtrees\simple.json'); mgr.add_tree_from_file('t2', r'd:\workspace\autodoor_behavior_tree\subtrees\set_var.json'); print('all_status:', mgr.get_all_status())"` |
| **预期结果** | 两个树实例都成功创建，状态为 idle |
| **实际结果** | |
| **通过/失败** | |

### TC-10.3 嵌套子树引用

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-10.3 |
| **测试目标** | 验证子树中再引用子树的嵌套场景 |
| **前置条件** | 1. 已创建 `subtrees/simple.json`<br>2. 创建 `subtrees/nested.json`，内容为引用 `simple.json` 的 SubtreeNode |
| **测试步骤** | 1. 创建 `subtrees/nested.json`：<br>```json<br>{"version":"2.0","format_type":"behavior_tree_standalone","root_node":"sub1","nodes":{"sub1":{"id":"sub1","type":"SubtreeNode","name":"嵌套子树","config":{"subtree_path":"subtrees/simple.json","blackboard_mode":"inherit"}}},"connections":[]}<br>```<br>2. 执行：<br>`python -c "from bt_core.nodes import SubtreeNode; from bt_core.config import NodeConfig; from bt_core.context import ExecutionContext; cfg = NodeConfig(name='外层'); cfg.set('subtree_path', 'subtrees/nested.json'); cfg.set('blackboard_mode', 'inherit'); node = SubtreeNode(config=cfg); ctx = ExecutionContext(project_root=r'd:\workspace\autodoor_behavior_tree'); status = node.tick(ctx); print('status:', status.name); print('depth:', ctx.get_subtree_depth())"` |
| **预期结果** | `status: SUCCESS`，`depth: 0`（执行完毕后已出栈） |
| **实际结果** | |
| **通过/失败** | |

---

## 十一、自动化单元测试验证

### TC-11.1 运行全部新增测试

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-11.1 |
| **测试目标** | 验证所有 60 个自动化测试全部通过 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`cd d:\workspace\autodoor_behavior_tree; python -m pytest tests/test_context_subtree.py tests/test_namespaced_blackboard.py tests/test_cycle_detector.py tests/test_subtree_node.py tests/test_serializer_subtree.py tests/test_tree_manager.py tests/test_file_watcher.py -v --tb=short` |
| **预期结果** | 60 passed, 0 failed |
| **实际结果** | |
| **通过/失败** | |

### TC-11.2 运行项目原有测试无回归

| 项目 | 内容 |
|------|------|
| **测试ID** | TC-11.2 |
| **测试目标** | 验证新增功能未破坏原有测试 |
| **前置条件** | 无 |
| **测试步骤** | 执行：<br>`cd d:\workspace\autodoor_behavior_tree; python -m pytest tests/ -v --tb=short` |
| **预期结果** | 所有测试通过，无回归失败 |
| **实际结果** | |
| **通过/失败** | |

---

## 测试结果汇总

| 测试模块 | 用例数 | 通过 | 失败 | 通过率 |
|----------|--------|------|------|--------|
| 一、ExecutionContext 子树追踪 | 4 | | | |
| 二、NamespacedBlackboard | 6 | | | |
| 三、CycleDetector | 8 | | | |
| 四、SubtreeNode | 11 | | | |
| 五、注册与反序列化 | 3 | | | |
| 六、Serializer | 3 | | | |
| 七、MultiTreeManager | 8 | | | |
| 八、SubtreeFileWatcher | 4 | | | |
| 九、GUI 集成 | 7 | | | |
| 十、端到端集成 | 3 | | | |
| 十一、自动化测试 | 2 | | | |
| **合计** | **59** | | | |

### 测试人员签字

| 角色 | 姓名 | 日期 | 签字 |
|------|------|------|------|
| 测试执行 | | | |
| 测试审核 | | | |
| 开发确认 | | | |

---

> 文档版本：v1.0
> 生成日期：2026-05-02
