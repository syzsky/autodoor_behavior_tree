**V1.7.0**

**新增内容**

**一、运行新机制**

- **节点「跳过」机制**：在节点参数中，新增区别于「启用」的「跳过」开关，跳过节点可以直接忽略当前节点，然后其子节点仍正常执行，更方便对子节点的调试使用
- **全局运行频率可配置**：设置页「运行设置」新增 tick 间隔控制，用于统一控制整体行为树执行频率（数值越高，运行频率越低），以适应更多的应用场景和不同性能档位的设备

**二、外部集成（消息总线与远程控制）**

- **消息总线与外部系统集成**：新增消息总线与 REST/WebSocket 服务端，行为树可与外部系统通信、被远程控制，并支持事件发布订阅

**三、插件系统（初版）**

- **插件扩展框架**：无需修改主程序，即可通过插件为行为树扩展自定义节点和新能力
  - 设置页新增插件管理面板，插件的加载、启动、停止、配置编辑一站式完成；插件启动后其节点自动出现在节点面板「插件节点」分类中，与内置节点一样拖拽使用
  - 命令行新增 `plugin` 命令组，行为树后台运行时插件同样生效
  - 插件异常不影响主程序运行，不同插件间节点互不冲突
  - 新增示例插件：文件处理（文件读取、写入、移动）、Excel 自动化（Excel 读取、写入、格式化）

**四、AI 助手（初版）**

- **AI 助手面板**：工具栏新增「AI助手」按钮，支持用自然语言描述自动生成行为树，或对已有行为树进行结构级修改
- **创建模式**：输入一句任务描述，AI 自动完成意图理解、节点选型、屏幕识别、脚本生成、试运行五个阶段，直接产出可运行的行为树；运行失败时 AI 分析原因并给出修复建议
- **分析修改模式**：读取当前画布的行为树，按指令完成增删改等结构调整，确认后应用
- **屏幕感知**：可通过视觉大模型分析截图，自动填充区域、坐标等参数，识别结果直接标注在画布上
- **AI 配置**：设置页新增 AI 配置区，分别填写文本模型与视觉模型的接口地址、API Key、模型名称即可使用

**优化内容**

- **调试日志增强**：精简重复日志，新增节点运行日志与错误堆栈，自动保存到应用目录的 `debug_log` 文件，便于排查问题

**修复内容**

- 修复重复引用同一子树文件时资源路径错位、提示截图不存在的问题
- 修复鼠标移动节点可能失败的问题
- 修复点击节点超过2次在当前节点卡死的问题
- 修复绑定窗口不在前台时点击可能不生效的问题

